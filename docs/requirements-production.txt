# Optional production integrations. The core local app still starts without them;
# enhanced social-card, QR, and responsive-image features use Pillow/qrcode.
boto3>=1.35,<2
psycopg[binary]>=3.2,<4
redis>=5,<6
qrcode[pil]>=8,<9
Pillow>=11,<13
argon2-cffi>=23.1,<26
cryptography>=43,<47
# Required for validated custom-font uploads and deterministic TTF/OTF -> WOFF2 optimization.
fonttools[woff]>=4.58,<5
Brotli>=1.1,<2
