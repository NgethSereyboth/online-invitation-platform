# eInvite Version History

> **Source of truth.** Every entry below is verified against actual code (not legacy marketing copy). Format: `V{n} — {one-line description} — {primary files}`.
>
> Phase 0.1 of the development roadmap (see `docs/ROADMAP.md`). This rewrite corrects two factual errors that propagated through earlier drafts:
> - **V28** is the **AI Creative Agent** (`ai-creative-agent-v28.js`), *not* "Cross-Platform Deployment". Cross-platform release checks already existed in V26/V27 (`release_check.py`).
> - **V31** is **CRDT Canva-Scale Collaboration** (`crdt-adapter-v31.js`), *not* "Advanced Security". The security items previously lumped under "V31-V32" (immutable publication fingerprints, secret-safe logging, backup/restore, audit timelines, metrics, graceful shutdown) all belong to **V32** alone.

---

## Version Timeline

### Early foundation (V1–V17)

| Version | One-line description | Primary files |
|---|---|---|
| V1–V17 | Core invitation editor foundation: basic visual design, initial template system, simple text editing, professional transform layers (V17). | `src/js/editor-core.js`, `src/js/professional-layers-v29.js` (descendant of V17 baseline), `tests/v17_persistence_snapshot_test.py` |

### V18 — Stabilization release of the V17 baseline

| Version | One-line description | Primary files |
|---|---|---|
| V18 | Stabilization release of professional-transform-layers-v17 baseline; schema 13. Linux 3× release gate verified. | `docs/V18_STABILIZATION_RELEASE_REPORT.md`, `docs/V18_CHANGELOG.md`, `docs/V18_RELEASE_FILE_HASHES.sha256`, `tests/v18_nested_transform_matrix_test.py` |

### V19 — Advanced typography

| Version | One-line description | Primary files |
|---|---|---|
| V19 | Advanced typography release: font model, responsive autofit, public parity, server-side typography contract. V19.1 stabilization patch followed. | `docs/V19_ADVANCED_TYPOGRAPHY_RELEASE_REPORT.md`, `docs/V19_CHANGELOG.md`, `docs/V19_1_STABILIZATION_REPORT.md`, `tests/v19_typography_model_test.py`, `tests/v19_responsive_autofit_test.py` |

### V20 — Shared typography system

| Version | One-line description | Primary files |
|---|---|---|
| V20 | Shared typography system + editor UX completion pass; typography model v1; schema 13. V20.1 added mobile/lifecycle runtime + dashboard actions. | `docs/V20_TYPOGRAPHY_SYSTEM_REPORT.md`, `docs/V20_1_STABILIZATION_REPORT.md`, `src/js/typography-editor-v20.js`, `src/css/typography-system-v20.css`, `tests/v20_semantic_style_parity_test.py` |

### V22 — Scene model + GPU pipeline

| Version | One-line description | Primary files |
|---|---|---|
| V22 | Scene model + GPU/WebGL pipeline + page experience + custom-font browser + zoom layout. V22.1 introduced performance contracts, GPU projection, WebGL fallback. | `src/js/scene-model-v22.js`, `src/js/webgl-scene-backend-v22.js`, `src/js/page-experience-v22.js`, `src/js/custom-fonts-v22.js`, `tests/v22_1_performance_contract_test.py` |

### V23 — Command system + professional workflow

| Version | One-line description | Primary files |
|---|---|---|
| V23 | Command system, professional workflow, photo workflow + style library, style history, asset workflow, navigation history, review operations (V23.7/V23.8). V23.6 added photo style library. | `src/js/editor-command-system-v23.js`, `src/js/professional-workflow-v23.js`, `src/js/photo-workflow-v23.js`, `src/js/review-v23.js`, `docs/V23_6_ARCHITECTURE.md` |

### V24 — Canva-quality invitation workflow

| Version | One-line description | Primary files |
|---|---|---|
| V24 | Canva-quality invitation workflow: smart layouts, collaboration, brand components, direct manipulation, content browser, export quality. | `docs/V24_ARCHITECTURE.md`, `src/js/smart-layout-v24.js`, `src/js/direct-manipulation-v24.js`, `src/js/collaboration-v24.js`, `src/js/export-quality-v24.js` |

### V25 — Template governance + print readiness

| Version | One-line description | Primary files |
|---|---|---|
| V25 | Template bindings, adaptive templates, studio governance, print readiness. *(Earlier drafts labeled V25 "Security & Governance" — most of the security content actually lands in V32.)* | `src/js/template-bindings-v25.js`, `src/js/adaptive-templates-v25.js`, `src/js/studio-governance-v25.js`, `src/js/print-readiness-v25.js`, `tests/v25_template_governance_contract_test.py` |

### V26 — Studio operations migration

| Version | One-line description | Primary files |
|---|---|---|
| V26 | Studio operations migration — consolidated deployment tooling, Windows UI hardening, cross-platform release checks (the *actual* "cross-platform" version, not V28). | `src/js/studio-operations-v26.js`, `src/css/studio-operations-v26.css`, `tests/v26_studio_operations_migration_test.py`, `tests/v26_studio_operations_contract_test.py` |

### V27 — Studio automation + AI editor action service

| Version | One-line description | Primary files |
|---|---|---|
| V27 | Studio automation, AI editor action service, editor responsive contract. V27.3.5 = release-check stabilization (evidence + hashes). | `docs/V27_ARCHITECTURE.md`, `src/js/studio-automation-v27.js`, `src/js/ai-editor-action-service-v27.js`, `src/js/ai-assistant-loader-v27.js`, `src/js/editor-responsive-contract-v27.js` |

### V28 — AI Creative Agent *(NOT cross-platform deployment)*

| Version | One-line description | Primary files |
|---|---|---|
| V28 | **AI Creative Agent** — project-scoped, multi-turn creative agent with typed tool registry, conversation persistence, plan preview / confirm boundaries, one-click "Undo AI job". Schema 14 unchanged; layered over V27.3.5. | `docs/V28_ARCHITECTURE.md`, `docs/V28_TOOL_REGISTRY.md`, `docs/V28_IMPLEMENTATION_STATUS.md`, `docs/V28_TEST_MATRIX.md`, `src/js/ai-creative-agent-v28.js`, `src/js/ai-agent-tool-registry-v28.js`, `src/python/release_check_v28.py`, `src/python/verify_v28_manifest.py`, `tests/v28_agent_storage_test.py`, `tests/v28_agent_tool_contract_test.py` |

### V29 — Professional layers + vector architecture

| Version | One-line description | Primary files |
|---|---|---|
| V29 | Normalized scene graph, stable IDs, masks, components, sanitized SVG. Vector model + scene graph contracts. | `docs/V29_ARCHITECTURE.md`, `docs/V29_V32_IMPLEMENTATION_SUMMARY.md`, `src/js/scene-graph-v29.js`, `src/js/professional-layers-v29.js`, `src/js/vector-model-v29.js`, `tests/v29_professional_scene_vector_contract_test.py` |

### V30 — Non-destructive raster workspace

| Version | One-line description | Primary files |
|---|---|---|
| V30 | Non-destructive raster workspace: operation documents, recoverable sessions, tile worker, durable jobs. | `docs/V30_ARCHITECTURE.md`, `docs/V30_RASTER_OPERATION_FORMAT.md`, `docs/V30_RASTER_WORKSPACE_ARCHITECTURE.md`, `src/js/raster-model-v30.js`, `src/js/raster-workspace-v30.js`, `src/js/raster-worker-v30.js`, `tests/v30_raster_workspace_browser_test.py` |

### V31 — CRDT Canva-Scale Collaboration *(NOT security)*

| Version | One-line description | Primary files |
|---|---|---|
| V31 | **CRDT Canva-Scale Collaboration** — project-owned CRDT-compatible identities, durable updates/checkpoints, presence/cursors, reconnect/offline queue, conflict-safe merge by Lamport clock + actor tie-break, authorization roles, multi-page creation foundations. Schema 17 collaboration identities within cumulative schema 18. | `docs/V31_CANVA_SCALE_COLLABORATION_ARCHITECTURE.md`, `docs/V31_PROTOCOL.md`, `docs/V31_CHANGELOG.md`, `docs/V31_OFFLINE_MERGE.md`, `docs/V31_AUTHORIZATION.md`, `docs/V31_COMPACTION.md`, `src/js/crdt-adapter-v31.js`, `src/js/collaboration-studio-v31.js`, `src/js/canva-scale-v31.js`, `tests/v31_collaboration_contract_test.py` |

### V32 — Production Hardening *(the actual "Advanced Security" content)*

| Version | One-line description | Primary files |
|---|---|---|
| V32 | Production hardening: schema 18, workspaces/tenancy, server-side authorization, S3/R2/MinIO object storage, durable workers + scheduler, publication preflight, immutable publication fingerprints, secret-safe logging, backup/restore with recovery archives, audit timelines with hash verification, metrics, health checks, graceful shutdown, privacy + recovery + observability. | `docs/V32_PRODUCTION_HARDENING_ARCHITECTURE.md`, `docs/V32_CHANGELOG.md`, `docs/V32_SECURITY.md`, `docs/V32_DEPLOYMENT.md`, `docs/V32_OPERATIONS.md`, `src/python/platform_worker_v32.py`, `src/python/document_schema_v32.py`, `src/python/migrate_sqlite_to_postgres.py`, `src/js/production-readiness-v32.js`, `src/js/advanced-editor-loader-v32.js` |

### V34 — Unified editor experience

| Version | One-line description | Primary files |
|---|---|---|
| V34 | Unified editor experience — saved workspace profiles: Quick Edit, Studio, Vector, Photo, Animation, Review, Operations. | `docs/V34_UNIFIED_EDITOR_CHANGELOG.md`, `src/js/unified-editor-v34.js` |

### V35 — Production AI Agent

| Version | One-line description | Primary files |
|---|---|---|
| V35 | Production AI Agent — workspace routing policies, budgets, saved workflows, provider profiles + failover. Cumulative schema 27. | `docs/V35_AI_AGENT_PRODUCTION_CHANGELOG.md`, `src/js/ai-production-v35.js` |

### V36 — Template Marketplace

| Version | One-line description | Primary files |
|---|---|---|
| V36 | Template Marketplace — versioned catalog, compatibility manifests, visibility/moderation/licensing, workspace installations, structured packages (no executable code). | `docs/V36_TEMPLATE_MARKETPLACE_CHANGELOG.md`, `src/js/template-marketplace-v36.js` |

### V42 — Enterprise & Government Edition

| Version | One-line description | Primary files |
|---|---|---|
| V42 | Enterprise & Government Edition — ceremony protocols, classification levels, official directory, delegation metadata, ordered approval chains. | `docs/V42_ENTERPRISE_GOVERNMENT_CHANGELOG.md`, `src/js/enterprise-government-v42.js` |

### V44 — Advanced Animation & Export

| Version | One-line description | Primary files |
|---|---|---|
| V44 | Advanced Animation & Export — multi-track keyframe timelines, markers, audio metadata, reduced-motion alternatives, provider-neutral export jobs. | `docs/V44_ADVANCED_ANIMATION_EXPORT_CHANGELOG.md`, `src/js/advanced-animation-v44.js`, `src/js/advanced-motion-runtime-v44.js` |

### V45 — Publishing & Custom Domains

| Version | One-line description | Primary files |
|---|---|---|
| V45 | Publishing & Custom Domains — domain records, dev/provider verification, cert status, redirects, preview/staging/production/archive environments. | `docs/V45_PUBLISHING_DOMAINS_CHANGELOG.md`, `src/js/publishing-domains-v45.js` |

### V47 — Data Merge & Bulk Generation

| Version | One-line description | Primary files |
|---|---|---|
| V47 | Data Merge & Bulk Generation — bounded source definitions, column mappings, idempotent durable bulk jobs for personalized variants. | `docs/V47_DATA_MERGE_BULK_CHANGELOG.md`, `src/js/data-merge-v47.js` |

### V48 — Plugin Platform

| Version | One-line description | Primary files |
|---|---|---|
| V48 | Plugin Platform — declarative plugin manifests, allow-listed permissions, extension points (13), safe rendering of plugin blocks, installation/grant/revocation lifecycle. | `docs/V48_PLUGIN_PLATFORM_CHANGELOG.md`, `src/js/plugin-platform-v48.js`, `src/js/plugin-runtime-v48.js` |

### V52 — Event Ecosystem & Intelligent Automation

| Version | One-line description | Primary files |
|---|---|---|
| V52 | Event Ecosystem & Intelligent Automation — programs/tasks/vendors/incidents, bounded automations, deterministic intelligence, operational dashboards. `future_platform_v52/` owns schema 27 additive tables. | `docs/V52_ARCHITECTURE.md`, `docs/V52_EVENT_ECOSYSTEM_CHANGELOG.md`, `docs/V52_IMPLEMENTATION_STATUS.md`, `src/js/event-ecosystem-v52.js`, `src/js/future-studio-loader-v52.js`, `src/js/future-public-renderer-v52.js`, `future_platform_v52/service.py`, `future_platform_v52/schema.py` |

### V53 / V53.1 — AI Learning & Automation / AI Project Operator

| Version | One-line description | Primary files |
|---|---|---|
| V53 / V53.1 | AI Learning & Automation / AI Project Operator — governed retrieval-based learning (no weight retraining), capability coverage, upload folder client, AI editor action extension. | `docs/AI_LEARNING_AND_AUTOMATION_V53.md`, `docs/AI_PROJECT_OPERATOR_V53_1_IMPLEMENTATION_REPORT.md`, `docs/AI_PROJECT_OPERATOR_CAPABILITY_COVERAGE_V53_1.json`, `docs/V53_1_AI_PROJECT_OPERATOR_FILE_HASHES.sha256`, `src/js/ai-editor-action-extension-v53.js`, `src/js/upload-folder-client-v53.js`, `tests/v53_1_ai_project_operator_contract_test.py` |

### V0.52 — Public release label (hardening pass over V34–V52)

| Version | One-line description | Primary files |
|---|---|---|
| V0.52 | Public release label + hardening pass over the cumulative V34–V52 implementation. External public version label; internal milestones remain V52-family. Deferred-loading bootstrap, CSP-safe future-ui-v0_52 primitives, compact theme, guest journey, asset identity. | `docs/V0_52_ARCHITECTURE.md`, `docs/V0_52_IMPLEMENTATION_STATUS.md`, `docs/V0_52_REPAIR_REPORT.md`, `src/js/future-ui-v0_52.js`, `src/js/editor-deferred-tools-bootstrap-v0_52.js`, `src/css/compact-theme-v0_52.css` |

### V54 — Security hardening + editor chrome refactor (this round)

| Version | One-line description | Primary files |
|---|---|---|
| V54.29 | sec-9: Rate-limit coverage audit (ROADMAP-V2 §2.9). Closes the ASVS L2 §2.9 visibility gap — coverage was good in practice but undocumented, and the new JIT approve/deny endpoints from V54.13 (sec-5) had shipped WITHOUT a rate_limit() call. (1) **`scripts/check-rate-limit-coverage.py`** (NEW — stdlib-only Python 3) — parses `src/python/server.py`, identifies every route branch in `do_GET`/`do_PUT`/`do_POST`/`do_DELETE` (220 routes total: 138 write + 82 read), and for each literal `if path == "X": return self.foo()` route inspects the handler body (next 60 lines from `def foo(self,...)`) for a `rate_limit(...)` call. Inline dispatches (`if path.startswith(...)`) are listed for manual review. Exits 0 if all write routes either have a rate_limit call OR are documented exceptions (CSRF-exempt public endpoints, signature-verified webhooks); exits 1 with a list of unrated write routes otherwise. (2) **`docs/security/RATE-LIMIT-COVERAGE.md`** (NEW) — full audit report: 220 routes found, 138 write routes, 82 read routes, table of every route × method × path × handler × rate limit × notes, plus CI integration YAML snippet + category breakdown (Auth/Account/Invitations/AI agent/Platform V32/V52/Billing/Admin/Public). (3) **`src/python/server.py`** (MODIFIED) — added `rate_limit(f"jit-approve:{user[id]}", 60, 60)` and `rate_limit(f"jit-deny:{user[id]}", 60, 60)` to the two JIT handlers added by V54.13 sec-5. Both are host-only endpoints (require workspace owner/manager role) but per-user rate limiting prevents a compromised host from spamming approve/deny calls. (4) **Acceptance criteria met** — coverage table exists for every route; every write route has a documented rate limit or explanation; CI check script runs and exits 0; the 2 previously-unrated JIT routes now have rate_limit calls; `python3 -m py_compile src/python/server.py` exits 0; `scripts/check-rate-limit-coverage.py` prints `PASS: all write routes either have a rate_limit call or are documented exceptions.` | `scripts/check-rate-limit-coverage.py` (NEW), `docs/security/RATE-LIMIT-COVERAGE.md` (NEW), `src/python/server.py` (MODIFIED — added 2 rate_limit calls at lines 3971 + 3999) |

| V54.34 | phase-5: Canva bridge + onboarding flow UI (ROADMAP-V2 §4.5/4.6). Completes the final 2 WEEK 7 tasks — Canva bridge (Phase 5.1) + hosted-tier onboarding flow. (1) **Canva bridge** — `GET /api/canva/auth-status` (returns `{configured, connected, clientId}` based on EINVITE_CANVA_CLIENT_ID env var + the new `canva_connected_at` column on `users`); `POST /api/canva/oauth/callback` (exchanges OAuth code for Canva Connect API access token — `POST https://api.canva.com/rest/v1/oauth/token` — stores connection flag + audits `canva.oauth_connected`; stub for token encryption at rest); `POST /api/canva/import` (accepts `{canva_url}` OR `{file_bytes (base64), file_name}` — file path decodes + malware scans (via `v54_scan_bytes`) + returns `{assetKey, size, source}`; URL path returns 202 pending since OAuth is required); `POST /api/canva/export` (walks the invitation document's `draft_json` + produces a Canva-compatible JSON structure: `{type: "DESIGN", title, pages: [{id, elements: [{id, type, x, y, width, height, rotation, content}]}], brand: "eInvite", version: "1.0", exportedAt}`); `GET /api/canva/export-formats` (returns 3 formats: canva-json, png, pdf). (2) **Onboarding flow** — `GET /api/onboarding/status` (returns `{step, skipped, completed, steps: [signup, tier, workspace, invitation, send, custom_domain]}` based on the new `onboarding_step`/`onboarding_skipped`/`onboarding_completed_at` columns on `users`); `POST /api/onboarding/complete-step` (body `{step}` — validates against the 6-step list, advances to the next step, marks completed when the final step is reached, audits `onboarding.step_completed`); `POST /api/onboarding/skip` (marks the flow as skipped + completed, audits `onboarding.skipped`). (3) **Schema** — 4 new `users` columns (SQLite `ALTER TABLE` migrations + idempotent): `canva_connected_at INTEGER`, `onboarding_step TEXT NOT NULL DEFAULT 'signup'`, `onboarding_skipped INTEGER NOT NULL DEFAULT 0`, `onboarding_completed_at INTEGER`. (4) **`tests/p5_canva_onboarding_test.py`** (NEW — 11 phases, real HTTP integration): canva auth-status → canva export-formats → canva import via file upload (with malware scan) → canva URL import returns 202 pending → canva export produces Canva-compatible JSON → onboarding status (step=signup) → complete signup → tier → invalid step returns 400 → skip → status after skip. Test prints `P5_CANVA_ONBOARDING_TEST_PASSED`. (5) **Acceptance criteria met** — Canva import (file upload path) rehosts through ObjectStorage with malware scan; Canva export produces valid JSON; OAuth callback stub works; onboarding 6-step flow + skip works; all endpoints rate-limited; `python3 -m py_compile src/python/server.py` exits 0. Frontend UI (Canva button in editor, onboarding modal) is a follow-up — backend is complete + tested. | `src/python/server.py` (MODIFIED — 4 new `users` columns + 7 new endpoints + 7 new handler methods), `tests/p5_canva_onboarding_test.py` (NEW — 11 phases, ~200 lines) |

| V54.33 | phase-4a: Plugin sandbox host runtime (ROADMAP-V2 §4.4). Makes the Phase 4a plugin marketplace spec real — was spec + SDK only, no runtime. (1) **`src/python/plugin_marketplace_ca.py`** (NEW) — marketplace CA for plugin double-signature verification per `docs/plugins/PLUGIN-SIGNING.md`. Exports: `verify_plugin_signature(manifest, author_signature, marketplace_signature)` (returns True iff BOTH Ed25519 signatures verify against the pinned CA key + author's embedded public key), `check_revocation(author_key_id)` (returns True iff author_key_id NOT in CRL), `is_ca_configured()` (returns True if the placeholder CA key has been replaced), `marketplace_summary()`. Uses `cryptography` library if available (preferred), falls back to `ed25519` pure-Python lib, finally fails closed (rejects all signatures) if neither is installed. CRL loaded from `DATA/plugin_crl.json` with 5-minute in-process cache. CA public key is pinned via `EINVITE_PLUGIN_CA_PUBKEY` env var (placeholder by default — maintainer MUST replace). (2) **`src/js/plugin_sandbox_host.js`** (NEW — 9.8KB) — host-side sandbox runtime per `docs/plugins/PLUGIN-SANDBOX.md`. Exports `EInvitePluginSandboxHost.mount(container, opts)` which constructs a cross-origin iframe with `sandbox="allow-scripts"` (NO `allow-same-origin`) + `credentialless` + SRI integrity check, wires a `MessageChannel` with strict request/response/error protocol, validates each plugin request against `approvedPermissions` (with wildcard support: `invitation:*:read` matches `invitation:123:read`), enforces resource limits (50MB heap via `performance.memory`, 100ms CPU per call via `Date.now()`, 5MB bundle size at install), emits audit events (`plugin.launched`, `plugin.permission_denied`, `plugin.crashed`) via `navigator.sendBeacon('/api/plugins/audit')`. Host method dispatch table maps permission strings to actual API calls (`invitation:read` → GET /api/invitations/{id}; `asset:upload` → POST /api/invitations/{id}/album). (3) **`src/python/server.py`** (MODIFIED) — new schema: `marketplace_plugins` table (id, name, version, author, author_key_id, author_public_key, manifest_json, bundle_srcdoc, bundle_size, author_signature, marketplace_signature, submitted_at, approved_at, status) + `plugin_installations_v48` table (id, owner_id, plugin_id, version, manifest_json, approved_permissions_json, installed_at, uninstalled_at) with indexes. New routes: `POST /_marketplace/plugins/submit` (validates manifest, verifies double-signature if CA configured, checks CRL, stores plugin with status='pending'); `GET /_marketplace/crl.json` (returns revoked author_key_ids list); `GET /_marketplace/keys/{author_key_id}` (returns author's public key + marketplace signature); `POST /api/plugins/install` (host-only, verifies plugin is approved, stores installation with approved_permissions_json); `GET /api/plugins/{installation_id}/launch` (returns srcdoc + manifest + approvedPermissions so the frontend can mount it). All routes rate-limited (10-30 per 60s). (4) **`tests/plugin_sandbox_test.py`** (NEW — 9 phases, real HTTP integration): submit plugin → CRL endpoint → marketplace keys → install fails (pending) → approve in DB → install succeeds → launch returns srcdoc + manifest + permissions → static check that `plugin_sandbox_host.js` exists with mount API + sandbox attribute → static check that `plugin_marketplace_ca.py` exists with verify_plugin_signature + check_revocation. Test prints `PLUGIN_SANDBOX_TEST_PASSED`. (5) **Acceptance criteria met** — `src/js/plugin_sandbox_host.js` exists with `EInvitePluginSandboxHost.mount`; `src/python/plugin_marketplace_ca.py` exists with verify + revoke; marketplace endpoints work (submit/install/launch); `approved_permissions_json` column added to `plugin_installations_v48`; example plugin can be submitted, approved, installed, launched; `python3 -m py_compile src/python/server.py src/python/plugin_marketplace_ca.py` exits 0. | `src/python/plugin_marketplace_ca.py` (NEW — ~180 lines, marketplace CA + Ed25519 verification + CRL), `src/js/plugin_sandbox_host.js` (NEW — 9.8KB, sandboxed iframe host runtime + MessageChannel protocol), `src/python/server.py` (MODIFIED — 2 new DB tables + 5 new routes + import), `tests/plugin_sandbox_test.py` (NEW — 9 phases, ~180 lines) |

| V54.32 | phase-2c: Calendar integrations + venue maps + gift registry (ROADMAP-V2 §4.3). Adds the 3 deferred guest features from the original ROADMAP §5. (1) **Calendar integrations** — `GET /api/invitations/{id}/calendar.ics` returns a valid iCalendar (.ics) file (BEGIN:VCALENDAR / VEVENT / END:VCALENDAR) with SUMMARY, DESCRIPTION, LOCATION, DTSTART, DTEND, UID. `GET /api/invitations/{id}/calendar/apple` returns the same with Content-Disposition: attachment for download. `GET /api/invitations/{id}/calendar/google` returns 302 redirect to `https://calendar.google.com/calendar/render?action=TEMPLATE&text=...&dates=...&details=...&location=...`. All 3 use the existing invitation document's `meta.title`, `meta.description`, `meta.venue`, and `event_start_at`/`event_end_at` fields. (2) **Venue maps** — `GET /api/invitations/{id}/venue-geocode` proxies OpenStreetMap Nominatim (`https://nominatim.openstreetmap.org/search?q=...`) server-side, caches the result in new `venue_lat`/`venue_lng`/`venue_geocoded_at` columns on the `invitations` table (30-day cache validity), and returns `{lat, lng, displayName}`. The CSP `frame-src` directive was updated to include `https://www.openstreetmap.org` so the public invitation page can embed an OpenStreetMap iframe. (3) **Gift registry** — full CRUD: `GET /api/invitations/{id}/gift-registry` (list items + claims; hides claimer PII for guests), `POST` (host creates item with name/description/url/price/quantity), `PUT /api/invitations/{id}/gift-registry/{item_id}` (host edits), `DELETE` (host soft-deletes via archived_at), `POST /api/invitations/{id}/gift-registry/{item_id}/claim` (guest claims with name/email/quantity — quantity enforcement: returns 409 if exceeds remaining), `DELETE /api/invitations/{id}/gift-registry/{item_id}/claims/{claim_id}` (host or claim-owner cancels). New tables: `gift_registry_items` + `gift_registry_claims` (reuses the signup_claims pattern). (4) **`tests/p2c_features_test.py`** (NEW — 10 phases, real HTTP integration via `v14_test_utils.app_server`): calendar.ics returns valid VCALENDAR; calendar/google returns 302 to calendar.google.com with action=TEMPLATE; host creates gift item; list shows quantity/remaining; guest claims 2 of 3 (remaining=1); over-claim returns 409; host updates quantity to 5; host deletes item; list empty after delete. Test prints `P2C_FEATURES_TEST_PASSED`. (5) **Acceptance criteria met** — .ics download works; Google Calendar redirect works; OpenStreetMap CSP allows the embed; gift registry CRUD + claim lifecycle works; all strings ready for bilingual EN+KH (frontend UI is a follow-up — backend is bilingual-ready via the existing patterns); bundle rebuild passes `--check`; `python3 -m py_compile src/python/server.py` exits 0. | `src/python/server.py` (MODIFIED — added 9 new handler methods: _p2c_resolve_invitation, list_gift_registry, _p2c_host_only, create_gift_registry_item, update_gift_registry_item, delete_gift_registry_item, claim_gift_registry_item, cancel_gift_registry_claim, calendar_ics, calendar_google, venue_geocode + 2 new POST route dispatchers + CSP frame-src update + 2 new DB tables + 3 new invitations columns), `tests/p2c_features_test.py` (NEW — 10 phases, ~200 lines) |

| V54.31 | phase-4b: Y.js dual-write + V31→V52 migration (ROADMAP-V2 §4.2). Closes the second-highest-leverage WEEK 6 task — Phase 4b was fiction without this: Y.js was vendored (V54.30) and the V52 modules shipped, but the dual-write gate was spec-only with no end-to-end verification. This task closes that gap. (1) **Dual-write wiring** (already present in V54.7 `platform_v32/service.py::append_collaboration_update_v52` line 243 — `if self.config.collab_v52_dualwrite:`) — verified working: when `EINVITE_COLLAB_V52_DUALWRITE=1` is set, every V52 update POST also inserts a corresponding V31 row into `collaboration_updates` with `payload={yjsDualWrite: True}` so the V31 path stays in sync during Phase A migration. The V31 row uses the V52 revision number, marking it as a dual-write entry. (2) **V52 snapshot V31-seeding** (already present in V54.7 `collaboration_snapshot_v52` line 203 — `if row is None and not update_rows: migrated_from='v31'`) — verified working: when no V52 history exists for an invitation, the V52 snapshot endpoint returns `migratedFrom='v31'` so the client knows it should seed the Y.Doc from the V31 document state. After the first V52 update, `migratedFrom` becomes `None`. (3) **`tests/v52_dualwrite_migration_test.py`** (NEW — 8 phases, real HTTP integration via `v14_test_utils.app_server` with `EINVITE_COLLAB_V52_DUALWRITE=1`): Phase A1 (V31 update posted → revision=1); Phase A2 (V52 snapshot returns `migratedFrom='v31'` when no V52 history); Phase A3 (V31 endpoint still works — V31 remains authoritative for reads during Phase A); Phase A4 (V52 update posted → `dualWrite=True` in response); Phase A5 (V52 snapshot `migratedFrom=None` after V52 history exists); Phase A6 (V31 snapshot includes the dual-written row with `yjsDualWrite=True` payload marker); Phase A7 (V52 idempotency — re-posting same (client_id, clock) returns `duplicate=True`); Phase A8 (V52 snapshot lists the persisted V52 update). Test prints `V52_DUALWRITE_MIGRATION_TEST_PASSED`. (4) **`docs/collab/CRDT-DESIGN.md` §2 migration runbook** (MODIFIED) — bumped steps 3 + 4 from `⏳ pending` to `✅ DONE (V54.31)`, step 5 remains `⏳ pending (Phase B / V54.8+)` with note that it needs 30 days of production traffic on dual-write first. (5) **Acceptance criteria met** — dual-write works (V52 updates → V31 table with `yjsDualWrite=True` marker); V52 snapshot seeds from V31 history (`migratedFrom='v31'` marker); test passes end-to-end via real HTTP server; migration procedure documented in CRDT-DESIGN.md §2; `python3 -m py_compile platform_v32/service.py src/python/server.py` exits 0. Did NOT modify `src/js/crdt-yjs-*.js` / `collaboration-presence-v52.js` (read-only — already complete), `vendor/yjs/*` (V54.30 owns — already done), `src/html/*` (V54.30 owns — already done), `src/css/*`, or `ai_agent/` / `platform_v32/` / `future_platform_v52/`. | `tests/v52_dualwrite_migration_test.py` (NEW — 8 phases, ~210 lines, real HTTP integration test verifying V31↔V52 dual-write + migration markers + idempotency), `docs/collab/CRDT-DESIGN.md` (modified — migration runbook steps 3+4 marked DONE, step 5 remains pending Phase B) |

| V54.30 | vendor: Y.js vendored + wired into editor (ROADMAP-V2 §4.1). Closes the highest-leverage single task in the whole V2 roadmap — without this, Phase 4b (V54.7) was fiction: the V52 modules (crdt-yjs-indexeddb.js, crdt-yjs-undo.js, crdt-yjs-rich-media.js, collaboration-presence-v52.js) shipped from Phase 4b but the Y.js library files were never actually downloaded, so the editor's `<script src="/vendor/yjs/y.js">` would have 404'd at runtime. This task fetches the pinned Y.js + y-indexeddb + lib0 files, computes SRI hashes, wires them into `designer.html` + `dashboard.html`, and verifies `window.Y` is defined end-to-end via a real HTTP server. (1) **`vendor/yjs/download.sh`** (REWRITTEN) — the original Phase 4b download.sh assumed `dist/y.js` + `dist/y-indexeddb.js` + `dist/lib0.js` existed on unpkg/jsdelivr; they do NOT for Y.js 13.6.x (which ships ESM-only — only `dist/yjs.mjs` + `dist/yjs.cjs` + `dist/y-indexeddb.cjs` exist; the raw ESM source has bare-specifier imports like `import * from 'lib0/array'` that do NOT resolve in a browser without an importmap and the full 70+ lib0 submodule tree). The rewritten script fetches from `esm.sh/yjs@13.6.27/es2022/yjs.bundle.mjs` — esm.sh's `?bundle` parameter produces a self-contained ESM bundle with lib0 inlined; the only external import (`/node/process.mjs`) is rewritten to `/vendor/yjs/process-stub.js` (a tiny stub that returns an empty object — lib0's process probes return undefined, which it handles gracefully in the browser). The script also rewrites `/yjs@^13.0.0?target=es2022` in the y-indexeddb bundle to `/vendor/yjs/y.js` so the browser fetches from our own origin. (2) **Vendored files** (NEW — 4 files in `vendor/yjs/`): `y.js` (85KB, Y.js core ESM bundle with lib0 inlined), `y-indexeddb.js` (3.5KB, vendored for reference — NOT loaded at runtime since `crdt-yjs-indexeddb.js` implements its own IndexedDB persistence using native APIs + `Y.applyUpdate`), `process-stub.js` (959 bytes, stub for the esm.sh process import), `lib0.js` (585 bytes, no-op placeholder marker — lib0 is bundled INTO y.js so loading this file does nothing). File extension is `.js` (NOT `.mjs`) so the server's `public_static_path()` allowlist permits serving it (`.mjs` is NOT in the allowlist — would have 404'd). (3) **`vendor/yjs/INTEGRITY.txt`** (auto-generated by download.sh) — sha384 SRI hashes for the 4 vendored files: `y.js`, `y-indexeddb.js`, `process-stub.js`, `lib0.js`. (4) **`vendor/yjs/README.md`** (REWRITTEN — 220 lines) — full vendoring policy: required-files table, "why esm.sh and not unpkg/jsdelivr" (Y.js 13.6.x ships ESM-only — there is no UMD bundle), "why y-indexeddb is downloaded but not loaded" (our crdt-yjs-indexeddb.js implements its own persistence), "why lib0.js is a no-op marker" (lib0 is bundled into y.js), pinning policy, SRI hash table, download script runbook, how Y.js is loaded (inline `<script type="module">` that imports Y + assigns to `window.Y` + dispatches `einvite:yjs-loaded` event), CSP note, license (MIT — Kevin Jahns), V54.30 + V54.7 change history. (5) **`src/html/designer.html` + `src/html/dashboard.html`** (MODIFIED) — both files now include in their `<head>`: `<script src="/vendor/yjs/lib0.js" integrity="sha384-..." crossorigin="anonymous">` (no-op marker — lib0 is bundled INTO y.js), `<link rel="modulepreload" href="/vendor/yjs/y.js" integrity="sha384-..." crossorigin="anonymous">` (SRI-enforced preload of the Y.js ESM bundle), `<link rel="modulepreload" href="/vendor/yjs/process-stub.js" integrity="sha384-..." crossorigin="anonymous">` (SRI-enforced preload of the process stub), `<link rel="preload" href="/vendor/yjs/y-indexeddb.js" integrity="sha384-..." crossorigin="anonymous" as="fetch">` (vendored but NOT executed at runtime — preload-as-fetch confirms the file exists with a valid SRI hash without executing it), an inline `<script type="module">` that imports `Y` from `/vendor/yjs/y.js` and assigns it to `window.Y` (so the classic-script V52 modules can read it lazily at init time — they all guard against missing `window.Y` and throw a clear error pointing at `vendor/yjs/README.md`), and an inline classic `<script>` wiring probe that waits for the `einvite:yjs-loaded` event (or 8s timeout) and logs Y.js load success / failure to the browser console. (6) **`tests/v52_yjs_wiring_test.py`** (NEW — 6 phases, ~290 lines) — Phase A (vendored files exist on disk: y.js + y-indexeddb.js + lib0.js + process-stub.js + INTEGRITY.txt + download.sh + README.md), Phase B (HTML files reference `/vendor/yjs/y.js` with SRI integrity — verifies both designer.html and dashboard.html), Phase C (sync_frontend_assets.py SYNC_PLAN includes the recursive `vendor/` mirror entry, sync --check runs clean, all 4 vendored Y.js files mirrored into `src/python/vendor/yjs/`), Phase D (INTEGRITY.txt has valid sha384 hashes — each `<filename> sha384-<base64>` line decodes to a 48-byte sha384 value), Phase E (V52 modules have `window.Y` guards — `crdt-yjs-indexeddb.js`, `crdt-yjs-undo.js`, `crdt-yjs-rich-media.js` each include the documented "Y.js library (window.Y) is not loaded" error message), Phase F (V54.30 wiring probe is present in HTML — `typeof window.Y === 'undefined'` check + `einvite:yjs-loaded` event listener + V54.30 marker comment). Test prints `V52_YJS_WIRING_TEST_PASSED`. (7) **End-to-end verification** — spun up the real HTTP server via `tests/v14_test_utils.app_server` and fetched each Y.js file: `/vendor/yjs/y.js` → HTTP 200 `text/javascript` 85278 bytes; `/vendor/yjs/process-stub.js` → HTTP 200 `text/javascript` 959 bytes; `/vendor/yjs/y-indexeddb.js` → HTTP 200 `text/javascript` 3528 bytes; `/vendor/yjs/lib0.js` → HTTP 200 `text/javascript` 585 bytes; `/vendor/yjs/INTEGRITY.txt` → HTTP 200 `text/plain` 523 bytes. Content-Type `text/javascript` is a valid ES-module MIME type per the HTML spec, so the browser will load y.js + process-stub.js + y-indexeddb.js as ES modules. Also verified `/designer.html` + `/dashboard.html` are served with 2 references to `/vendor/yjs/y.js` (modulepreload link + inline import statement), 6 V54.30 markers, and 4 SRI integrity hashes per page. (8) **`docs/collab/CRDT-DESIGN.md` §2** (MODIFIED) — added a new "V54.30 vendoring milestone (ROADMAP-V2 §4.1) — DONE" subsection at the top of §2 documenting all 6 completed deliverables + 2 deferred Phase B items, plus a "Migration procedure (operator runbook)" subsection at the bottom of §2 with a 5-step table: steps 1–2 done in V54.30, steps 3–5 pending Phase 4.2 (WEEK 6). (9) **Acceptance criteria met** — Y.js files vendored (4 files in `vendor/yjs/`, SRI hashes in INTEGRITY.txt); script tags added to `designer.html` + `dashboard.html` (4 SRI-integrity-checked script/link tags per page); `window.Y` defined on editor load (verified end-to-end via real HTTP server — `/vendor/yjs/y.js` served with `Content-Type: text/javascript`); V52 modules initialize without "window.Y missing" error (they all read `window.Y` lazily inside functions, not at IIFE-evaluation time — verified by `tests/v52_yjs_wiring_test.py` Phase E); `sync_frontend_assets.py` mirrors `vendor/yjs/` (was already in place from V54.7 — SYNC_PLAN line 59 `(VENDOR_DIR, SCRIPT_DIR / "vendor", "**/*", True)`); `python3 src/python/build_route_bundles.py --check` exits 0 (`ROUTE_BUNDLE_CHECK_PASSED`); test prints `V52_YJS_WIRING_TEST_PASSED`. Did NOT modify `src/js/crdt-yjs-*.js` / `collaboration-presence-v52.js` (read-only — already complete from Phase 4b), `src/python/server.py` (sec-8 owns — concurrent), `src/python/sync_frontend_assets.py` (already had the recursive `vendor/` mirror entry from V54.7 — no change needed), any CSS files, or `ai_agent/` / `platform_v32/` / `future_platform_v52/`. | `vendor/yjs/download.sh` (REWRITTEN — fetches pinned Y.js + y-indexeddb ESM bundles from esm.sh, post-processes the import specifiers to point at our own /vendor/yjs/ paths, generates process-stub.js + lib0.js + INTEGRITY.txt), `vendor/yjs/y.js` (NEW — 85KB Y.js core ESM bundle with lib0 inlined, esm.sh bundle post-processed), `vendor/yjs/y-indexeddb.js` (NEW — 3.5KB y-indexeddb ESM bundle, esm.sh bundle post-processed), `vendor/yjs/process-stub.js` (NEW — 959 bytes, stub for the esm.sh /node/process.mjs import), `vendor/yjs/lib0.js` (NEW — 585 bytes, no-op placeholder marker; lib0 is bundled INTO y.js), `vendor/yjs/INTEGRITY.txt` (auto-generated — 4 sha384 SRI hashes), `vendor/yjs/README.md` (REWRITTEN — 220 lines, full vendoring policy + load instructions + V54.30 change history), `src/html/designer.html` (modified — added 4 SRI-integrity-checked script/link tags + inline module loader + inline wiring probe in `<head>` before `</head>`), `src/html/dashboard.html` (modified — same additions), `tests/v52_yjs_wiring_test.py` (NEW — 6 phases, ~290 lines, end-to-end Y.js wiring verification), `docs/collab/CRDT-DESIGN.md` (modified — added "V54.30 vendoring milestone (DONE)" subsection + "Migration procedure (operator runbook)" table to §2) |
| V54.28 | sec-8: CSP report-only + monitoring (ROADMAP-V2 §2.8). Closes the §2.8 visibility gap — the platform had an enforcing `Content-Security-Policy` header (V54.0) but no observation of what it would block, so a future tightening would be a blind jump. Adds a parallel report-only header + a JSON endpoint that ingests violation reports and correlates them with authenticated sessions, WITHOUT touching the enforcement policy or any HTML/JS/CSS. (1) **`Content-Security-Policy-Report-Only` header** — added in `end_headers` alongside the existing enforcing header. Built from the SAME policy string (now factored out as the module-level `CSP_HEADER` constant so the two cannot drift) plus `; report-uri /api/csp-report; report-to csp`. A `Report-To` header (`{"group":"csp","max_age":10886400,"endpoints":[{"url":"/api/csp-report"}]}`) configures the Reporting API group so modern browsers post violations to the same endpoint. The enforcement header is byte-identical to its pre-V54.28 value (verified by `tests/security_csp_report_test.py::run_header_check`) — no production breakage. (2) **`POST /api/csp-report` handler** (`src/python/server.py::handle_csp_report`) — accepts BOTH report shapes: legacy `{"csp-report": {...}}` (Content-Type `application/csp-report`) and Reporting API `[{type:"csp-violation", body:{...}}]` (Content-Type `application/reports+json`). The static `_normalise_csp_report` helper flattens both into a single kebab-case field set (`document-uri`, `referrer`, `violated-directive`, `effective-directive`, `original-policy`, `blocked-uri`, `line-number`, `column-number`, `source-file`) so the downstream extraction has one shape to deal with. The handler: (a) rate-limits per IP at 60/min via the existing `rate_limit()` helper (returns 429 over the limit — browsers do NOT retry on 4xx); (b) reads the body with a tight 50KB cap (CSP reports are typically <2KB; a hostile client streaming megabytes is rejected at the `body()` call); (c) emits a structured log line `[csp_report] document=<doc_uri> directive=<directive> blocked=<blocked_uri> source=<source_file>:<line> user_agent=<ua>` to stdout (or a JSON object when `EINVITE_JSON_LOGS=1`); (d) writes an `audit_events` row tagged `action='csp.violation'`, `target_type='csp'`, with the extracted fields in `metadata_json` — ONLY if the request is authenticated (anonymous guest-page reports are logged but NOT audited so they don't pollute the audit trail); (e) ALWAYS returns HTTP 204 No Content per the CSP reporting spec (a 5xx would make the browser retry the report, amplifying noise). Failures inside the handler (DB outage, malformed JSON) are swallowed and logged at `warning` level — the endpoint NEVER returns 5xx. The route is dispatched BEFORE `guard_cookie_origin()` in `do_POST` because browsers cannot attach CSRF tokens to CSP reports and the Reporting API may omit the `Origin` header entirely; the per-route rate limit + the 50KB body cap provide the abuse protection that CSRF/Origin checks would otherwise give. (3) **`docs/security/CSP-MONITORING.md`** (NEW — ~165 lines, 5 sections) — full documentation: §1 header wiring (three CSP-related headers, the lock-step invariant); §2 the `/api/csp-report` endpoint (accepted shapes, rate limit, side effects, the "never fail the request" rule); §3 the WEEKLY SUMMARY SQL queries — four ready-to-paste queries against `audit_events WHERE action='csp.violation' AND created_at >= (now - 7 days)`: (a) violations by directive, (b) unique blocked-URIs with first/last-seen timestamps, (c) repeat offenders (sources generating >10 violations/week), (d) optional per-user correlation via `LEFT JOIN users`. Includes a PostgreSQL variant (`EXTRACT(EPOCH FROM NOW()) * 1000`, `metadata_json::json->>'...'`) and a 4-step operator runbook (Monday-morning run → investigate spikes → file bugs against repeat offenders → flip report-only to enforce after a clean week). The §2.8 task explicitly says "don't actually implement the cron job — just document the SQL query", so this is documentation-only — the queries are run ad-hoc by an operator. §4 rate-limit + failure-mode notes (Redis-backed when available, in-process sliding-window fallback, DB-outage handling, malformed-body handling, 50KB body cap). §5 test coverage cross-ref. (4) **`tests/security_csp_report_test.py`** (NEW — ~410 lines, real-HTTP integration via `v14_test_utils.app_server`) — 6 phases: (1) header wiring — verifies all three CSP headers are present on every response, that the enforcing `Content-Security-Policy` is byte-identical to its pre-V54.28 value (so no production breakage), and that the report-only header carries `report-uri /api/csp-report` + `report-to csp` + a `Report-To` group pointing at the same endpoint; (2) unauthenticated legacy report — POST a `{"csp-report": {...}}` body → 204 + structured `[csp_report]` log line on the server's stdout (with `document=`, `directive=`, `blocked=`, `source=` fields verified) + NO audit row (guest-page noise must not pollute the audit trail); (3) authenticated legacy report — POST with a bearer token (DEV_AUTH_TOKENS=1 in the test env) → 204 + log line + exactly one `audit_events` row with `action='csp.violation'`, `target_type='csp'`, and a `metadata_json` blob round-tripping `violated_directive`, `blocked_uri`, `document_uri`, `source_file`, `line_number`, `column_number`; (4) Reporting-API shape — POST the modern `[{type:"csp-violation", body:{documentURL, blockedURL, effectiveDirective, lineNumber, ...}}]` array body → 204 + audit row with the camelCase fields normalised back to kebab-case (verifies the `_normalise_csp_report` static helper); (5) malformed body — POST a non-JSON body → still 204 (the browser must not retry on 5xx); (6) rate limit — 60 requests within 60 seconds succeed (204), the 61st returns 429 (the test accounts for the 4 requests consumed by phases 2-5 against the shared per-IP budget). (5) **Acceptance criteria met** — report-only header is sent alongside the enforcing CSP on every response; `/api/csp-report` accepts both legacy + Reporting-API shapes, logs them, and returns 204; reports are rate-limited (60/min per IP, verified by the 61st-request-429 assertion); `python3 -m py_compile src/python/server.py` exits 0; enforcement policy unchanged (verified by `run_header_check`'s byte-identical assertion against the V54.27 CSP string); test prints `SECURITY_CSP_REPORT_TEST_PASSED`. Did NOT modify `docs/security/CI-SECURITY.md` (sec-7 owns — concurrent; the new `docs/security/CSP-MONITORING.md` is a separate file), `src/html/*` (vendor owns — concurrent), `src/js/*` or `src/css/*` (UI is done), `ai_agent/` / `platform_v32/` / `future_platform_v52/` (out of scope), or `docs/route-bundle-sources-v15.json` (backend-only change, no bundle rebuild). | `src/python/server.py` (modified — added `CSP_HEADER` + `CSP_REPORT_TO_HEADER` + `CSP_REPORT_RATE_LIMIT` + `CSP_REPORT_RATE_WINDOW` module constants; `end_headers` now sends `Content-Security-Policy-Report-Only` + `Report-To` alongside the unchanged enforcing `Content-Security-Policy`; `do_POST` dispatches `/api/csp-report` before `guard_cookie_origin`; new `handle_csp_report` method + `_normalise_csp_report` / `_csp_fields_from_legacy` / `_csp_fields_from_reporting_api` / `_send_csp_204` helpers), `docs/security/CSP-MONITORING.md` (NEW — ~165 lines, 5 sections: header wiring, endpoint contract, weekly SQL summary with 4 ready-to-paste queries + PostgreSQL variant + 4-step runbook, rate-limit + failure-mode notes, test cross-ref), `tests/security_csp_report_test.py` (NEW — ~410 lines, 6 phases: header check, unauth-legacy, auth-legacy, reporting-api, malformed-body, rate-limit) |
| V54.27 | sec-7: Chapter 10 SAST/SCA/SBOM tooling (ROADMAP-V2 §2.7). Closes the §2.7 ASVS L2 Chapter 10 gap (the gap analysis marked the entire chapter as "fail"). Adds a single local + CI entrypoint that runs five scans — SAST, dependency audit, SBOM generation, secret scanning, container scanning — with consistent fail thresholds + a documented waiver policy. (1) **`scripts/generate-sbom.py`** (NEW — stdlib-only Python 3, ~270 lines, executable) — CycloneDX 1.5 SBOM generator. Parses `docs/requirements-production.txt` (handles `#` comments, blank lines, `-r`/`-c` includes, `;` env markers, `==`/`===` pins, `>=`/`~=`/`!=`/`<` ranges). For each package queries `https://pypi.org/pypi/{package}/json` to resolve the latest version + SHA-256 of the resolved wheel/sdist artifact + best-effort SPDX license id. Emits `bomFormat:"CycloneDX" specVersion:"1.5"` JSON to stdout with each component carrying a `pkg:pypi/{name}@{version}` purl + bom-ref + `hashes:[{alg:"SHA-256",content:"<hex>"}]` + `externalReferences` (distribution URL + website) + `licenses`. Top-level `metadata.component` is `pkg:generic/einvite-platform@v54` (the application itself — required by CycloneDX 1.5 so consumers can distinguish the BOM owner from its dependencies). Top-level `dependencies` block has a single edge from the application to every library component. **Network-optional design**: if PyPI is unreachable (sandbox / air-gapped runner), the component is emitted without checksums but with a `properties:[{name:"einvite:network",value:"pypi-unreachable"}]` marker recording the gap; CI re-runs at release time with network access to fill in checksums. Does NOT execute `pip` — only reads the requirements file + queries PyPI over HTTPS (safe to run in CI without privileged access). Verified locally: emitted a 9-component SBOM (boto3 1.43.94, psycopg 3.3.5, redis 8.1.0, qrcode 8.2, pillow 12.3.0, argon2-cffi 25.1.0, cryptography 50.0.1, fonttools 4.65.0, brotli 1.2.0) — all 9 carry SHA-256 hashes + 4 carry SPDX license ids. JSON re-parse + structural validation (every component has type=library, purl starting with `pkg:pypi/`, bom-ref matching purl, hashes alg=SHA-256 with 64-char content) passes. (2) **`scripts/security-scan.sh`** (NEW — bash, ~165 lines, executable) — single local + CI entrypoint. Runs five scans, each gated by `command -v` so missing tools emit a `WARN` line (NOT a failure): (a) `bandit -r src/python ai_agent platform_v32 future_platform_v52 -ll` — SAST, fails on HIGH severity (bandit `-ll` reports only HIGH); (b) `pip-audit -r docs/requirements-production.txt --desc` — SCA, fails on HIGH/CRITICAL (pip-audit exit 1 = findings, exit 2 = environment error → only 1 fails the gate); (c) `python3 scripts/generate-sbom.py docs/requirements-production.txt > sbom.cdx.json` — always runs (stdlib-only, no install gate), validates the output is parseable JSON + reports the component count; (d) `gitleaks detect --no-git --source . --report-path /tmp/gitleaks-report.json` — secret scanning, fails on any finding (exit 1 = findings, other = environment error); (e) `trivy image --severity HIGH,CRITICAL --exit-code 1 einvite-scan:latest` — container scan against the image built from `deploy/Dockerfile` (only runs if Dockerfile + trivy + docker all present). Exits 0 if every available tool ran clean (or was skipped with a WARN); exits 1 if any installed tool found a HIGH/CRITICAL finding OR the SBOM generator failed. Verified locally in sandbox (bandit/pip-audit/gitleaks/trivy/docker all missing): script emits 4 WARNs for missing tools + 1 PASS for the SBOM generation + RESULT: PASS, exit 0. The `pip-audit` block includes a `--ignore-vuln GHSA-PLACEHOLDER-REMOVE-ME 2>/dev/null ||` fallback that demonstrates the waiver pattern (documented in `docs/security/CI-SECURITY.md` §7). (3) **`.github/CODEOWNERS`** (NEW) — auto-review policy. Lists `@maintainer` (placeholder — replace with actual GitHub handle / team slug before enabling branch protection; documented in CI-SECURITY.md §5) as the required reviewer on every PR touching: `src/python/`, `ai_agent/`, `platform_v32/`, `future_platform_v52/`, `src/python/security_*.py`, `src/python/secrets_*.py`, `deploy/`, `scripts/security-scan.sh`, `scripts/generate-sbom.py`, `.github/CODEOWNERS`, `.github/workflows/security.yml`. Last-matching-pattern wins (gitignore semantics). (4) **`.github/workflows/security.yml`** (NEW — GitHub Actions) — runs on push to `main` + `v54-refactor-security-ui`, on PR into `main`, on a weekly Monday 06:00 UTC cron (catches new GHSA advisories published against pinned deps after a PR landed), and on `workflow_dispatch` (manual trigger from the Actions tab). Installs every tool (bandit/pip-audit via pip; gitleaks 8.21.2 via binary download; trivy via apt) so CI never sees a WARN — runs the full suite every time. `concurrency: cancel-in-progress` avoids piling up back-to-back runs. Uploads `sbom.cdx.json` as an artifact named `sbom` (retained 90 days) + gitleaks report (retained 30 days). YAML validated with `python3 -c "import yaml; yaml.safe_load(...)"` — parses cleanly. (5) **`docs/security/CI-SECURITY.md`** (NEW — 12 sections, ~280 lines) — full policy: §1 what this CI gate covers (table of 5 scans × tool × what it checks × fail threshold); §2 how to run locally (one-liner `bash scripts/security-scan.sh` + per-tool install table for mac/win/linux); §3 CI workflow triggers (push/PR/weekly cron/workflow_dispatch); §4 SBOM format + storage + lifecycle (CycloneDX 1.5, `sbom.cdx.json` written to repo root, uploaded as artifact — NOT committed to repo to avoid churn on every dependency bump); §5 CODEOWNERS policy + the manual "replace @maintainer + enable branch protection" step; §6 SBOM generator network behaviour + 4 failure modes (PyPI unreachable / 404 / no version pin + PyPI unreachable / requirements file missing); §7 how to waive specific findings (pip-audit `--ignore-vuln`, bandit `# nosec`, gitleaks `.gitleaksignore` + active-waivers table — currently empty); §8 weekly schedule rationale (catches new advisories/rules/DB updates — P2 incident response SLA = 1 business day); §9 container scan special case (3 prerequisites: Dockerfile + trivy + docker); §10 follow-ups (transitive SBOM edges, bandit SARIF upload, gitleaks history-aware scan in CI, trivy SBOM consumption, CODEOWNERS handoff); §11 coordination with sibling subagents (sec-8 owns server.py — SAST scans it but doesn't modify; sec-9 owns docs/security cross-refs; vendor owns HTML; ux-1/2 own JS); §12 change history. (6) **Acceptance criteria met** — `scripts/security-scan.sh` runs end-to-end with WARNs for missing tools (not failures) — verified locally in sandbox (4 WARNs + 1 PASS + exit 0); `scripts/generate-sbom.py` produces a valid CycloneDX 1.5 JSON SBOM — verified (9 components, all with SHA-256 hashes, JSON parses + structural assertions pass); `.github/CODEOWNERS` exists; `.github/workflows/security.yml` exists (YAML validates); `docs/security/CI-SECURITY.md` documents everything (12 sections covering scans, waivers, SBOM format, CODEOWNERS policy, weekly schedule, follow-ups). Did NOT modify `src/python/server.py` (sec-8 owns — concurrent), `src/html/*` (vendor owns — concurrent), `src/js/*` or `src/css/*` (UI is done), `ai_agent/` / `platform_v32/` / `future_platform_v52/` (scanned by bandit but no source changes), or `docs/route-bundle-sources-v15.json` (this is a CI/scripts/docs task — no bundle rebuild needed). | `scripts/generate-sbom.py` (NEW — stdlib-only CycloneDX 1.5 SBOM generator, ~270 lines, executable), `scripts/security-scan.sh` (NEW — bash local + CI scan entrypoint, ~165 lines, executable, runs bandit/pip-audit/SBOM/gitleaks/trivy with WARN-on-missing-tool semantics), `.github/CODEOWNERS` (NEW — auto-review policy for security-critical paths, `@maintainer` placeholder to be replaced per CI-SECURITY.md §5), `.github/workflows/security.yml` (NEW — GitHub Actions workflow: push/PR/weekly-cron/workflow_dispatch triggers, installs all 5 tools, runs scan, uploads SBOM + gitleaks report artifacts), `docs/security/CI-SECURITY.md` (NEW — 12-section policy doc) |
| V54.26 | ux-10: Bilingual consistency pass (ROADMAP-V2 §3.10). Closes the §3.10 UX gap where there was no CI guard against missing `km:` translations or byte-identical EN/KH placeholder pairs slipping through. (1) **CI check** — created `scripts/check-bilingual-consistency.py` (executable, stdlib-only Python 3, 633 lines). Walks every `src/js/*.js` source file (skipping generated `bundle-*.js` files — 169 files scanned, 13 contain bilingual string tables). Recognises the two bilingual string-table conventions used across the codebase: Pattern A (`STRINGS = { key1: { en: '...', km: '...' }, ... }` — used by `album.js`, `delivery-dialog.js`, `host-polls.js`, `host-signup-sheets.js`, `invitation-edit-history.js`, `polls.js`, `signup-sheets.js`, `toast.js`) and Pattern B (`STRINGS = { en: { key1: '...' }, km: { key1: '...' } }` — used by `collaboration-presence-v52.js`, `crdt-yjs-indexeddb.js`, `crdt-yjs-rich-media.js`, `crdt-yjs-undo.js`, `guest-journey.js`). A hand-rolled brace-balanced scanner walks every `{...}` object literal in the file (skipping string contents and `//` + `/* */` comments so braces inside string values don't confuse the depth counter); for each object it looks at the identifier before `:` before `{` to decide which pattern applies, then extracts the inner `en:` / `km:` string values via a JS-string-aware regex that handles single + double quotes, escapes (`\\n`, `\\t`, `\\uXXXX`, `\\xXX`), and multi-line entries. For each `(en, km)` pair it checks (a) that `km:` exists and is non-empty (else "MISSING km:"); (b) that the two values are not byte-identical after trim (else "byte-identical pair"). CLI flags: `--strict` (recommended for CI — fails on ANY identical pair not on the documented exception list), `--allow-identical STR` (comma-separated extra exception strings for one-off overrides without editing the default list), `--report PATH` (writes the Markdown report to the given path), `--json` (machine-readable output). Default exception list: 28 proper nouns / brand names / technical acronyms (`eInvite`, `Canva`, `Stripe`, `WhatsApp`, `Telegram`, `Slack`, `Discord`, `GitHub`, `Google`, `Apple`, `Microsoft`, `Facebook`, `Instagram`, `Twitter`, `TikTok`, `YouTube`, `Zoom`, `Webex`, `RSVP`, `API`, `CRDT`, `Y.js`, `Y.Doc`, `Y.Map`, `Y.Array`, `Y.UndoManager`, `WebAuthn`, `IndexedDB`, `WebSocket`, `WebRTC`, `HTTP`, `HTTPS`, `JSON`, `DOM`, `URL`, `ID`, `CSS`, `HTML`, `JS`, `MpCmdRun.exe`, `clamdscan`, `clamd`, `clamav`) + 8 regex patterns (emails, URLs, numbers/dates/currencies, `0 = ...` style hints, punctuation/symbols-only, leading `+`, ALL-CAPS 2-5 letter acronyms, single-digit labels). Exit code 0 if all good, 1 if any missing or non-exception identical pairs found. (2) **Report** — created `docs/i18n/BILINGUAL-CONSISTENCY-REPORT.md` (143 lines): summary table (239 `en:` strings found, 239 `km:` strings found, 239 bilingual pairs, 0 missing `km:`, 0 byte-identical pairs); missing-`km:` table (empty — none); byte-identical table (empty — none); exception list (proper nouns + ALL-CAPS acronyms + lowercase technical terms + 8 regex patterns with their raw patterns); pattern-detection explanation (both conventions); common-UI-term translation reference table (28 entries — Save/Cancel/Delete/Edit/Create/Close/Open/Loading/Error/Success/Retry/Search/Filter/All/None/Yes/No/Submit/Send/Copy/Copied/Settings/Help/Back/Next/Previous/Title/Email/Name); how-to-add-a-new-legitimate-exception guide (3 rules + `--allow-identical` example); per-file breakdown (13 files with pairs — total 239 pairs across them); CI integration YAML (the `.github/workflows/bilingual-check.yml` snippet that runs `python3 scripts/check-bilingual-consistency.py --strict`). (3) **Findings** — the existing STRINGS tables are already 100% bilingual and 100% non-identical: `host-polls.js` (69 pairs), `host-signup-sheets.js` (59), `album.js` (24), `signup-sheets.js` (17), `invitation-edit-history.js` (15), `delivery-dialog.js` (14), `polls.js` (13), `collaboration-presence-v52.js` (7), `guest-journey.js` (7), `crdt-yjs-undo.js` (5), `crdt-yjs-indexeddb.js` (4), `crdt-yjs-rich-media.js` (4), `toast.js` (1). No missing `km:` translations found — every `en:` string has a non-empty `km:` partner. No byte-identical placeholder pairs found — every English string is paired with a distinct Khmer translation (no `en: 'Save', km: 'Save'` style slip-throughs). The CI check exists to KEEP it this way — any future PR that adds an `en:` string without a `km:` partner, or with `km:` set to the same English text, will fail `--strict` and block the merge. (4) **Verification** — `python3 scripts/check-bilingual-consistency.py --strict` → exit 0 (PASS); `python3 scripts/check-bilingual-consistency.py --strict --report docs/i18n/BILINGUAL-CONSISTENCY-REPORT.md` → exit 0 (PASS) + report written; negative-test by inserting a deliberately-broken `.ux10-test-fixture.js` with 1 missing `km:`, 1 empty `km:`, 1 placeholder identical pair (`en: 'Loading', km: 'Loading'`), 1 legitimate identical pair (`en: 'eInvite', km: 'eInvite'`), 1 legitimate acronym pair (`en: 'CRDT', km: 'CRDT'`) → script correctly reported `Missing km: 2` and `Identical pairs: 3 (2 legitimate, 1 PLACEHOLDER)` and exited 1; negative-test file removed before the final run. (5) **Acceptance criteria met** — CI check script exists, is executable, runs in <1s on 169 files; report exists with summary + 2 tables + exception list + per-file breakdown + CI YAML; all missing `km:` strings either fixed or flagged as TODO (none missing); all identical pairs either legitimate (documented exception) or fixed (none found); "no English text visible when the UI is in Khmer mode" verified by the check (every `en:` has a distinct `km:` partner). Did NOT modify `src/css/*` (ux-7/8/9 own — concurrent), `src/html/*` (ux-8 owns — concurrent), backend Python, or existing JS module logic (only added a missing `km:` translation would have been allowed; none were missing so no JS files were modified). `docs/route-bundle-sources-v15.json` unchanged (CI/grep task, no bundle rebuild needed). | `scripts/check-bilingual-consistency.py` (NEW — CI check, stdlib-only Python 3, executable, 633 lines, recognises Pattern A + Pattern B bilingual string tables, exits 0/1, supports `--strict`/`--allow-identical`/`--report`/`--json`), `docs/i18n/BILINGUAL-CONSISTENCY-REPORT.md` (NEW — 143 lines: summary + 2 tables + exception list + pattern detection + common-UI translation reference + CI YAML) |
| V54.25 | ux-8: Mobile responsiveness pass (ROADMAP-V2 §3.8). Closes the §3.8 accessibility gap noted by the WCAG audit: "reflow issues on narrow viewports". Audited all 16 HTML pages at 375px width (iPhone SE viewport) via static CSS analysis. Filled gaps with two universal responsive layers + per-page overrides + a desktop-optimised informational banner on the editor. (1) **Viewport meta tags** — verified and updated every `src/html/*.html` (16 files) to `<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`. 14 of 16 previously lacked `viewport-fit=cover` (only `checkin.html` had it pre-existing); `billing.html` was missed by the first sed pass and fixed in a follow-up. The `viewport-fit=cover` value enables iOS safe-area insets (`env(safe-area-inset-left)` etc.) already used by `checkin-v13.css`. (2) **`src/css/modern-ui.css`** — appended a universal V2-UX-8 responsive block (lines 178-456) applying to every page that loads `modern-ui.css` (14 of 16 — exceptions are `public.html` and `checkin.html` which use their own guest/checkin CSS). Adds: (a) `@media(max-width:540px){html,body{font-size:16px}}` floor — prevents iOS Safari from zooming inputs on focus when input font-size < 16px; (b) header wrap rule (`flex-wrap:wrap; height:auto; min-height:60px; padding:10px 14px; overflow-x:auto`) so nav buttons wrap to a second row at 375px instead of overflowing; (c) touch targets ≥ 44×44px on `a.button-link`, `header button`, `.dialog-actions button`, `.response-tabs button`, `.template-tabs button`, `.admin-tabs button`, `.empty-state__action button`, `.error-state__retry button`, `.v13-security-actions button`, inputs/textarea/select (WCAG 2.5.5); (d) dialog full-screen treatment at ≤ 540px — `dialog, [aria-modal="true"], .create-dialog, .template-preview-dialog, .edit-material-dialog, .studio-dialog, .ei-dialog` get `width:100vw; max-width:100vw; height:100dvh; max-height:100dvh; border-radius:0; border:0; margin:0; padding:16px; overflow:auto`; (e) two-column dialog layouts collapse to single column (`.template-preview-shell`, `.template-preview-phone`, `.template-preview-grid`); (f) head rows wrap (`.dash-head`, `.library-head`, `.billing-head`, `.response-top`, `.studio-head`, `.analytics-head`); (g) multi-column grids collapse to `1fr` or `1fr 1fr` (`.upload-row`, `.add-form`, `.library-toolbar`, `.checkin-tools`, `.checkin-hero`, `.checkin-person`, `.usage-grid`, `.plans`, `.response-metrics`, `.designer-grid`, `.workspace-metrics`, `.account-grid`, `.admin-metrics`, `.admin-tabs`, `.invite-grid`, `.payment-assurance`, `.v13-security-grid`, `.v13-security-actions`); (h) `.mobile-desktop-hint` visibility — `display:none` by default, `display:flex` at `@media(max-width:720px)` (reveals the bilingual editor banner — see fix 3 below); (i) editor header pruning — at ≤ 540px, hides `body[data-page="index"]>header #backupBtn, #restoreBtn, #undoBtn, #redoBtn` (still available via Ctrl+K command palette); truncates `#saveState` + `#serverState` to `font-size:11px; max-width:120px; text-overflow:ellipsis`; (j) tables scroll horizontally inside their containers (`.v13-list table, .v13-audit table, .admin-panel table, .analytics-card table` get `min-width:540px` inside an `overflow-x:auto` parent); (k) `@media(max-width:380px)` very-narrow tightening (iPhone SE / older Androids) — font-size and padding reductions. (3) **`src/css/organized/styles.css`** — appended a universal V2-UX-8 layer (lines 19-128) applying to EVERY page (including `public.html` + `checkin.html` which do NOT load `modern-ui.css`). Adds: same `font-size:16px` floor; same header wrap; same `min-height:44px` touch targets on `button, a.button-link, input[type=submit/button/reset], .button-link`; same `min-height:44px + font-size:16px` on inputs; same `dialog, [aria-modal="true"]` full-screen treatment (catches `<dialog id="qrModal">` on guests.html, `<dialog id="scannerDialog">` on checkin.html, `<dialog id="studioDialog">` on templates.html, `<dialog id="editDialog">` on materials.html, `<dialog id="modal">` on index.html, `<dialog id="createDialog">` + `<dialog id="templatePreviewDialog">` on dashboard.html); same `min-width:540px` table horizontal scroll; full-screen `.scanner-shell, .qr` shells. Adds an editor safety-net: `body[data-page="index"]:not(.studio-experience)>main{display:block; grid-template-columns:none; height:auto; overflow-x:hidden}` — if `studio-experience.js` fails to add the body class, the universal 3-column grid (declared unconditionally in `organized/styles.css` line 1) is overridden so the page doesn't horizontally scroll. Once JS runs and adds `studio-experience`, the `editor-responsive-contract-v27.css` rules take over (mobile/compact/desktop layouts). (4) **`src/html/index.html`** — added a bilingual `<aside class="mobile-desktop-hint" role="note" aria-label="Desktop recommended">` element as the first child of `<body>` (lines 61-77). Contains: EN "The editor works best on a desktop screen. Some tools may be unavailable on mobile." / KH "កម្មវិធីនិពន្ធដំណើរការល្អបំផុតនៅលើផ្ទៃតាំង។ ឧបករណ៍មួយចំនួនអាចមិនមាននៅលើទូរស័ព្ទ។". The banner is hidden by default (CSS in `modern-ui.css` line 184: `.mobile-desktop-hint{display:none}`) and revealed at `@media(max-width:720px)` via `display:flex` (slightly wider than the 540px main breakpoint so small tablets in portrait see the hint before the editor loads). The banner is purely informational — no script, no blocking — the editor remains usable on mobile via the existing `editor-responsive-contract-v27.js` layout-contract drawers (`einvite-layout-mobile` mode at ≤ 820px). Per ROADMAP §3.8: "Ensure the editor toolbar is usable on mobile (or explicitly document the editor as desktop-only with a graceful message)." — the editor's dense toolbar + multi-panel layout is genuinely desktop-first; the contract mode collapses side panels into drawers but the toolbar remains cramped at 375px; the informational banner satisfies the "explicitly document" branch of the acceptance criterion. (5) **Audit doc** — created `docs/a11y/RESPONSIVE-AUDIT.md`: §1 methodology (static CSS analysis, no browser automation in sandbox); §2 per-page × viewport matrix (375px / 768px / 1024px / 1440px) — all 16 pages PASS at 375px, with the editor marked PASS* (desktop-optimised with graceful banner); §3 specific fixes applied (the modern-ui.css block, the organized/styles.css block, the index.html banner, the viewport-meta updates); §4 pages documented as desktop-only (just `index.html`); §5 acceptance criteria table (every criterion PASS with evidence); §6 honest caveats (static-audit only, `overflow-x:auto` on header is a fallback, editor at 375px is technically usable but cramped, iOS Safari input zoom prevented by 16px font floor, `100dvh` used for dialogs with `vh` fallback for pre-2022 browsers, dark-mode interaction with ux-9's overrides, `public.html` guest-page mobile breakpoints preserved, `checkin.html` scanner dialog inner shell preserved, build verification); §7 coordination with concurrent subagents (ux-7 toast, ux-9 dark mode, ux-10 bilingual, ux-1/ux-2 host panels, ux-3 edit-history); §8 change history. (6) **No files outside the modification scope were touched** — `src/css/toast.css` (ux-7 owns), `src/css/tokens.css` (ux-9 owns), `src/js/*.js` (ux-7/ux-10/others own), and all page-specific CSS files (`dashboard-page-v13.css`, `account-page-v13.css`, etc. — they already had their own `@media` rules; V2-UX-8's universal layer is additive). (7) **Build verification** — `python3 src/python/build_route_bundles.py` → "WROTE 16 route bundles"; `python3 src/python/sync_frontend_assets.py` → "SYNC_FRONTEND_ASSETS_DONE copied=16 skipped=309" (the 14 modified HTML files + `modern-ui.css` + `organized/styles.css` + 14 rebuilt bundle CSS files synced to `src/python/`); `python3 src/python/build_route_bundles.py --check` → `ROUTE_BUNDLE_CHECK_PASSED` (exit 0). Verified the synced `src/python/modern-ui.css` contains `V2-UX-8` (1 match), `src/python/styles.css` contains `V2-UX-8` (1 match), `src/python/index.html` contains `mobile-desktop-hint` (1 match), `src/python/bundle-dashboard-v15.css` has 29 `@media` rules (was 27 before; +2 from V2-UX-8), `src/python/bundle-index-v15.css` has 47 `@media` rules, `src/python/bundle-public-v15.css` has 14 `@media` rules (universal organized/styles.css layer applied since public.html doesn't load modern-ui.css), `src/python/bundle-account-v15.css` has 28 `@media` rules. Acceptance criteria met: all 16 pages render without horizontal scroll at 375px (per-page matrix in §2); dialogs are usable at 375px (full-screen or scrollable — universal rule applies to every `<dialog>` + `[aria-modal="true"]` element); editor toolbar usable on mobile OR documented as desktop-only with a graceful message (bilingual banner at ≤ 720px + dense toolbar buttons hidden at ≤ 540px with Ctrl+K command-palette fallback); touch targets ≥ 44px on all interactive elements (universal `min-height:44px` rule on `button, a.button-link, input[type=submit/button/reset], .button-link` + specific overrides for tab/action buttons + inputs/textarea/select); bundle rebuild passes `--check` (ROUTE_BUNDLE_CHECK_PASSED exit 0). | `src/css/modern-ui.css` (appended V2-UX-8 responsive block lines 178-456 — universal mobile overrides for the 14 of 16 pages that load modern-ui.css: font-size floor, header wrap, touch targets ≥ 44px, dialog full-screen, head-row wrap, multi-col grid collapse, editor header pruning, table horizontal scroll, very-narrow tightening, `.mobile-desktop-hint` visibility at ≤ 720px), `src/css/organized/styles.css` (appended V2-UX-8 universal layer lines 19-128 — same overrides for EVERY page including public.html + checkin.html which do NOT load modern-ui.css; editor safety-net `body[data-page="index"]:not(.studio-experience)>main{display:block}`), `src/html/index.html` (added bilingual `<aside class="mobile-desktop-hint" role="note">` banner lines 61-77 — hidden by default, visible at ≤ 720px), `src/html/*.html` × 16 (viewport meta tag updated to include `viewport-fit=cover`), `docs/a11y/RESPONSIVE-AUDIT.md` (NEW — per-page × viewport matrix, specific fixes, desktop-only documentation, acceptance criteria, honest caveats, coordination notes), `src/css/bundle-*-v15.css` × 16 + `src/python/bundle-*-v15.css` × 16 (rebuilt to include the new responsive layers), `src/python/modern-ui.css` + `src/python/styles.css` + `src/python/*.html` × 16 (synced from src/) |
| V54.24 | ux-9: Dark mode audit (ROADMAP-V2 §3.9). Closes the §3.9 accessibility gap where the Phase 2b audit (`docs/a11y/WCAG-AA-AUDIT.md` §§1-10) only verified contrast ratios in light mode — the dark-mode token block (`html[data-theme="dark"]` in `tokens.css:4` + `modern-ui.css:4`) was assumed to pass but had never been measured against the WCAG formula. (1) **Audit** — wrote a Python script (`/home/z/my-project/einvite-platform/.ux9-tmp/contrast_audit.py`) implementing the WCAG 2.1 contrast formula `L = 0.2126·R + 0.7152·G + 0.0722·B` (linearised sRGB); computed ratios for every text/border/accent/shadow token against every dark surface it can plausibly appear on (`--surface-0`/`--app-bg` `#111218`; `--surface-1`/`--app-surface` `#191a21`; `--surface-2`/`--app-surface-2` `#202129`; `--surface-3` `#292b34` / `--app-surface-3` `#282a33`). Found 3 dark-mode failures: (a) `--text-3` `#858895` was 4.92:1 on `#191a21` (PASS) but 4.00:1 on `#292b34` surface-3 (FAIL); (b) `--app-faint` `#777a87` was 4.06:1 on `#191a21` (FAIL) and 3.35:1 on `#282a33` surface-3 (FAIL) — the Phase 2b comment in `modern-ui.css:3` documented this as "stays (4.06:1 on #191a21)" but never checked surface-3; (c) white-on-`--app-accent` button text was 2.80:1 in dark mode (FAIL AA-large) — `.response-tabs button.active`, `.template-tabs button.active`, `.button-link.primary`, `.empty-state__action button` all use `background:var(--app-accent)` (`#d5829c` in dark mode, a light pink chosen for text/icon visibility on the dark surface) + `color:#fff` (hardcoded). Light mode passes (white-on-`#8b465b` is 6.72:1) but dark mode fails because `--app-accent` was lightened for text contrast — creating a conflict between text-accent-use and button-background-use. (2) **Fixes** — bumped dark-mode `--text-3` from `#858895` to `#90939f` in `tokens.css:7` (now 5.66:1 on `#191a21`, 4.61:1 on `#292b34` — PASS on every dark surface). Bumped dark-mode `--app-faint` from `#777a87` to `#9396a3` in `modern-ui.css:8` (now 5.89:1 on `#191a21`, 4.79:1 on `#292b34`, 4.85:1 on `#282a33` — PASS). Added a dark-mode-only override block in `modern-ui.css:172-176` that changes the primary button background to `color-mix(in srgb, var(--app-accent) 65%, #1f0a11)` = `#95586b` (white-on-`#95586b` = 5.40:1 PASS, button-vs-`#191a21` = 3.21:1 PASS for 1.4.11 non-text contrast). The `--app-accent` text token itself is unchanged so accent text/links/icons keep their 6.19:1 contrast on dark surfaces. Added a dark-mode-only override for the header shadow at `modern-ui.css:177`: the light-mode shadow `0 8px 28px rgba(0,0,0,.035)` was invisible against the `#111218` dark background — bumped to `rgba(0,0,0,.32)` (9.1× stronger) and reduced the white-inset highlight from 28% to 6% so the top-edge highlight doesn't dominate the dark header. Light-mode values are UNCHANGED per the §3.9 task scope ("Don't change the light-mode value — Phase 2b already audited it"). (3) **Verification** — re-ran the contrast script after the fixes: every dark-mode text token now passes AA-normal on every dark surface; every border token is acceptable per WCAG 1.4.11 note (decorative borders paired with a luminance-shifted fill + label, not sole indicators); every icon is `currentColor`-inherited or hardcoded-AA-passing (`#db5963` on dark = 4.64:1, on light = 3.74:1 — both pass AA-large); every shadow token has a dark-mode alpha 3-4× higher than light-mode (visible against `#191a21`); Khmer text inherits the same color tokens as Latin (the `:lang(km)` rule from ux-5/V54.19 does NOT override color — only line-height/font-size/letter-spacing/font-feature-settings — so Khmer passes AA-normal whenever Latin does, which is now the case). (4) **Audit doc** — appended §11 "Dark Mode Audit" to `docs/a11y/WCAG-AA-AUDIT.md` (lines 552-701): §11.1 methodology; §11.2 contrast-ratio table for text tokens × every dark surface; §11.3 border/non-text 1.4.11 table; §11.4 white-on-accent button-text table with before/after ratios; §11.5 shadow visibility table; §11.6 icon visibility audit; §11.7 Khmer-text-in-dark-mode table (verified-by-inheritance); §11.8 tokens-adjusted summary table (4 rows: `--text-3`, `--app-faint`, button-bg override, header shadow override); §11.9 acceptance-criteria table; §11.10 honest caveats (token-level static audit not rendered-pixel audit; `--app-muted` light-mode `#757783` measured 4.45:1 — below AA-normal — was missed by Phase 2b's claim of 5.63:1; flagged as follow-up §11.10 caveat 1 but not fixed per task scope; `--app-warn` light-mode `#d58b20` is 2.78:1 — pre-existing §7-P2-H follow-up; real-browser screenshots deferred to orchestrator). Added V54.24 row to §10 change history. (5) **Build verification** — `python3 src/python/build_route_bundles.py` → "WROTE 16 route bundles" (the new dark-mode values + the override block propagated to every bundle that includes `tokens.css` + `modern-ui.css` — 15 of 16 page bundles have the new `#90939f`/`#9396a3`/`#95586b` colors, only `bundle-public-v15.css` has 0 because the public/guest bundle uses `guest-features-v54_1.css` + `advanced-public-renderer-v32.css` instead of `modern-ui.css`); `python3 src/python/sync_frontend_assets.py` → "SYNC_FRONTEND_ASSETS_DONE copied=16 skipped=309" (tokens.css + modern-ui.css + 14 rebuilt bundle CSS files synced to `src/python/`); `python3 src/python/build_route_bundles.py --check` → `ROUTE_BUNDLE_CHECK_PASSED` (exit 0). Verified the synced `src/python/tokens.css` carries `--text-3:#90939f` in the dark-mode block; verified `src/python/modern-ui.css` carries `--app-faint:#9396a3` + the new `color-mix(in srgb,var(--app-accent) 65%,#1f0a11)` button-bg override + the `rgba(0,0,0,.32)` header shadow override. Acceptance criteria met: every dark-mode text token passes AA-normal on every dark surface (§11.2); every icon/border/shadow is visible in dark mode (§11.3-§11.6); Khmer text renders correctly in dark mode (§11.7 — verified-by-inheritance from `:lang(km)` rule which doesn't override color); audit doc §11 documents the results with a complete table of all text tokens × both modes × contrast ratio × pass/fail. | `src/css/tokens.css` (dark-mode `--text-3` `#858895` → `#90939f` at line 7; updated comment block at lines 2-6), `src/css/modern-ui.css` (dark-mode `--app-faint` `#777a87` → `#9396a3` at line 8; updated comment block at lines 2-7; new dark-mode-only override block at lines 157-177 covering primary-button backgrounds + header shadow), `docs/a11y/WCAG-AA-AUDIT.md` (appended §11 "Dark Mode Audit" — lines 552-701, 10 sub-sections; added V54.24 row to §10 change history), `src/css/bundle-*-v15.css` × 15 (rebuilt to include the new dark-mode tokens + override block), `src/python/tokens.css` + `src/python/modern-ui.css` + `src/python/bundle-*-v15.css` × 15 (synced from src/) |
| V54.23 | ux-7: Toast notification system (ROADMAP-V2 §3.7). Closes the §3.7 UX gap where success/error feedback on the host panels was inconsistent — `host-signup-sheets.js` used `alert()` on delete failure, `host-polls.js` used `alert()` on both delete failure + close-now failure, and NO mutation showed a success toast (the save path silently closed the modal + refreshed the list, leaving the host to infer success). Adds a new dependency-free toast stack (`window.EInviteToast.show({type, message_en, message_km, duration})`) that mounts a top-right `#toast-stack` container, slides in toasts from the right with a fade, auto-dismisses after 4s for success/info/warning and 6s for errors, supports a manual close button, enforces max 5 visible (oldest is dismissed when a 6th would arrive), and pauses the auto-dismiss timer on hover/focus. Types: `success` (green ✓), `error` (red ⚠), `info` (blue ℹ), `warning` (amber ⚠) — border-left color is the primary signal; the icon is decorative (`aria-hidden="true"`). Accessibility: each toast is `role="status"` + the stack carries `aria-live="polite"` + `aria-atomic="false"`; the close button is a real `<button>` with a bilingual `aria-label` ("Close" / "បិទ"); `prefers-reduced-motion` disables the slide-in; `@media (max-width: 540px)` collapses the stack to a full-width banner pinned to top:0 with no border-radius. Bilingual message selection: picks EN or KH by `window.EInviteLang` then `document.documentElement.lang` (matches ux-5's P1-D dynamic `<html lang>` work on public.html). (1) **New file `src/js/toast.js`** — IIFE exposing `window.EInviteToast.show({...})`. Idempotent (no-ops if already defined). Auto-creates `#toast-stack` if missing. Stores the dismiss timer on the element so manual close + hover-pause can cancel it. `dismiss()` animates out via the `.toast--leave` class + listens for `transitionend` (with a 280ms fallback) before removing from the DOM. (2) **New file `src/css/toast.css`** — separate from `modern-ui.css` (ux-6 owns it) and `organized/styles.css` (ux-5 owns it). Uses existing tokens (`--app-surface`, `--app-text`, `--app-muted`, `--app-line`, `--app-shadow`, `--app-radius`, `--danger`, `--app-good`, `--app-warn`) with literal fallbacks. The `--info` token does NOT exist in `tokens.css` yet (ux-9 owns it concurrently); `.toast--info` uses `var(--info, var(--app-accent-2, #3b82f6))` so if ux-9 adds `--info` later, the toast picks it up automatically. The stack uses `z-index: 9999` (below the existing `.ui-theme-menu` at 10000 to avoid covering the theme switcher). `pointer-events: none` on the stack, `pointer-events: auto` on each toast, so the gaps between toasts don't block clicks. Dark-mode rule bumps the box-shadow so the toast reads against the dark backdrop. (3) **`src/html/dashboard.html`** — added `<link rel="stylesheet" href="toast.css">` in `<head>` (after the bundle link; toast.css is synced as a standalone file to `src/python/toast.css` by `sync_frontend_assets.py` since the task scope forbids editing `modern-ui.css` to add toast styles there). Added `<div id="toast-stack" class="toast-stack" aria-live="polite" aria-atomic="false"></div>` immediately before `</body>` — the empty container is invisible until JS populates it. (4) **`docs/route-bundle-sources-v15.json`** — added `"toast.js"` at the END of the `dashboard.html.scripts` array (after `"host-polls.js"`). Toast calls happen inside event handlers (not at module top-level), so load order doesn't matter for runtime. `editor.html` does NOT exist in the manifest (no `editor.html` in `src/html/` — confirmed via `ls`) so the "if it exists" clause is skipped. (5) **`src/js/host-signup-sheets.js`** — replaced the single `alert(err.message || STRINGS.errDelete.en)` in `onDelete()`'s catch block with `EInviteToast.show({type:'error', message_en: err.message || STRINGS.errDelete.en, message_km: err.message || STRINGS.errDelete.km, duration: 6000})` (the `err.message` is server-side text shown in both languages since it's already single-language; the STRINGS fallback is bilingual). Added a `success` toast after `closeOverlay() + await refresh()` in the form-submit success path (create/edit mutation): `EInviteToast.show({type:'success', message_en: STRINGS.saved.en, message_km: STRINGS.saved.km, duration: 4000})` — `STRINGS.saved` already existed. Added an `error` toast in the form-submit catch block so the host gets feedback even after closing the modal (the inline `statusEl.innerHTML` is only visible while the modal is open). Added a `success` toast after the delete mutation's `await refresh()`. (6) **`src/js/host-polls.js`** — same pattern: replaced `alert()` in `onDelete()` and `onCloseNow()` catch blocks with error toasts. Added `success` toasts for the create/edit mutation (`STRINGS.saved`), the delete mutation (`STRINGS.deleted`), and the close-now mutation (`STRINGS.closed` — a NEW STRINGS key added since host-polls didn't have a "Poll closed" success message). Added `saved`/`deleted`/`closed` to the STRINGS table (bilingual EN+KH) since host-signup-sheets.js already had `saved` + `deleted` but host-polls.js was missing all three. All calls guarded with `if (window.EInviteToast)` so the panel still works if toast.js fails to load. (7) **Build verification**: `node -e "new Function(...)"` reports `SYNTAX_OK` for `toast.js` + the edited `host-signup-sheets.js` + `host-polls.js`; `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles (the dashboard bundle now includes `toast.js` at the end); `python3 src/python/sync_frontend_assets.py` synced `toast.js` + `toast.css` + `host-signup-sheets.js` + `host-polls.js` + `dashboard.html` + rebuilt `bundle-dashboard-v15.js` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Verified the synced `src/python/bundle-dashboard-v15.js` contains 23 occurrences of `EInviteToast` (toast.js's definition + 6 call sites in host-signup-sheets.js + 6 in host-polls.js + the close-button label helper). Verified `src/python/dashboard.html` contains both the `<link rel="stylesheet" href="toast.css">` and the `<div id="toast-stack" class="toast-stack" aria-live="polite" aria-atomic="false"></div>` container. Verified `src/python/toast.css` exists (5.3 KB) and contains `.toast-stack`, `.toast--success`, `.toast--error`, `.toast--info`, `.toast--warning`, `.toast__icon`, `.toast__message`, `.toast__close`, `prefers-reduced-motion`, `@media (max-width: 540px)`. Verified zero `alert(` calls remain in `host-signup-sheets.js` or `host-polls.js` (grep returns only comment mentions of "alert()"). Acceptance: every create/edit/delete/close mutation in both host panels shows a toast; `confirm()` calls for delete + close-now are PRESERVED (blocking is correct for irreversible confirmations); toast auto-dismisses after 4s (6s for errors); close button is keyboard-focusable; `role="status"` for screen readers; bilingual strings per call site; bundle rebuild passes `--check`. | `src/js/toast.js` (NEW — IIFE exposing `window.EInviteToast.show({type, message_en, message_km, duration})`; auto-creates `#toast-stack`; max 5 visible; hover/focus pauses auto-dismiss; `prefers-reduced-motion` respected via the CSS), `src/css/toast.css` (NEW — `.toast-stack` + `.toast` + 4 type variants + `.toast__icon` + `.toast__message` + `.toast__close` + slide-in/leave transitions + `prefers-reduced-motion` opt-out + mobile full-width banner + dark-mode shadow bump), `src/html/dashboard.html` (added `<link rel="stylesheet" href="toast.css">` in `<head>` + `<div id="toast-stack" class="toast-stack" aria-live="polite" aria-atomic="false"></div>` before `</body>`), `src/js/host-signup-sheets.js` (replaced 1 `alert()` in `onDelete()` catch with an error toast; added success toast in form-submit success + delete success paths; added error toast in form-submit catch), `src/js/host-polls.js` (replaced 2 `alert()` calls in `onDelete()` + `onCloseNow()` catches with error toasts; added success toasts in form-submit success + delete success + close-now success paths; added 3 new STRINGS keys `saved`/`deleted`/`closed` — bilingual), `docs/route-bundle-sources-v15.json` (added `"toast.js"` to the `dashboard.html.scripts` array after `"host-polls.js"`), `src/js/bundle-dashboard-v15.js` + `src/python/bundle-dashboard-v15.js` + `src/python/dashboard.html` + `src/python/toast.js` + `src/python/toast.css` + `src/python/host-signup-sheets.js` + `src/python/host-polls.js` (rebuilt/synced) |
| V54.22 | ux-3: Wire edit-history view into dashboard (ROADMAP-V2 §3.3). Closes the §3.3 UX gap where `src/js/invitation-edit-history.js` (Phase 2a-2 / V54.4) shipped `window.EInviteEditHistory.mountHistoryView(root,{invitationId})` + `mountBadge(root,{invitationId,editedAfterSendAt,sentAt,mode})` but no dashboard HTML called them — the backend `GET /api/invitations/{id}/edit-history` (returns `{history,sentAt,editedAfterSendAt,documentVersion,updatedAt}`) and `POST /api/invitations/{id}/resend-notification` endpoints were implemented and tested but invisible to hosts. This task is purely additive HTML + a small inline mount script; no JS module changes (the `invitation-edit-history.js` source is read-only per the task scope — already complete since Phase 2a-2) and no CSS changes (ux-5 owns `styles.css` and ux-6 owns `modern-ui.css` concurrently). (1) **HTML** — added `<section id="edit-history-panel" class="invitation-section event-section" data-event-section="edit-history" hidden>` to `src/html/dashboard.html`, placed AFTER the dashboardView + create/template-preview dialogs and BEFORE the `<section id="signup-sheets-panel">` (ux-1, §3.1) + `<section id="polls-panel">` (ux-2, §3.2) so the sibling ordering inside `<main>` is edit-history → signup-sheets → polls per the task spec. The section contains: a bilingual `<h2>` (EN "Edit history" / KH "ប្រវត្តិកែសម្រួល" — `i18n-en` + `i18n-km khmer-text` spans), a `<p id="edit-history-meta" class="muted" role="status" aria-live="polite">` for the sent-at + edited-after-send timestamp line, a `<div id="edited-after-send-badge" aria-live="polite">` mount point for `mountBadge` (renders the "Edited after sending · <timestamp>" badge above the diff list when the host has actually edited after sending), and a `<div id="edit-history-mount">` mount point for `mountHistoryView` (the JS view renders its own `<section class="invitation-edit-history guest-feature">` inside the mount with its own h2 + intro + resend-notification button + status p + diff list — wired to the existing `POST /api/invitations/{id}/resend-notification` endpoint via the JS view's own click handler, no separate wiring needed). (2) **Inline mount script** — added a third `<script>` block (after ux-1's signup-sheets + ux-2's polls inline scripts) that runs on DOMContentLoaded and: (a) resolves the active invitation id via the same chain ux-1/ux-2 use (`window.EINVITE_INVITATION_ID` → URL `?invitation=` → `EInviteContext.getInvitationId()` → `localStorage.sovan-active-invite`), (b) detects language mode via `window.EInviteLang` → `document.documentElement.lang` → default `'both'` (same pattern as `host-signup-sheets.js`), (c) un-hides the panel + calls `EInviteEditHistory.mountHistoryView(mountEl, {invitationId, mode})` (the view fetches `/api/invitations/{id}/edit-history` itself and renders the diff list + the resend button), (d) in parallel fetches the same endpoint once to extract `sentAt` + `editedAfterSendAt` for the badge + meta line (the JS view's internal fetch is for the diff list only; this separate fetch is for the badge + meta — a deliberate duplicate round-trip kept small since the endpoint is cheap and host-only), (e) renders the meta line bilingually ("Sent <ts> · Edited after sending <ts>" / KH "បានផ្ញើ <ts> · បានកែប្រែបន្ទាប់ពីផ្ញើ <ts>") in `#edit-history-meta`, (f) calls `EInviteEditHistory.mountBadge(badgeEl, {invitationId, editedAfterSendAt, sentAt, mode})` — the badge returns early when `editedAfterSendAt` is null so no badge renders for invitations that haven't been edited after sending. The script guards against `EInviteEditHistory` not being loaded (logs `console.warn` and returns), against mount throws (logs `console.warn`), against fetch failures (silent `.catch`), and shows a bilingual "Select an invitation first…" placeholder when no invitation id is resolvable (matching the ux-1/ux-2 empty-state pattern). (3) **Bundle manifest** — verified `invitation-edit-history.js` is already in the `dashboard.html` scripts array in `docs/route-bundle-sources-v15.json` (entry 11 of 19, between `delivery-dialog.js` and `host-signup-sheets.js`) per the Phase 2a-2 worklog; left the JSON unchanged. (4) **Build verification**: `node -e "new Function(...)"` reports `SYNTAX_OK` for the extracted inline script and for the rebuilt `src/js/bundle-dashboard-v15.js`; `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles (the dashboard bundle now contains the new dashboard.html section + the inline mount script + the unchanged invitation-edit-history.js); `python3 src/python/sync_frontend_assets.py` synced `dashboard.html` + rebuilt `bundle-dashboard-v15.js/css` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Verified `src/python/dashboard.html` is identical to `src/html/dashboard.html` (zero diff) and contains the new `edit-history-panel` / `edited-after-send-badge` / `edit-history-mount` / `edit-history-meta` IDs. Regression: `PYTHONPATH=src/python:. python3 tests/p2a_post_send_edit_test.py` → `P2A_POST_SEND_EDITING_TEST_PASSED` (the Phase 2a-2 backend test for the `/edit-history` + `/resend-notification` endpoints still passes — this task only adds frontend HTML + script, no backend changes). Acceptance: host sees the diff history when viewing an invitation (rendered by the JS view inside `#edit-history-mount`); "Resend update notification" button fires the email via the JS view's own click handler wired to `POST /api/invitations/{id}/resend-notification` (verified by the still-passing Phase 2a-2 test); `editedAfterSendAt` timestamp shown at the top of the panel when set (both in the `#edit-history-meta` line and in the `mountBadge` "Edited after sending · <ts>" badge); no console errors when the panel loads (guards against missing API, mount throws, fetch failures); bundle rebuild passes `--check`; all visible strings bilingual EN+KH (the JS view's STRINGS dict covers the history view + badge + resend button + status messages; the wrapper section's h2 + meta line use bilingual i18n spans matching ux-1/ux-2's pattern). | `src/html/dashboard.html` (added `<section id="edit-history-panel">` after the create/template-preview dialogs and before the signup-sheets-panel — contains bilingual h2 + #edit-history-meta + #edited-after-send-badge + #edit-history-mount; added a third inline `<script>` block that mounts EInviteEditHistory + fetches sentAt/editedAfterSendAt for the badge + meta line; no other HTML changes), `src/js/bundle-dashboard-v15.js` + `src/css/bundle-dashboard-v15.css` (rebuilt — no source JS/CSS changes), `src/python/dashboard.html` + `bundle-dashboard-v15.js` + `bundle-dashboard-v15.css` (synced from src/), `docs/route-bundle-sources-v15.json` (verified unchanged — `invitation-edit-history.js` was already present at entry 11 per the Phase 2a-2 worklog) |
| V54.21 | ux-4: Real upload progress bar for album photos (§3.4). Closes the §3.4 UX gap where `src/js/album.js`'s upload path disabled the submit button and gave no feedback for large uploads — a 5 MB photo on a slow connection appeared stuck for several seconds with no progress and no way to cancel. The MIME allow-list (`accept="image/jpeg,image/png,image/webp,image/gif"` on the file input), the FormData fields (`file` + `caption`), the endpoint (`POST /api/invitations/{id}/album`), the success → reset + reload-grid contract, and the server-side malware scan (HTTP 422 + `code: "malware_detected"`) are ALL preserved unchanged — this task only touches the client-side transport + UI. (1) **`src/js/album.js` rewrite of `onUpload()`** — the previous `fetch(fd)` call is replaced with `XMLHttpRequest`. `xhr.open('POST', ...)` + `xhr.setRequestHeader('X-Invitation-Guest'/'X-Invitation-Access', ...)` mirrors the prior `fetch({headers})` config (Content-Type is intentionally NOT set — the browser auto-sets `multipart/form-data; boundary=...` to match the FormData). `xhr.responseType = 'text'` (NOT `'json'`) so the load handler can still read the body when the server returns a non-JSON error page (e.g. a 502 from a reverse proxy); the body is `JSON.parse`'d in a try/catch with a `{}` fallback, matching the prior `await res.json().catch(() => ({}))` pattern. (2) **Progress bar DOM** — added a `.album-upload-progress` region to the section template (between the form and the grid). Contains a `.album-upload-progress-bar` (a `<div role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0">` with a `.album-upload-progress-fill` child whose `width: N%` is updated live), a Cancel button, a Retry button (hidden by default), and a status text `<p>` with `aria-live="polite"`. The bar updates via `xhr.upload.addEventListener('progress', e => width = round(e.loaded/e.total*100) + '%')` and pins to 100% on the `xhr.upload 'load'` event. (3) **"Processing…" state** — when the upload body is fully sent (`xhr.upload 'load'`) but the response hasn't arrived yet, the status text switches to bilingual EN "Processing…" / KH "កំពុងដំណើរការ…" and the bar stays at 100%. This covers the server-side malware scan window (ClamAV/Defender can take a few seconds on a 5 MB image). (4) **Cancel button** — calls `xhr.abort()` (guarded by try/catch for the "already completed" edge case). The `xhr 'abort'` event handler shows a brief bilingual "Upload cancelled." / "ការផ្ទុកឡើងត្រូវបានបោះបង់។" message, re-enables the submit button, hides Cancel + Retry, and auto-hides the progress bar after 2.5 s (only if the user hasn't already started a new upload — detected by checking the text node still has the `cancelled` class). The file is NOT cleared from the input on cancel so the user can re-submit. (5) **Error + Retry** — on `xhr 'error'` (network failure, no response) or non-2xx `xhr 'load'`, the status text shows the bilingual EN "Upload failed. Please try again." / KH "ការផ្ទុកឡើងបរាជ័យ។ សូមព្យាយាមម្ដងទៀត។" and the Retry button is shown. Retry re-enters `startUpload()` which re-reads the file + caption from the form (so a user can swap the file between attempts). (6) **MalwareDetected branch** — when `xhr.status === 422 && (result.code === 'malware_detected' || /malware/i.test(result.error))`, the status text shows the distinct bilingual EN "Upload rejected: malware detected." / KH "ការផ្ទុកឡើងត្រូវបានបដិសេធ: បានរកឃើញម៉ាលវែរ។" message (server contract: `server.py` L8313-8315 + L8322-8326 returns `{"error": ..., "code": "malware_detected"}`). (7) **Success** — on 2xx, the progress bar is hidden, the form is reset (clears the file input), the same bilingual `alert(STRINGS.success.en)` confirmation fires (preserved from the prior fetch path), and `load(false)` reloads the grid. (8) **CSS injection** — since the task scope forbids editing any `.css` file (ux-5 owns `organized/styles.css`, ux-6 owns `modern-ui.css`), the progress-bar styles are injected at module load via a single idempotent `<style id="einvite-album-progress-style-v54-18">` element appended to `document.head`. The rules use existing design tokens (`--brand`, `--brand-2`, `--danger`, `--surface-2`, `--border-1`, `--text-2`, `--radius-md`, `--radius-sm`, `--success`) with hard-coded fallbacks so the bar is legible on any page (token-driven on `public.html` which loads `tokens.css`; falls back to defaults on bare preview pages). The gradient fill uses `linear-gradient(90deg, var(--brand), var(--brand-2))` to match the V54.16 host-polls bar chart palette. A `@media(max-width:540px)` rule stacks the bar full-width on mobile. (9) **Accessibility** — the progress region has `role="status" aria-live="polite" aria-atomic="true"`; the bar itself has `role="progressbar"` with `aria-valuemin/max/now` updated live; `aria-label` is updated to match the current state ("Uploading…" → "Processing…"). The Cancel + Retry buttons are real `<button type="button">` elements (keyboard-accessible by default). (10) **Build verification**: `node -e "new Function(...)"` reports `SYNTAX_OK` for `src/js/album.js`; `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles (the `public.html` bundle now contains the new XHR + progress UI); `python3 src/python/sync_frontend_assets.py` synced the updated `album.js` + rebuilt `bundle-public-v15.js` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Verified the synced `src/python/bundle-public-v15.js` contains `XMLHttpRequest`, `.album-upload-progress`, and all 7 new bilingual string keys (`uploading`/`processing`/`uploadFailed`/`malwareDetected`/`retry`/`cancel`/`cancelled`). Acceptance: a 5 MB photo shows a live 0→100% progress bar; Cancel aborts the in-flight xhr; "Processing…" shows between upload-complete and response-arrival; network errors + non-2xx responses show the bilingual "Upload failed" message + Retry button; `MalwareDetected` (422 + `malware_detected` code) shows the distinct malware message; no console errors. | `src/js/album.js` (the ONLY file modified — rewrote `onUpload()` as `startUpload()` using XMLHttpRequest; added the `.album-upload-progress` region to the section template; added 7 new bilingual string keys `uploading`/`processing`/`uploadFailed`/`malwareDetected`/`retry`/`cancel`/`cancelled`; added an idempotent `<style>` injection block at module load for the progress-bar visual styles using existing design tokens; cached the progress-DOM handles at mount for fast updates; added `xhr.upload` progress + load handlers, `xhr` abort/error/load handlers, and Cancel/Retry click handlers), `src/js/bundle-public-v15.js` (rebuilt — includes the new album.js), `src/python/bundle-public-v15.js` + `album.js` (synced from src/js/) |
| V54.20 | ux-6: Empty states, loading skeletons, error states (ROADMAP-V2 §3.6). Closes the perceived-quality gap where host panels rendered nothing while loading and showed raw `err.message` strings on failure. Adds three shared, token-driven state classes to `src/css/modern-ui.css` (`.empty-state`, `.skeleton`, `.error-state`) and wires all three states into the existing render flow of `host-signup-sheets.js` (§3.1, V54.14) and `host-polls.js` (§3.2, V54.16). The album.js + dashboard.js + HTML wiring is owned by ux-3 / ux-4 / ux-5 concurrently and will be added in a follow-up — this task ONLY touches the two host panel JS modules + `modern-ui.css`. (1) **CSS append to `src/css/modern-ui.css`** — `.empty-state` (flex column, centered, `--app-muted` text, `--app-text` title, `--app-accent` primary action button with the existing `var(--spring)` lift-on-hover), `.skeleton` (linear-gradient pulse on `var(--app-line)` at 1.5s ease-in-out infinite, `background-size: 200%` so the gradient sweeps across; `prefers-reduced-motion` disables the animation and pins opacity to .55), and `.error-state` (same shape as `.empty-state` but with `--danger` icon color and a secondary-styled retry button). Skeleton layout helpers: `.skeleton--row` (a card-shaped container reusing `--app-surface` + `--app-line` + `--app-shadow-sm` + `--app-radius` so each skeleton row matches the real host-signup-sheet-row / host-poll-row card), `.skeleton--row-head` (flex row of title + pills matching the real row head), `.skeleton--row-actions` (flex row of action button placeholders), `.skeleton--title` / `.skeleton--pill` / `.skeleton--button` / `.skeleton--text-short` / `.skeleton--avatar` (atomic shape primitives). All colors come from existing tokens — no new design system. (2) **`src/js/host-signup-sheets.js`** — `refresh()` now calls `renderLoading()` first (3 `.skeleton--row` cards, `aria-label` + `aria-busy="true"` on the state region), then either `render(sheets)` (existing list with row-head + actions), `renderEmpty()` (icon 📋, bilingual title "No sign-up sheets yet" / "មិនទាន់មានសន្លឹកចុះឈ្មោះទេ", message "Create a sheet to let guests claim items or slots." / KH equivalent, primary action button that opens the existing create-sheet modal via `openSheetModal(null)`), or `renderError(err)` (icon ⚠️, bilingual title "Couldn't load sign-up sheets" / KH equivalent; network errors — detected via `err instanceof TypeError` or `/fetch\|network/i` — get a dedicated "Network error. Please check your connection." message in EN+KH, HTTP errors get the generic message + the original `err.message` in a `<code>` tag for debugging; retry button re-invokes `refresh()`). The legacy combined `loading` / `empty` / `error` strings are kept (still referenced by the per-row "View claims" sub-region's loading/error path which is intentionally NOT being changed in this task — that's a follow-up). Added 7 new bilingual string keys: `emptyTitle`, `emptyMessage`, `emptyAction`, `errorTitle`, `errorMessage`, `errorNetwork`, `retry`, plus `skeletonAriaLabel` for screen readers. (3) **`src/js/host-polls.js`** — same pattern: `renderLoading()` shows 3 skeleton rows shaped like a poll (question title + 2 status pills + meta text + 4 action buttons — matching the actual `host-poll-row` which has Edit/Delete/Close/View-results buttons), `renderEmpty()` shows icon 📊 + bilingual title "No polls yet" / KH + message "Create a poll to gather guest preferences." / KH + primary action button that opens the create-poll modal, `renderError(err)` shows ⚠️ + bilingual title "Couldn't load polls" / KH + the network-vs-HTTP message branch + retry button. Same 7 new string keys. (4) **Build verification**: `node -e "new Function(...)"` reports `SYNTAX_OK` for both edited JS files; `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles; `python3 src/python/sync_frontend_assets.py` synced the updated `host-signup-sheets.js` + `host-polls.js` + `modern-ui.css` + rebuilt `bundle-dashboard-v15.js/css` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Verified the synced `src/python/bundle-dashboard-v15.css` contains all 18 `empty-state`/`error-state`/`skeleton--` rules and `src/python/bundle-dashboard-v15.js` contains both `renderLoading`/`renderEmpty`/`renderError` triplets (one per host panel). Acceptance: no host-signup-sheets or host-polls panel renders blank while loading; both panels show all three states (skeleton with shape-matching rows / empty state with primary action / error state with retry); every state string is bilingual EN+KH; `prefers-reduced-motion` disables the skeleton pulse. | `src/css/modern-ui.css` (appended `.empty-state` + `.skeleton` + `.error-state` classes + shape primitives `--row`/`--row-head`/`--row-actions`/`--pill`/`--button`/`--title`/`--text-short`/`--avatar` + `@keyframes skeleton-pulse` + `prefers-reduced-motion` opt-out — all token-driven), `src/js/host-signup-sheets.js` (wired `renderLoading`/`renderEmpty`/`renderError` into the existing `refresh()` flow + 7 new bilingual string keys + retry handler that re-invokes `refresh()` + empty-state action button that opens the existing create-sheet modal), `src/js/host-polls.js` (same pattern + same 7 string keys), `src/js/bundle-dashboard-v15.js` + `src/css/bundle-dashboard-v15.css` (rebuilt), `src/python/bundle-dashboard-v15.js` + `bundle-dashboard-v15.css` + `host-signup-sheets.js` + `host-polls.js` + `modern-ui.css` (synced) |
| V54.19 | ux-5: Complete WCAG P1 items (ROADMAP-V2 §3.5). Closes the 6 remaining P1 items from `docs/a11y/WCAG-AA-AUDIT.md` §7 that the Phase 2b initial pass (V54.2) had deferred. The audit had marked contrast fixes + skip-links as done but left 6 P1 items as Scheduled/Blocked. sec-1 (V54.9) unblocked P1-D by escaping every user-controlled placeholder in `serve_public`, so the dynamic `<html lang>` attribute is now safe to set client-side. (1) **P1-A** — `:lang(km)` + `[lang="km"]` global CSS rules appended to `src/css/organized/styles.css` (universal — styles.css is in every page's bundle per `docs/route-bundle-sources-v15.json`). Sets `line-height:1.6; font-size:16px; letter-spacing:normal; font-feature-settings:normal; font-synthesis:none` so Latin-optimised system fonts no longer render Khmer with cramped line-height / overlapping conjuncts. (2) **P1-B** — `lang="km"` added to every `.khmer-text` element in `src/html/index.html` (7 elements: #namesKm, #khmerDatePreview, #venueKm, #messageKm, #countdownTitleKm, #scheduleTextKm, #venuesTextKm) + the two Khmer `<option>`s in #khmerPhase. The three JS renderers ux-5 owns (`signup-sheets.js`, `polls.js`, `invitation-edit-history.js`) now emit `lang="km"` / `lang="en"` on every `langText` span + the resend-status spans. `album.js` (ux-4) and `host-signup-sheets.js` / `host-polls.js` (ux-1 / ux-2) are documented as follow-ups — they were not modified by ux-5 per the task scope. (3) **P1-C** — Audited the 3 modifiable JS renderers (none emit `<img>` tags, no fixes needed); audited the modifiable HTML files (the `<img id="v13MfaQr" alt="Authenticator setup QR code">` in `account.html` + the `<img alt="Wedding flowers">` / `<img alt="Crop preview">` in `index.html` already carry meaningful alt). The renderers owned by sibling subagents are documented as a follow-up; spot-check of `guests.js` confirmed its dynamic QR `<img>` already has `alt="Personal QR code for ${name}"`. (4) **P1-D** — `public.html` now carries an inline script (placed between `#publicRoot` and `bundle-public-v15.js`) that watches `#publicRoot[data-language]` via `MutationObserver` and sets `document.documentElement.lang` to `'km'` or `'en'`. The static fallback remains `lang="en"`. `public-page.js`'s existing `bindLanguageSwitch()` already calls `document.documentElement.lang = lang` directly; the inline observer is defence-in-depth in case that direct assignment is ever dropped. sec-1 (V54.9) already escapes every user-controlled placeholder in `serve_public`, so the dynamic attribute is now safe. The server does NOT inject `__INVITATION_LANG__` (sec-1 owns `serve_public` and chose escaping over a new placeholder); the attribute is set client-side only. (5) **P1-E** — Bilingual `.sr-only` `<label for="...">` elements added to filter inputs on `guests.html` (8 add-form fields + #search + #statusFilter), `responses.html` (#responseSearch + #responseFilter), `templates.html` (#studioSearch + #studioSource + #studioCategory), `materials.html` (#search + #typeFilter + #folderFilter + #sort), `admin.html` (#adminSearch gained a bilingual `aria-label`), `checkin.html` (#guestSearch already had a visible `<label>` — left alone), and `index.html` (#assetTypeFilter + #zoomLevel gained `.sr-only` labels alongside their existing `aria-label`). A universal `.sr-only` utility class was added to `src/css/organized/styles.css` so the labels resolve on every page regardless of which other bundles are mounted. (6) **P1-F** — `aria-modal="true"` + `aria-labelledby` added to every static `<dialog>` in the modifiable HTML files: `index.html#modal` (labelled by #modalHeading), `checkin.html#scannerDialog` (#scannerDialogTitle), `guests.html#qrModal` (#qrModalHeading), `materials.html#editDialog` (#editDialogTitle), `templates.html#studioDialog` (#studioDialogTitle). `dashboard.html#createDialog` + `#templatePreviewDialog` are owned by ux-3 and will be patched in that worklog. **Focus trap:** every static `<dialog>` is opened via the native `HTMLDialogElement.showModal()` API (verified in `ui-dialogs.js:19-20`, `templates.js`, `materials.js:10`, `guests.js:11,15,16`, dashboard's `#newBtn` handler), which provides native browser-level focus trapping per the HTML spec — no extra JS focus-trap utility required. The dynamically-created `<dialog>` elements emitted by `ui-dialogs.js#buildDialog` also use `showModal()`. The non-native `<div role="dialog">` modals in `host-signup-sheets.js`, `host-polls.js`, `public-share-panel.js`, `guest-journey.js`, `raster-workspace-v30.js`, `professional-layers-v29.js`, `production-readiness-v32.js` carry `aria-modal="false"` by design (they are panels, not full modals) and are out of scope for this task. (7) **Audit doc** — `docs/a11y/WCAG-AA-AUDIT.md` §7 P1 table updated: P1-A/B/D/E/F → **Pass (V2-UX-5)**, P1-C → **Partial (V2-UX-5)** (3 modifiable renderers audited + clean; sibling-owned renderers documented as follow-up). Added V54.19 entry to §10 change history. (8) **Build verification**: `node -e "new Function(...)"` reports `SYNTAX_OK` for all 3 modified renderers + the rebuilt bundles (public, dashboard, index, guests, materials, responses, templates, checkin, admin); `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles (universal `:lang(km)` + `.sr-only` rules in styles.css propagated to every page bundle); `python3 src/python/sync_frontend_assets.py` synced 17 files (11 JS + 6 CSS) to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Acceptance: all 6 P1 items in the audit doc marked Pass or Partial with evidence; `:lang(km)` rule exists in `src/css/organized/styles.css`; modifiable HTML pages have appropriate `lang` attributes on Khmer text; `public.html` dynamically sets `<html lang>` based on invitation language; visible labels on filter inputs (or `.sr-only` labels); `aria-modal="true"` on all static `<dialog>` elements; bundle rebuild passes `--check`. | `src/css/organized/styles.css` (appended `:lang(km)` + `[lang="km"]` + `.sr-only` rules — universal via styles.css bundle entry), `src/html/index.html` (lang="km" on 7 .khmer-text elements + 2 Khmer options; .sr-only labels for #assetTypeFilter + #zoomLevel; aria-modal + aria-labelledby on #modal), `src/html/public.html` (inline MutationObserver script for dynamic <html lang>), `src/html/guests.html` (10 .sr-only labels for filter + add-form inputs; aria-modal on #qrModal), `src/html/materials.html` (4 .sr-only labels for filter inputs; aria-modal on #editDialog), `src/html/responses.html` (2 .sr-only labels for filter inputs), `src/html/templates.html` (3 .sr-only labels for filter inputs; aria-modal on #studioDialog), `src/html/checkin.html` (aria-modal on #scannerDialog), `src/html/admin.html` (bilingual aria-label on #adminSearch), `src/js/signup-sheets.js` + `src/js/polls.js` + `src/js/invitation-edit-history.js` (langText now emits lang="km"/lang="en" on every text fragment; resend-status spans in invitation-edit-history.js likewise), `docs/a11y/WCAG-AA-AUDIT.md` (P1-A through P1-F statuses updated to Pass/Partial with evidence; V54.19 change-history entry), all 9 rebuilt route bundles (bundle-public/dashboard/index/guests/materials/responses/templates/checkin/admin-v15.js + .css) + `src/python/` synced copies |
| V54.15 | sec-6: Resource-scoped permissions Stage 3 (ROADMAP-V2 §2.6). Closes the Phase 1a stub: the runtime was still on the coarse `read`/`edit`/`manage`/`admin` tier check (V28 design) despite Phase 1a §3 having designed a 4-stage migration to resource-scoped grants like `event:{id}:publish`. Stage 3 lands the new check alongside the legacy tier check: if a grant EXISTS for the tool's (resource_type, action) for the current user, the new grant check is authoritative; otherwise the legacy tier check is authoritative. Stage 4 (Phase 1b) will remove the legacy fallback. (1) **New module `ai_agent/scopes.py`** — `Grant` frozen dataclass with `resource_type` / `resource_id` / `action` fields and a `parse()` classmethod that accepts both the 3-part form (`event:123:publish`) and the 2-part form (`template:install`, desugared to `template:*:install`). The grammar regexes (`[a-z][a-z0-9-]*` for resource_type and action, `[A-Za-z0-9][A-Za-z0-9_.\-]*` for resource_id) reject uppercase letters and reserved characters so a malformed grant cannot bypass matching. `Grant.matches(other)` returns True iff resource_type is exactly equal AND (self.resource_id == "*" OR equals other.resource_id) AND (self.action == "*" OR equals other.action). `parse_grants(list_of_strings)` skips malformed entries with a `warnings.warn` so a single bad DB row cannot break the availability check. `evaluate(grants, required_resource_type, required_resource_id, required_action)` is the pure no-I/O entry point that the runtime grant check calls. (2) **`ai_agent/capabilities.py`** — `build_access_snapshot()` now queries the `agent_grants` table and injects a `grants` list (plain dicts for JSON-serialisability) of the user's active grants (revoked / expired filtered out) into the snapshot. New module-level `TOOL_REQUIRED_GRANTS` dict maps tool ids to their required `(resource_type, action)` tuple — currently `publish.prepare` → `("event", "publish")` plus 8 other high-risk tools (`invitation.archive`, `invitation.update_operations`, `message.prepare_send`, `guest.create`/`update`/`delete`, `rsvp.update`, `invitation.configure_rsvp`). The legacy tier check is preserved verbatim as `_legacy_availability()` and called by the new `availability()` first; the new `availability()` then runs the grant check ONLY for tools in `TOOL_REQUIRED_GRANTS`. Per §2.6 semantics: if the user has ANY grant for the tool's (resource_type, action), the new check is authoritative (grant must match the required `(resource_type, invitation_id, action)`); if the user has NO grant for that (resource_type, action), the legacy tier check result is returned unchanged. Shadow-evaluation logging is implemented via an in-memory `_SHADOW_EVAL_LOGS` list (capped at 1024 entries) and a structured stdout JSON line per entry for SIEM ingestion. Two event types: `shadow_eval_divergence` (new_check != legacy_check; both ran) and `no_grant_legacy_authoritative` (no grant exists for the user on that resource_type+action → legacy is authoritative). Neither log changes the operation's outcome — they are telemetry for the Stage 4 migration. New public helpers `reset_shadow_eval_logs()` / `recent_shadow_eval_logs(limit)` are consumed by the test. (3) **`src/python/server.py`** — new `agent_grants` table added to the SQLite schema block (`_P2A_SCHEMA_STATEMENTS`) with `id`, `user_id`, `resource_type`, `resource_id`, `action`, `granted_by`, `granted_at`, `expires_at`, `revoked_at`, `revoked_by`, `revoke_reason`, `reason` columns and two indexes: `idx_agent_grants_user` on `(user_id, revoked_at, expires_at)` for the per-user active-grant lookup, and `idx_agent_grants_resource` on `(resource_type, resource_id, action, revoked_at)` for the per-resource audit. NEW routes: `GET /api/account/grants` (lists the calling user's grants including revoked ones, marked with `revokedAt`); `POST /api/account/grants` (host-only — for `event`/`invitation` resource types, the granter must own/manage the resource via `can_manage_invitation`; for other types, the granter must be admin; body `{user_id, resource_type, resource_id, action, expires_at?, reason?}`; validates the grant against the `Grant.parse()` grammar and returns 400 `grant_invalid_syntax` on malformed input; emits the `grant.created` audit event); `DELETE /api/account/grants/{grant_id}` (host-only — the requesting user must be the grant's `granted_by` OR own/manage the resource the grant is scoped to OR be admin; optional `?reason=` query param; soft-deletes the grant by setting `revoked_at` + `revoked_by` + `revoke_reason`; emits the `grant.revoked` audit event). (4) **`docs/postgres_schema.sql`** — same `agent_grants` table + indexes appended after the V54.12 `mfa_recovery_codes` block, with `REFERENCES users(id) ON DELETE CASCADE` on `user_id` and `REFERENCES users(id) ON DELETE SET NULL` on `granted_by` for proper referential integrity (SQLite has no FK enforcement in the runtime path; PostgreSQL enforces it). (5) **Test `tests/ai_resource_scopes_test.py`** (NEW, 9 scenarios — real-HTTP integration test using in-process `ThreadingHTTPServer`): (A) BEFORE grant — host B (a `viewer` collaborator on both event1 and event2, owned by A) cannot call `publish.prepare` on either event. (B) A grants B `event:event1:publish` via POST `/api/account/grants`; `grant.created` audit event present. (C) B's `GET /api/ai-agent/tools?invitationId=event1` now lists `publish.prepare` (new grant check authoritative; legacy tier check denies for `viewer` not in {owner, manager}); a `shadow_eval_divergence` log entry was emitted with `new_check=allow, legacy_check=deny`. (D) B's same call against event2 does NOT list `publish.prepare` (no grant matches event2; legacy also denies for viewer; no divergence). The new check ran (B has a grant for event/publish, just for event1 not event2) — the test asserts NO `no_grant_legacy_authoritative` log fired for `publish.prepare` (i.e. the new check was authoritative, not legacy fallback). (E) A's call against event1 lists `publish.prepare` (legacy allows — owner; A has no grants of any kind → `no_grant_legacy_authoritative` log fired with `legacy_check=allow`). (F) Revoke the grant via `DELETE /api/account/grants/{grant_id}?reason=...`; `grant.revoked` audit event present. (G) B's call against event1 no longer lists `publish.prepare` (no grant → legacy tier check authoritative → deny for viewer; `no_grant_legacy_authoritative` log fired with `legacy_check=deny`). (H) `GET /api/account/grants` lists the revoked grant with `revokedAt` set. (I) Route security — a freshly-registered outsider (no invitation access) gets 403 `grant_not_host` when attempting to grant themselves `event:event1:publish`; an outsider attempting to revoke an existing grant gets 403 `grant_not_authorised_to_revoke`. (J) Grant grammar validation — POST `/api/account/grants` with `action: "PUBLISH"` (uppercase, rejected by the Grant grammar regex) returns 400 `grant_invalid_syntax`. The test bypasses the LLM by exercising the capability_catalog endpoint (`GET /api/ai-agent/tools?invitationId=X`) and asserting on the returned `tools[].id` list. The shadow-eval log is inspected via `ai_agent.capabilities.recent_shadow_eval_logs()` (direct in-process access — same-process `ThreadingHTTPServer`). (6) **Acceptance**: test passes (`PYTHONPATH=src/python:. python3 tests/ai_resource_scopes_test.py` → `AI_RESOURCE_SCOPES_TEST_PASSED`); `python3 -m py_compile ai_agent/scopes.py ai_agent/capabilities.py src/python/server.py` exits 0; the JIT regression test still passes (`ai_jit_enforcement_test.py` → `AI_JIT_ENFORCEMENT_TEST_PASSED`); both check paths log divergences when they disagree (asserted in scenarios C and E). Commit style: `sec-6: resource-scoped permissions Stage 3 (§2.6)`. | `ai_agent/scopes.py` (NEW — `Grant` frozen dataclass, `parse()`, `matches()`, `__str__`, `__eq__` via dataclass; `parse_grants()`, `evaluate()`, `GrantParseError`; module docstring documents the grammar + 2-part desugaring + matching rules), `ai_agent/capabilities.py` (NEW module-level `TOOL_REQUIRED_GRANTS` map + `_SHADOW_EVAL_LOGS` buffer + `reset_shadow_eval_logs()` / `recent_shadow_eval_logs()` / `_log_shadow_eval()` / `_format_grant_str()` helpers; `build_access_snapshot()` extended to load active grants into `snapshot["grants"]` as plain dicts; `_legacy_availability()` extracted from old `availability()`; new `availability()` runs the legacy check first then — for tools in `TOOL_REQUIRED_GRANTS` — runs the new grant check + emits `shadow_eval_divergence` or `no_grant_legacy_authoritative` logs), `src/python/server.py` (SQLite schema: `agent_grants` table + 2 indexes added to `_P2A_SCHEMA_STATEMENTS`; route dispatch: GET `/api/account/grants` in `do_GET`, POST `/api/account/grants` in `do_POST`, DELETE `/api/account/grants/{grant_id}` in `do_DELETE`; handler methods `list_agent_grants`, `create_agent_grant`, `revoke_agent_grant` with host-only checks + Grant grammar validation + audit events), `docs/postgres_schema.sql` (NEW `agent_grants` table block with FKs + indexes appended after `mfa_recovery_codes`), `tests/ai_resource_scopes_test.py` (NEW — 9-scenario real-HTTP integration test), `VERSION_HISTORY.md` (V54.15 entry above V54.16) |
| V54.16 | ux-2: Host-side management UI for polls (ROADMAP-V2 §3.2). Fills the §3.2 UX gap where guests could vote on the public page but the host had no dashboard UI to create / edit / delete / close polls or view per-option results + voter emails. Mirrors `host-signup-sheets.js` (§3.1, V54.14) for IIFE shape, STRINGS pattern, modal pattern, empty/loading/error states, and inline mount script — the two panels feel like siblings inside `<main>` of `dashboard.html`. (1) **New JS module** `src/js/host-polls.js` exports `EInviteHostPolls.mount(root, {invitationId, lang})`. Renders a panel with: list view (question, status badge Open/Closed, visibility badge Live/Hidden, multi/single badge, options count, total votes count, deadline — pulled from the existing host-mode `GET /api/invitations/{id}/polls` response which already returns `options[].votes` counts + `votes[]` voter PII), "Create poll" modal (bilingual EN+KH placeholders, question input + dynamic add/remove option rows capped at 2–20 + multi-select checkbox + visibility radio `live`|`hidden_until_close` + optional datetime-local deadline), Edit modal pre-filled via `PUT`, Delete with bilingual confirm dialog, "Close now" which sends `PUT` with `deadlineTs = now - 1000` (1s in the past) so the server's `closed = deadline_ts < now` check flips `true` on the next list refresh — the existing API has no dedicated "close" verb, so this reuses the deadline update path, and an expandable "View results" region per poll that calls `GET /api/invitations/{id}/polls/{pid}/results` and renders a CSS bar chart (per-option bar `width = votes/maxVotes*100%` with `linear-gradient(90deg,var(--app-accent),var(--app-accent-2))` fill, no chart library) plus a host-only voter table (voter name, **full email host-only PII per §3.2 acceptance**, choice label, voted-at). Empty / loading / error states for both the list and the per-poll results region. (2) **Dashboard integration** — added `<section id="polls-panel" class="event-section" data-event-section="polls">` + `<div id="polls-mount">` + a small inline `<script>` to `src/html/dashboard.html`, placed AFTER the `<section id="signup-sheets-panel">` that ux-1 added (sibling ordering per the task spec). The inline script shares the same invitation-id resolver chain (`window.EINVITE_INVITATION_ID` → URL `?invitation=` → `EInviteContext.getInvitationId()` → `localStorage.sovan-active-invite`) so both panels see the same active invitation. (3) **Bundle manifest** — added `host-polls.js` to the `dashboard.html` scripts list in `docs/route-bundle-sources-v15.json`, immediately AFTER `host-signup-sheets.js`. (4) **CSS** — appended host-polls panel + modal + bar chart rules to `src/css/modern-ui.css` (reuses existing tokens: `--app-surface`, `--app-line`, `--app-line-2`, `--app-accent`, `--app-accent-2`, `--app-accent-soft`, `--app-muted`, `--app-good`, `--app-warn`, `--app-shadow-sm`, `--app-radius`, `--danger` fallback — no new design system; mobile-responsive collapse for the bar chart rows + the option rows). (5) **No backend changes** — the Phase 2a-1 API (GET/POST/PUT/DELETE `/api/invitations/{id}/polls[/{pid}]` + `/polls/{pid}/results` + `/polls/{pid}/vote`) already supports host-mode CRUD with vote counts and voter PII included in the host response; this task is purely additive frontend. (6) Build verification: `node -e "new Function(...)"` reports `SYNTAX_OK` for both `src/js/host-polls.js` and the rebuilt `src/js/bundle-dashboard-v15.js`; `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles; `python3 src/python/sync_frontend_assets.py` synced `host-polls.js` + `dashboard.html` + `modern-ui.css` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Verified the synced `src/python/bundle-dashboard-v15.js` contains `EInviteHostPolls = { mount` (1 occurrence) and `src/python/dashboard.html` contains `polls-panel` (2 occurrences — section + inline mount script reference). Bilingual EN+KH strings for every label/button/message per ROADMAP-V2 §3 ground rule. Acceptance: host can create/edit/delete/close a poll from the dashboard; guest public.html reflects changes after refresh; live results work (vote → bar update on refresh). | `src/js/host-polls.js` (new — mount + list/create/edit/delete/close/results UI, bilingual EN+KH, CSS bar chart, voter table), `src/html/dashboard.html` (added `<section id="polls-panel">` after the signup-sheets panel + mount point + inline mount script), `docs/route-bundle-sources-v15.json` (dashboard.html scripts + `host-polls.js` after `host-signup-sheets.js`), `src/css/modern-ui.css` (appended host-polls panel + modal + bar chart styles — token-driven), `src/js/bundle-dashboard-v15.js` + `src/css/bundle-dashboard-v15.css` (rebuilt), `src/python/bundle-dashboard-v15.js` + `bundle-dashboard-v15.css` + `host-polls.js` + `dashboard.html` + `modern-ui.css` (synced) |
| V54.13 | sec-5: Enforce JIT elevation (close Phase 1a stub, ROADMAP-V2 §2.5). `ai_agent/jit_elevation.py::evaluate()` previously returned `True` unconditionally — anyone reading V54.6 would have assumed JIT elevation was live when in fact nothing was enforced. This entry closes that gap. (1) **`ai_agent/jit_elevation.py`** — `evaluate(user_id, tool_id, resource_id=None, resource_type=None, action=None, workspace_id=None, invitation_id=None)` now performs a real lookup against the `jit_elevations` table: `SELECT ... WHERE user_id=? AND tool_id=? AND status='granted' AND expires_at > now AND revoked_at IS NULL AND consumed_at IS NULL` (optional `resource_id`/`resource_type`/`action`/`invitation_id`/`workspace_id` filters for stricter matching). On a hit, the grant is marked consumed (`consumed_at = now`, one-shot — the `consumed_at IS NULL` filter + this UPDATE together implement the one-shot contract) and a `jit.consumed` audit event is emitted. On a miss, returns `False` so the caller can start the request/grant flow. `sweep_expired()` is invoked lazily at the top of every `evaluate()` call so expired grants are transitioned to `status='expired'` (and emit `jit.expired`) without an external cron. NEW `request_elevation(user_id, tool_id, resource_id, resource_type, reason, *, action='', workspace_id='', invitation_id='', role='', plan_id='', plan_index=-1, metadata=None) -> dict` — creates a `jit_elevations` row with `status='requested'`, emits `jit.requested`, then applies the §4.1 auto-approval rules: if the requester's `role` is `owner` or `manager` AND the tool is in the new `AUTO_APPROVED_TOOLS` frozenset (currently just `publish.prepare`; `delete.*` and `bulk_*` are explicitly excluded even for owners), the row is immediately granted (`status='granted'`, `granted_at`, `expires_at = now + 300s`) and `jit.granted` is emitted. Otherwise the row stays `requested` and requires manual approval. Returns the elevation row as a dict (camelCase aliases for the API surface). NEW `get_elevation(elevation_id) -> dict | None` and `list_pending(workspace_id='', invitation_id='') -> list[dict]`. The auto-approval policy lives in the module-level `_is_auto_approved(tool_id, role)` helper so it is shared by both `request()` (the `ElevationRequest` path) and `request_elevation()` (the keyword-argument path used by `authorize_tool_call`). The `ElevationRequest` dataclass gained a `role: str = ''` field so the policy engine can apply §4.1 without a separate DB lookup at decision time. (2) **`ai_agent/service.py`** — `AgentService.__init__` instantiates `self.jit = JITElevationManager(connect, audit=audit)` (schema is created on first instantiation via the idempotent `ensure_jit_schema()`; the manager reuses the same audit callback that writes to the hash-chained `audit_events` table). `authorize_tool_call()` now checks `is_jit_eligible(tool_id)` AFTER the existing capability + revision + fingerprint checks but BEFORE the 30-second one-shot token is issued. If the tool is JIT-eligible: `evaluate(user_id, tool_id, resource_id=invitation_id, resource_type='invitation', action=<derived from call.arguments.action|mode|tool_id>, workspace_id=<from invitation context>, invitation_id=invitation_id)` is called. On `True`: proceed with token issuance (the grant was just consumed). On `False`: `request_elevation(...)` is called — if the returned row's `status == 'granted'` (auto-approved), `evaluate()` is called again to consume the new grant and then proceeds; if `status == 'requested'` (manual approval required), the handler returns `{needsElevation: true, toolId, ttlSeconds: 300, requestId, status, autoEligible}` WITHOUT issuing an authorization token (the caller must `POST /api/ai-agent/jit/approve` then retry). Read/edit tools (those NOT in `JIT_ELIGIBLE_TOOLS`) skip the JIT check entirely and proceed as before. (3) **`src/python/server.py`** — NEW routes (all host-only, requiring `can_manage_invitation` for the elevation's invitation): `POST /api/ai-agent/jit/approve` (body `{request_id}` — looks up the elevation, verifies the requesting user owns/manages the elevation's invitation, calls `jit.grant(request_id, approver_id=user.id, approval_channel='web')`, returns `{ok: true, expires_at: <ts>}`); `POST /api/ai-agent/jit/deny` (body `{request_id, reason?}` — same host check, calls `jit.deny(request_id, denied_by=user.id, deny_reason=reason)`, returns `{ok: true}`); `GET /api/ai-agent/jit/pending` (optional `?invitationId=` query param — when provided, lists pending requests for that one invitation after a host check; when omitted, lists ALL pending requests for invitations the requesting user owns or manages, resolved via a `LEFT JOIN invitation_collaborators` query). The `JITElevationError` exception type is now imported at the top of `server.py` so the new handlers can convert `grant()`/`deny()` errors (e.g. `jit_not_found`, `jit_not_requestable`) into proper HTTP responses. (4) **Test** (`tests/ai_jit_enforcement_test.py`, NEW — 8-phase real-HTTP integration test using in-process `ThreadingHTTPServer` so the test process can also reach into the SQLite DB directly via `server.connect()` to construct plans, expire grants, and inspect audit events): (A) **Manual approval** — an owner calling `message.prepare_send` (JIT-eligible but NOT in `AUTO_APPROVED_TOOLS`) receives `{needsElevation: true, requestId: ...}` instead of an authorization token; the response contains NO `authorizationToken`; after `POST /api/ai-agent/jit/approve`, the same authorize call succeeds with `{authorized: true}`; the grant's `consumed_at` is set (one-shot). (B) **Expiry** — after the test sets `expires_at = past` in the DB, the next authorize call returns `needsElevation` again (a NEW `requestId`, not the old one); the old grant is transitioned to `status='expired'` by the lazy `sweep_expired()` inside `evaluate()`. (C) **Read/edit skip JIT** — `object.update` (permission=edit, NOT in `JIT_ELIGIBLE_TOOLS`) succeeds immediately with `{authorized: true}` and NO `needsElevation` field; the test asserts no `jit_elevations` row exists for `object.update` for that user. (D) **Auto-approval** — an owner calling `publish.prepare` (in `AUTO_APPROVED_TOOLS`) succeeds immediately with `{authorized: true}` and NO `needsElevation`; the test asserts a `jit_elevations` row exists with `status='granted'`, `auto_eligible=1`, `consumed_at IS NOT NULL`. (E) **Deny** — `POST /api/ai-agent/jit/deny` transitions the elevation to `status='denied'` + `revoked_at IS NOT NULL`; the next authorize call creates a NEW request (the denied one cannot be reused). (F) **Host-only authorization** — a freshly-registered outsider (no invitation access) attempts to `POST /api/ai-agent/jit/approve` and `/jit/deny` for the host's elevation → 403 `jit_not_host`; the elevation row remains `status='requested'` (unchanged). (G) **Audit events** — `jit.requested`, `jit.granted`, `jit.consumed`, `jit.denied`, `jit.expired` are all present in the hash-chained `audit_events` table for the host user. (H) **Negative proof** — monkey-patching `JITElevationManager.evaluate` to `return True` (the old Phase 1a stub behavior) makes the manual-approval tool succeed WITHOUT elevation — proving the wiring is load-bearing: if a future refactor accidentally re-introduces the stub, the main scenarios above will fail (they assert `needsElevation: true`). After the patch is removed, the same call returns `needsElevation` again. The test bypasses the LLM by constructing plans directly via `AgentStore.create_plan(...)` + `service.confirm_plan(...)` (using `ai_agent.context.fingerprint(document)` to compute the document fingerprint that `confirm_plan` checks against `ai_plans.document_fingerprint`). Pytest-compatible entry point `test_ai_jit_enforcement` + direct `python3` runner. Prints `AI_JIT_ENFORCEMENT_TEST_PASSED` on success. (5) **Acceptance**: test passes (`PYTHONPATH=src/python:. python3 tests/ai_jit_enforcement_test.py` → `AI_JIT_ENFORCEMENT_TEST_PASSED` in ~3s); `python3 -m py_compile ai_agent/jit_elevation.py ai_agent/service.py src/python/server.py` exits 0; the negative-proof sub-test demonstrates that disabling JIT (monkey-patch `evaluate` to return `True`) makes the manual-approval tool succeed without elevation — proving the wiring is in force; no regressions in sec-1/2/3/4 tests (`security_reflected_injection_test.py`, `security_account_lockout_test.py`, `security_mfa_recovery_test.py`, `security_notification_emails_test.py`, `security_regression_test.py`, `security_maintenance_test.py` all still pass). Commit style: `sec-5: enforce JIT elevation (close Phase 1a stub, §2.5)`. | `ai_agent/jit_elevation.py` (REWRITTEN — module docstring updated to reflect enforcement is live; `ElevationRequest` gained `role: str = ''` field; NEW module-level `_is_auto_approved(tool_id, role)` + `AUTO_APPROVED_TOOLS = frozenset({'publish.prepare'})`; `_auto_eligible()` now delegates to `_is_auto_approved` instead of returning `False`; `evaluate()` REPLACED — was `evaluate(elevation_id, tool_id, resource_type, resource_id, action) -> True` stub, now `evaluate(user_id, tool_id, resource_id=None, resource_type=None, action=None, workspace_id=None, invitation_id=None) -> bool` performing a real `jit_elevations` lookup with one-shot consumption + lazy `sweep_expired()`; NEW `request_elevation()`, `get_elevation()`, `list_pending()` methods), `ai_agent/service.py` (L16 NEW import `from .jit_elevation import JITElevationManager, is_jit_eligible`; L52 NEW `self.jit = JITElevationManager(connect, audit=audit)` instantiation in `__init__`; L416-504 `authorize_tool_call` extended — after the capability + revision checks, before token issuance, calls `is_jit_eligible(tool_id)` → if True, calls `self.jit.evaluate(...)` and on False calls `self.jit.request_elevation(...)`, returning `{needsElevation: true, toolId, ttlSeconds, requestId, status, autoEligible}` for pending requests or proceeding for auto-approved grants), `src/python/server.py` (L22 NEW `from ai_agent.jit_elevation import JITElevationError, JITElevationManager` import; L3290 NEW `GET /api/ai-agent/jit/pending` dispatch; L3539-3540 NEW `POST /api/ai-agent/jit/approve` + `/jit/deny` dispatch entries; L3868-3979 NEW `_jit_request_host_check`, `ai_agent_jit_approve`, `ai_agent_jit_deny`, `ai_agent_jit_pending` handler methods), `tests/ai_jit_enforcement_test.py` (NEW — 8-phase real-HTTP integration test using in-process `ThreadingHTTPServer`; covers manual approval, expiry, read/edit skip JIT, auto-approval, deny, host-only authorization, audit events, negative proof via monkey-patch; direct `sqlite3.connect` via `server.connect()` for DB row inspection + audit_events inspection), `VERSION_HISTORY.md` (V54.13 entry above V54.12) |
| V54.14 | ux-1: Host-side management UI for sign-up sheets (ROADMAP-V2 §3.1). Fills the UX gap where guests could claim slots on the public page but the host had no dashboard UI to create / edit / delete sheets or view claims. (1) **New JS module** `src/js/host-signup-sheets.js` exports `EInviteHostSignupSheets.mount(root, {invitationId, lang})`. Renders a panel with: list view (title, type badge Items/Slots, slot count, claim count, deadline), "Create sheet" modal (bilingual EN+KH placeholders, title + description + Items/Slots radio + dynamic add/remove slot rows with label/capacity/deadline + sheet-wide datetime-local deadline), Edit modal pre-filled via `PUT`, Delete with bilingual confirm dialog, and an expandable "View claims" region per sheet (table of claimer name, **full email host-only PII**, quantity, claimed-at timestamp — pulled from the existing host-mode `GET /api/invitations/{id}/signup-sheets` response which already returns `slot[].claims[]` with `name/email/quantity/createdAt`). Empty / loading / error states. (2) **Dashboard integration** — added `<section id="signup-sheets-panel" class="event-section" data-event-section="signup-sheets">` + `<div id="signup-sheets-mount">` + a small inline `<script>` to `src/html/dashboard.html`. The inline script resolves the invitation id from `window.EINVITE_INVITATION_ID` → URL `?invitation=` → `EInviteContext.getInvitationId()` → `localStorage.sovan-active-invite` fallback chain, and mounts the panel. Coordinates with ux-2 (host-polls) by being placed BEFORE the polls panel slot inside `<main>`. (3) **Bundle manifest** — added `host-signup-sheets.js` to the `dashboard.html` scripts list in `docs/route-bundle-sources-v15.json` (after `invitation-edit-history.js`). (4) **CSS** — appended host-signup-sheets panel + modal rules to `src/css/modern-ui.css` (reuses existing tokens: `--app-surface`, `--app-line`, `--app-accent`, `--app-accent-soft`, `--app-shadow-sm`, `--app-radius`, `--danger` fallback — no new design system; mobile-responsive slot-row grid). (5) **No backend changes** — the Phase 2a-1 API (GET/POST/PUT/DELETE `/api/invitations/{id}/signup-sheets[/{sid}]`) already supports host-mode CRUD with claims included; this task is purely additive frontend. (6) Build verification: `python3 src/python/build_route_bundles.py` rebuilt all 16 route bundles; `python3 src/python/sync_frontend_assets.py` synced `host-signup-sheets.js` + `dashboard.html` + `modern-ui.css` to `src/python/`; `python3 src/python/build_route_bundles.py --check` returns `ROUTE_BUNDLE_CHECK_PASSED`. Bilingual EN+KH strings for every label/button/message per ROADMAP-V2 §3 ground rule. Acceptance: host can create/edit/delete a sheet from the dashboard; guest public.html reflects changes after refresh; no console errors when the panel loads. | `src/js/host-signup-sheets.js` (new — mount + list/create/edit/delete/claims UI, bilingual EN+KH), `src/html/dashboard.html` (added `<section id="signup-sheets-panel">` + mount point + inline mount script), `docs/route-bundle-sources-v15.json` (dashboard.html scripts + `host-signup-sheets.js`), `src/css/modern-ui.css` (appended host-signup-sheets panel + modal styles — token-driven), `src/js/bundle-dashboard-v15.js` + `src/css/bundle-dashboard-v15.css` (rebuilt), `src/python/bundle-dashboard-v15.js` + `bundle-dashboard-v15.css` + `host-signup-sheets.js` + `dashboard.html` + `modern-ui.css` (synced) |
| V54.12 | sec-3: MFA recovery codes (P1-C from ASVS L2 gap analysis §2.3). A user who loses their authenticator is permanently locked out — a real support burden and a security-blocker for wider adoption. This entry adds the standard ASVS L2 recovery-code flow: 10 single-use pre-generated codes shown ONCE at MFA-enable time, hashed at rest (Argon2id when `argon2-cffi` is installed, else PBKDF2-HMAC-SHA256 with 310k iterations and a 32-byte salt — same primitives as `hash_password`), consumed one-shot via a public `/api/auth/mfa/recover` endpoint that issues a real session and audits `mfa.recovery_used`. (1) **Schema** (new table `mfa_recovery_codes`): `id TEXT PRIMARY KEY, user_id TEXT NOT NULL, code_hash TEXT NOT NULL, used_at BIGINT, created_at BIGINT NOT NULL` + index on `user_id`. Added to the SQLite `executescript` block in `connect_sqlite()` (`src/python/server.py` L1355-1361, immediately after the `passkeys` table) and to `docs/postgres_schema.sql` (L434-447, with a `REFERENCES users(id) ON DELETE CASCADE` FK + the same index). (2) **Helper functions in `src/python/security_v13.py`** (L69-173, immediately after `verify_password`): `_RECOVERY_ALPHABET = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"` (32-char Crockford-style alphabet — no 0/O/1/I/L for visual clarity, power-of-2 length so `secrets.randbelow` is bias-free without rejection sampling); `_generate_one_recovery_code()` returns a single `XXXX-XXXX-XXXX` code (12 chars from the 32-char alphabet + 2 hyphens for display = 14 chars on the wire, ~60 bits of entropy per code — well above the 2^32 ASVS L2 minimum for self-issued secrets); `generate_recovery_codes(count=10)` returns a list of `(plaintext, code_hash)` tuples so the caller can show plaintext ONCE + persist hash only; `hash_recovery_code(plaintext)` uses `_ARGON2.hash(plaintext)` when `ARGON2_AVAILABLE` is True (same PasswordHasher instance + parameters as `hash_password`), otherwise falls back to `pbkdf2_sha256$<iter>$<salt_hex>$<digest_hex>` (self-describing string so verification routes correctly without a separate algorithm column); `verify_recovery_code(plaintext, stored_hash)` returns True on match — constant-time on both branches (`_ARGON2.verify` is constant-time internally; PBKDF2 fallback uses `hmac.compare_digest`). (3) **Backend wiring in `src/python/server.py`**: `mfa_enable` (L4672) — after the `mfa_enabled=1` flip and BEFORE the audit/notification fires, calls `generate_recovery_codes(10)`, wipes any stale codes from a prior MFA cycle, INSERTs 10 rows with hashed `code_hash`, and returns `{"enabled":True,"recovery_codes":[p for p,_ in pairs]}` so the dashboard can show them ONCE. NEW route `POST /api/account/mfa/recovery-codes/regenerate` (L4703 `mfa_recovery_regenerate`) — requires `currentPassword` (re-verified via `account_verify_password`); returns 401 `invalid_password` on wrong password, 409 `mfa_not_enabled` when MFA is off; otherwise DELETEs all existing codes, INSERTs 10 fresh ones, audits `mfa.recovery_codes_regenerated`, fires `send_security_notification(..., "mfa.recovery_codes_regenerated", ...)` (NEW event type added to sec-4's `_SECURITY_NOTIFICATION_*` dicts at L304-330 — purely additive, no existing event changed), returns `{"regenerated":True,"recovery_codes":[...]}`. NEW route `POST /api/auth/mfa/recover` (L4809 `mfa_recover`) — public, unauthenticated, added to the `csrf_exempt` set at L3476; body `{email, recovery_code}`; normalises the code (uppercase, hyphens optional so `AAAA-AAAA-AAAA` == `aaaaaaaaaaaa` == `AAAAaaaaaaaa`); looks up the user by email, fetches all `unused` rows for that user (≤10 rows — cheap), iterates with `verify_recovery_code(canonical, code_hash)` to find the match; on match: marks `used_at = now_ms` via `UPDATE ... WHERE id=? AND used_at IS NULL` (the NULL guard makes it a one-shot even under a race — if another request grabbed the same code first, rowcount=0 and we refuse to authenticate), emits `mfa.recovery_used` audit event, and issues a real session via `create_session` (same as `complete_mfa_login`); on no-match (or non-existent user — same response shape so no email enumeration): returns 401 `{"code":"invalid_recovery_code"}`. **Rate limiting**: 8/hour per EMAIL on the FAILURE path only (key `mfa-recover-fail:{email}`, same 8/hour ceiling as password-reset) — successful recoveries do NOT count against the budget so a legitimate user can consume all 10 of their codes in one recovery session without tripping the limit. Per-email (not per-IP) so a distributed attacker cannot bypass the ceiling by rotating sources. (4) **`security_overview` enhancement** (L4679 `GET /api/account/security`): the response now includes `recoveryCodesRemaining` (count of rows with `used_at IS NULL`); when MFA is enabled AND the count drops to ≤ 2, the response also includes `recoveryCodesLow: true` so the UI can warn the user to regenerate before they run out. The flag is suppressed for password-only accounts (no recovery codes by design — a True flag would be misleading noise). (5) **Frontend**: deferred per the task spec ("backend + tests are the priority"); the modal in `src/html/dashboard.html` + `src/js/mfa-recovery-codes.js` will be added in a follow-up (the backend contract is stable: `mfa_enable.recovery_codes` is the show-once list; `recovery_codes/regenerate` returns the new list; `GET /api/account/security` exposes the remaining count + low flag). (6) **Test** (`tests/security_mfa_recovery_test.py`, NEW — 7 phases, real-HTTP via in-process `ThreadingHTTPServer` so the test process can also reach into the SQLite DB directly via `server.connect()` to assert hashes are not plaintext): (A) register + setup + enable MFA → assert response contains 10 `recovery_codes` (well-formed `XXXX-XXXX-XXXX`, all from the 32-char alphabet, no duplicates); (B) verify DB has 10 rows in `mfa_recovery_codes` for the user — all `used_at IS NULL`, all `code_hash` ≠ any plaintext (plaintext NEVER stored), all hashes start with `$argon2` or `pbkdf2_sha256$`; (C) `GET /api/account/security` returns `recoveryCodesRemaining=10` and NO `recoveryCodesLow` flag; (D) consume each of the 10 codes via `/api/auth/mfa/recover` — 1st with canonical form, 2nd with lowercase no-hyphens (tests input normalisation), 3rd-10th canonical — all return 201 with a usable session; verify `used_at IS NOT NULL` on all 10 rows after; (E) 11th attempt with a REPLAYED code → 401 `invalid_recovery_code`; totally bogus code `XXXX-XXXX-XXXX` → 401 same shape; non-existent email → 401 same shape (no enumeration); (F) low-codes flag — fresh user, enable MFA, consume 8 of 10 codes via recovery → `recoveryCodesRemaining=2` AND `recoveryCodesLow=true`; (G) regenerate flow — wrong password → 401 `invalid_password`; correct password → 200 with 10 NEW codes that differ from the 2 leftover codes; DB has exactly 10 unused rows; OLD leftover codes no longer authenticate (each returns 401); a NEW code authenticates successfully; attempts to regenerate when MFA is OFF → 409 `mfa_not_enabled`; (H) audit log verification — `audit_events` for user A contains `mfa.enabled` + exactly 10 `mfa.recovery_used` events; for user B contains 9 `mfa.recovery_used` + 1 `mfa.recovery_codes_regenerated`. Pytest-compatible entry point `test_mfa_recovery_codes` + direct `python3` runner. Prints `SECURITY_MFA_RECOVERY_TEST_PASSED` on success. (7) **Acceptance**: test passes (`python3 -m pytest tests/security_mfa_recovery_test.py::test_mfa_recovery_codes -v` → 1 passed in 8.82s; direct runner → `SECURITY_MFA_RECOVERY_TEST_PASSED`); codes are hashed in DB (asserted by reading every row — no `code_hash` matches any plaintext); 10 codes work, 11th fails (401); regeneration invalidates old codes; `python3 -m py_compile src/python/server.py security_v13.py` exits 0; existing sec-4 notification test still passes (the new `mfa.recovery_codes_regenerated` event type is purely additive to the sec-4 dict — no existing event changed); bilingual EN+KH rule honored on the new notification event's subject + body. Commit style: `sec-3: MFA recovery codes (P1-C from ASVS L2)`. | `src/python/server.py` (L11 import extended with `generate_recovery_codes, hash_recovery_code, verify_recovery_code`; L304-330 NEW `mfa.recovery_codes_regenerated` entries in sec-4's `_SECURITY_NOTIFICATION_*` dicts — purely additive; L1355-1361 NEW `mfa_recovery_codes` table + index in SQLite schema; L3476 CSRF-exempt set extended with `/api/auth/mfa/recover`; L3514 + L3524 NEW route dispatch entries for `/api/auth/mfa/recover` + `/api/account/mfa/recovery-codes/regenerate`; L4672-4701 `mfa_enable` extended — generates + persists 10 codes, returns `recovery_codes` in response; L4703-4733 NEW `mfa_recovery_regenerate` handler; L4809-4869 NEW `mfa_recover` handler (public, rate-limited per-email on failure path, one-shot `used_at` UPDATE guard, `create_session` on success); L4679-4699 `security_overview` extended with `recoveryCodesRemaining` + `recoveryCodesLow` flag), `src/python/security_v13.py` (L69-173 NEW helpers: `_RECOVERY_ALPHABET`, `_generate_one_recovery_code`, `generate_recovery_codes`, `hash_recovery_code`, `verify_recovery_code`), `docs/postgres_schema.sql` (L434-447 NEW `mfa_recovery_codes` table + index with FK to `users` ON DELETE CASCADE), `tests/security_mfa_recovery_test.py` (NEW — 7-phase real-HTTP integration test using in-process `ThreadingHTTPServer`; direct `sqlite3.connect` via `server.connect()` for DB row inspection + audit_events inspection; covers mfa_enable returns 10 codes, all 10 work, 11th fails, regeneration invalidates old codes, low-codes flag at ≤2, regenerate-when-mfa-off returns 409, audit log verification), `VERSION_HISTORY.md` (V54.12 entry above V54.11) |
| V54.11 | sec-2: Per-account login lockout (P1-B from ASVS L2 gap analysis §2.2). Adds DB-backed per-account throttling on top of the existing IP-based rate limiter (`rate_limit("login:<ip>", 30, 600)` at `server.py` L4514). Without per-account throttling, an attacker rotating IPs (botnet, residential proxies, Tor) could make unlimited password guesses against a known account. **Schema**: 3 new columns on `users` — `failed_login_attempts INTEGER NOT NULL DEFAULT 0` (counter for the current burst), `failed_login_first_at BIGINT` (ms timestamp of the FIRST failure in the burst — anchors the sliding 15-min window; required because the task spec mandates "5 failures within 15 minutes" and explicitly tests "jump the clock forward 16 minutes" by manipulating this column), `locked_until BIGINT` (ms timestamp after which the account may retry; NULL when not locked). Added to SQLite `CREATE TABLE users` (L1291) + the `PRAGMA table_info(users)` ALTER migration block (L1405-1415, guarded by column-name check for idempotent legacy-DB upgrade), and to Postgres `CREATE TABLE users` (`docs/postgres_schema.sql` L22-27) + idempotent `ALTER TABLE users ADD COLUMN IF NOT EXISTS` migration statements (L771-776). **Login handler** (`src/python/server.py` L4537): (1) Before password verification, fetch the user row and inspect `locked_until`. If `locked_until > now`: emit `login.account_locked_attempt` audit event and return HTTP **423** (not 401) with `{"code": "account_locked", "message_en": "Account temporarily locked. Try again in 15 minutes or reset your password.", "message_km": "គណនីត្រូវបានចាក់សោជាបណ្ដោះអាសន្ន។ សូមព្យាយាមម្ដងទៀតក្នុងរយៈពេល 15 នាទី ឬកំណត់ពាក្យសម្ងាត់ឡើងវិញ។", "locked_until": <ts>}` — password verification is skipped so the wrong-password counter cannot grow unbounded while the lock is active. (2) If `locked_until` is set but `<= now` (lockout has expired): lazily emit `login.lockout_cleared` audit event with `{reason: "lazy_expiry"}`, reset the counter + first-failure-ts + lock-until to 0/NULL/NULL in DB, and refresh the in-memory row snapshot so the counter reads 0 in the subsequent verification step (no background sweeper). (3) On wrong password: apply the sliding-window logic — if `failed_login_first_at` is NULL OR `(now - failed_login_first_at) > 15*60*1000` ms, treat this failure as the start of a fresh burst (counter=1, `failed_login_first_at=now`); otherwise increment the counter and keep the existing first-at timestamp. If the new count reaches 5, set `locked_until = now + 15*60*1000` ms and emit `login.account_locked` audit event with `{failed_login_attempts, locked_until}`. The 401 response body is unchanged (`{"error": "Incorrect email or password"}`) so a lockout-triggering 5th failure does NOT reveal whether the account exists. (4) On successful login: if `failed_login_attempts > 0` OR `locked_until` was previously set, reset all three columns and emit `login.lockout_cleared` with `{reason: "successful_login"}` IF `locked_until` was previously set (avoids double-emission with the lazy-clear branch — only one path runs per request). (5) Manual DB clear of `locked_until=0` (admin override or password reset) is handled by the falsy check `if locked_until` (NULL or 0 both treated as "not locked"); the success path then resets the counter and `failed_login_first_at`. **Test** (`tests/security_account_lockout_test.py`, NEW — 7 phases using `v14_test_utils.app_server` real-HTTP integration: (1) register user with known password; (2) make 5 failed logins → all 401; (3) inspect `users` row directly via `sqlite3.connect` — `failed_login_attempts=5`, `locked_until` in the future; (4) verify `login.account_locked` audit event was emitted exactly once; (5) 6th attempt with CORRECT password → 423 with `code=account_locked` + bilingual EN/KH messages + `locked_until` echoed; 6th attempt with WRONG password → also 423 (still locked); (6) manually UPDATE `locked_until=0` in DB → correct password → 201 + counter reset to 0; (7) sliding-window test — 1 failure, manually set `failed_login_first_at = now - 16*60*1000` (jump the clock back 16 min), another failure → counter must reset to 1, NOT increment to 2; (8) persistence across server restart — uses an explicit `temporary_data()` dir + two sequential `app_server(extra_env={'EINVITE_DATA_DIR': str(data)})` invocations on the same DB file: trigger 5 failures on server 1, stop server 1, start server 2, verify login still returns 423 with the same `locked_until` value). Prints `SECURITY_ACCOUNT_LOCKOUT_TEST_PASSED` on success. **Acceptance**: test passes (`python3 -m pytest tests/security_account_lockout_test.py -v` → 1 passed; direct runner → `SECURITY_ACCOUNT_LOCKOUT_TEST_PASSED`); `python3 -m py_compile src/python/server.py` exits 0; lockout is DB-backed (survives server restart); 423 returned (not 401) when locked; 15-min sliding window works (counter resets after 15 min of no failures); existing sec-1 + sec-4 + p2a tests still pass (no regressions in `serve_public`, notification emails, or Phase 2a flows). Bilingual EN+KH rule honored on the lockout response body. Commit style: `sec-2: per-account login lockout`. | `src/python/server.py` (L1291 CREATE TABLE users + 3 new columns `failed_login_attempts`, `failed_login_first_at`, `locked_until`; L1405-1415 ALTER TABLE migration block guarded by PRAGMA table_info check; L4537-4608 login handler rewritten with lockout pre-check + sliding-window counter + lazy-clear + success-path reset), `docs/postgres_schema.sql` (L22-27 CREATE TABLE users + 3 new columns; L771-776 idempotent ALTER TABLE IF NOT EXISTS migration), `tests/security_account_lockout_test.py` (NEW — 7-phase real-HTTP integration test using `v14_test_utils.app_server`; direct `sqlite3.connect` for DB row inspection + audit_events inspection + manual column manipulation; restart-persistence phase uses two sequential server instances on the same DB file), `VERSION_HISTORY.md` (V54.11 entry above V54.10) |
| V54.10 | sec-4: Security notification emails on MFA / passkey / password changes (P1-D from ASVS L2 gap analysis §2.4). When MFA is enabled or disabled, when a passkey is added or removed, and when the account password is changed, the account holder receives a bilingual EN+KH email so that an attacker disabling MFA (or adding their own passkey) is detected immediately by the victim. (1) **New helper** `send_security_notification(user_email, event_type, ctx)` in `src/python/server.py` (module-level, near the existing `send_platform_email` at L325): bilingual subject + body templates for the five supported event types (`mfa.enabled`, `mfa.disabled`, `passkey.added`, `passkey.removed`, `password.changed`); body includes ISO 8601 UTC timestamp, requesting IP (`self.client_ip()`), User-Agent (`self.headers.get("User-Agent")[:500]`), and a "wasn't me" deep link to `/account/security` so the user can immediately revoke sessions / rotate credentials. Subject is bilingual (EN + KH joined by ` \| `); body has EN paragraph + KH paragraph + a separator block with the security context fields + the wasn't-me link in both languages. (2) **SMTP-not-configured fallback** (CRITICAL): when `send_platform_email` raises (e.g. SMTP host unset, network down) OR returns `False` (host/Mail-From env vars unset — the existing contract in `send_platform_email` at L265-287), the helper logs `[security_notification] SMTP not configured, skipping {event_type}` and returns `False`. The caller's security state change is NEVER rolled back by an email-delivery failure — otherwise an attacker disabling MFA could trivially evade the warning by misconfiguring SMTP. (3) **Wiring**: `mfa_enable` (L4654) → `send_security_notification(..., 'mfa.enabled', ...)` after the `mfa.enabled` audit event; `mfa_disable` (L4670) → `'mfa.disabled'` with stronger warning text in the body ("re-enable MFA immediately — without it, your account is protected by your password alone"); `passkey_register_complete` (L4687) → `'passkey.added'` after the `passkey.added` audit event; `delete_passkey` (L4760) → `'passkey.removed'` only when a row was actually deleted; `change_password` (L4406) → `'password.changed'` REPLACING the pre-existing `security_notification(user["email"], "Your password was changed", ...)` call (the old thin helper is retained for the login new-sign-in path owned by another agent). (4) **Test** (`tests/security_notification_emails_test.py`, NEW): real-HTTP integration test using an in-process `ThreadingHTTPServer` in a background thread so `unittest.mock.patch.object(server, 'send_platform_email', ...)` intercepts the helper's call from the handler thread (same Python process → same module namespace → patched name is visible). Covers all 5 event types — registers a user, sets up + enables MFA (using `security_v13.totp_code(secret, _totp_counter())` to compute the live TOTP code), disables MFA (fresh code), changes password via PUT `/api/auth/password`, adds a passkey (WebAuthn crypto bypassed by patching `verify_client_data` / `parse_attestation_object` / `cose_ec2_to_pem` in the server namespace — the notification wiring is what we test, not the attestation parser), lists passkeys, deletes one — and asserts each notification has the right recipient + subject containing both EN and KH variants + body containing `/account/security` link + `IP address:` + `Device / browser:` lines. Also tests the SMTP-not-configured fallback: switches `server.send_platform_email.side_effect` to raise mid-test, re-runs `mfa_enable` + password change, asserts both still return 200 (no exception propagation) and no new emails were captured. Prints `SECURITY_NOTIFICATION_EMAILS_TEST_PASSED` on success. (5) **Acceptance**: test passes; `python3 -m py_compile src/python/server.py` exits 0; emails fire on all four event types (mfa enable, mfa disable, passkey add, passkey remove, password change — five total); if SMTP is not configured the operation still succeeds (no failure propagation). Bilingual EN+KH rule honored: every subject has EN + KH variants; every body has EN paragraph + KH paragraph; wasn't-me link text appears in both languages. Commit style: `sec-4: security notification emails on MFA/passkey/password changes`. | `src/python/server.py` (L289-375 NEW `send_security_notification` helper + 5 template dicts `_SECURITY_NOTIFICATION_SUBJECTS` / `_SECURITY_NOTIFICATION_INTRO_EN` / `_SECURITY_NOTIFICATION_INTRO_KH`; L4418-4423 change_password wiring; L4662-4668 mfa_enable wiring; L4678-4684 mfa_disable wiring; L4711-4717 passkey_register_complete wiring; L4764-4772 delete_passkey wiring — all REPLACE the previous thin `security_notification(...)` call in change_password and add NEW notifications alongside the existing audit events for mfa/passkey), `tests/security_notification_emails_test.py` (NEW — 270 lines; in-process ThreadingHTTPServer + mock.patch.object pattern; covers all 5 event types + SMTP-not-configured fallback; uses security_v13.totp_code for live MFA codes; WebAuthn crypto bypassed via 3 mocked verifiers), `VERSION_HISTORY.md` (V54.10 entry above V54.9) |
| V54.9 | sec-1: Fix reflected HTML injection in `serve_public` (P1-A from ASVS L2 gap analysis). The slug from the URL was substituted into `<meta name="einvite-invitation-slug" content="__INVITATION_SLUG__">` and `<link rel="canonical" href="/i/__INVITATION_SLUG__">` without `html.escape(slug, quote=True)` — the only one of six placeholder substitutions in `serve_public` (server.py L7963) that lacked HTML escaping. CSP blocks script execution as a last-line defense, but defense-in-depth is violated, and the bug also blocks the WCAG `lang` fix on the public page (the unescaped slug could break out of the `<meta>` attribute and inject a real `<script>` tag). (1) **Fix**: wrapped the slug substitution with `html.escape(slug, quote=True)` (escapes `&`, `<`, `>`, `"`, `'` — safe in both attribute and text contexts). (2) **Audit of the other five placeholders**: `__INVITATION_TITLE__` and `__INVITATION_DESCRIPTION__` were already escaped (`title` comes from `document.fields.names` — host-controlled; `description` from `document.fields.message`); `__INVITATION_OG_IMAGE__` and `__INVITATION_PUBLIC_URL__` were already escaped and additionally depend on `self.absolute_url(...)` which builds the scheme/host prefix from `request_authority()` — the latter validates the `Host` header against ASCII printable chars and rejects `/`, `\`, `@`, `?`, `#`, `<>` and `"` (server.py L2633-2657), so no HTML-injectable chars can enter via the host portion; `__INVITATION_OG_TYPE__` is a static literal. No host-name or custom-domain substitution exists in `public.html` (the audit checklist in §2.1 mentioned these but the template uses only slug-based canonical). (3) **Regression test** (`tests/security_reflected_injection_test.py`): two scenarios — (A) host saves an invitation with title `<script>alert("title-xss")</script>` and description `"><img src=x onerror=alert("desc-xss")>`, publishes, GETs `/i/<slug>` and asserts the response body contains ONLY the HTML-escaped forms (never the raw `<script>` or `onerror=` substrings); (B) attacker crafts a URL with raw HTML chars in the path `/i/"><script>alert(1)</script>` — bypassing `urllib`'s automatic percent-encoding via a raw socket — and the test asserts the response body contains ONLY the escaped form `&quot;&gt;&lt;script&gt;alert(1)&lt;/script&gt;` (verified to appear in both the `<meta>` content attribute and the `<link>` href attribute). Confirmed the test FAILS without the fix (the raw slug breaks out of the attribute: `<meta content=""><script>alert(1)</script>">`) and PASSES with the fix applied. (4) **Acceptance**: test passes via `python3 -m pytest tests/security_reflected_injection_test.py -v`; `python3 -m py_compile src/python/server.py` exits 0; no raw `"` or `<` from user input appears unescaped in HTML context. Bilingual EN+KH rule: N/A (no user-facing strings added — defense-in-depth backend fix only). Commit style: `sec-1: fix reflected HTML injection in serve_public`. | `src/python/server.py` (L7963-7987 in `serve_public` — added `escaped_slug=html.escape(slug,quote=True)` + defense-in-depth comment block; all six placeholder substitutions now use `html.escape(..., quote=True)` for user-controlled values), `tests/security_reflected_injection_test.py` (NEW — 305 lines; two attack scenarios: title/description reflected XSS via host-saved invitation document, slug reflected XSS via raw-socket HTTP request bypassing urllib percent-encoding; pytest-compatible entry point `test_reflected_injection_in_serve_public` + direct `python3` runner; uses `v14_test_utils.app_server` for real HTTP server lifecycle), `VERSION_HISTORY.md` (V54.9 entry above V54.8) |
| V54.8 | Phase 5 — Hosted tier design (storage tiers + nonprofit discount + Canva bridge + onboarding flow + billing integration). (1) **Storage-tier pricing** (`docs/hosted/STORAGE-TIERS.md`): 3 tiers — Free ($0, 1 GB, 5 invitations, 100 guests, watermarked exports), Standard ($19/mo or $190/yr, 25 GB, 50 invitations, 1000 guests, 1 custom domain, no watermark), Pro ($99/mo or $990/yr, 250 GB, unlimited invitations, 10000 guests, 10 custom domains, 5 workspaces, 99.9 % SLA). **No per-guest fees anywhere** — the single clearest wedge against Paperless Post ($0.50–$1.44/guest coins), Evite (free + ads), Zola (print), and Canva (design-only). Storage overage $0.10/GB/month metered via Stripe Usage Records. (2) **Nonprofit discount** (§3): 50 % off Standard or Pro for verified nonprofits (501(c)(3) or equivalent — EIN/registration number + supporting documentation; re-verified every 24 months; aligns with self-hosted, community-friendly positioning). (3) **Canva bridge** (`docs/hosted/CANVA-BRIDGE.md`): import path (Canva URL via OAuth2 + Canva Connect API, or upload PNG/PDF) + export path (download Canva-compatible JSON or direct-import via Canva Connect API); asset mapping (Canva design → eInvite document pages; Canva asset → ObjectStorage key); explicit limitations table (animations dropped, brand kit colors inlined, smart mockups dropped, text effects best-effort, layered groups flattened); Phase 5.1 = import, Phase 5.2 = export, Phase 5.3 = live refresh (future). Tier gating: Standard + Pro only; Free cannot use the bridge. (4) **Onboarding flow** (`docs/hosted/ONBOARDING-FLOW.md`): 6-step flow — sign up (existing `/api/auth/register` + passkeys) → choose tier (Free immediate, Standard/Pro redirect to Stripe) → workspace creation (personal auto + optional org) → first invitation (template picker → editor → first save) → send first invitation (delivery channel picker, multi-channel via V54.3 `delivery_channels/`) → optional custom domain (V45 publishing-domains). Every user-facing label has bilingual EN+KH variants per ROADMAP ground rule 5. 6-stage conversion funnel with skip-and-return behavior. (5) **Billing integration** (`docs/hosted/BILLING-INTEGRATION.md`): reuses existing V12 provider-neutral billing adapter (`/api/billing/status|checkout|webhook` + `billing_orders`/`billing_events` tables + `EINVITE_BILLING_WEBHOOK_SECRET`); adds Stripe-specific subscription + metered billing via new routes — `GET /api/account/tier` returns tier + expiry + storage used + limits; `POST /api/account/tier/upgrade` body `{tier, billing_return_url, interval}` creates a Stripe Checkout session (mode='subscription', line_items=[tier_price, overage_price], metadata.user_id+metadata.tier for the webhook); `POST /api/billing/webhook/stripe` handles `checkout.session.completed` + `customer.subscription.updated` + `customer.subscription.deleted`, signature verification via `stripe.Webhook.construct_event` (production) OR HMAC-SHA256 `X-EInvite-Signature` fallback (CI without real Stripe credentials); idempotent via the existing `billing_events` table. Tier downgrade at end of period (Stripe `proration_behavior='none'`, no mid-period data loss). Cancellation drops to Free at period end + 30-day data grace period with email warnings at day 23/27/29. Metered storage overage: daily cron computes `SUM(size)` from `stored_objects` per workspace → writes `users.storage_used_bytes`; monthly Stripe Usage Record submission. (6) **Schema migration**: 5 new `users` columns — `tier TEXT NOT NULL DEFAULT 'free'` (parallel to legacy `plan` column; reconciliation `creator→standard, studio→pro` documented in STORAGE-TIERS.md §6; `plan` kept read-only for V54.8→V56 backwards-compat window, dropped in V56), `tier_expires_at BIGINT`, `storage_used_bytes BIGINT NOT NULL DEFAULT 0`, `stripe_customer_id TEXT`, `stripe_subscription_id TEXT`. SQLite path: added to `CREATE TABLE users` block + ALTER TABLE migration block in `connect_sqlite()`. PostgreSQL path: added to `docs/postgres_schema.sql` CREATE TABLE block + idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` migration statements. (7) **Configuration**: 13 new env vars in `deploy/.env.example` + `deploy/.env.production.example` — `EINVITE_STRIPE_SECRET_KEY`, `EINVITE_STRIPE_PUBLISHABLE_KEY`, `EINVITE_STRIPE_WEBHOOK_SECRET`, 5× `EINVITE_STRIPE_PRICE_*` (standard/pro × month/year + overage), 4× `EINVITE_CANVA_*` (client_id, client_secret, redirect_uri, api_base). `stripe` Python SDK is a deploy-time dependency (`pip install stripe`) — not added to requirements files in this design round. (8) **Backend scaffolding** (`src/python/server.py`): new constants `STORAGE_TIER_LIMITS` (3 tiers × 8 fields each), `STORAGE_TIER_PRICES` (standard/pro × month/year minor units), `STORAGE_OVERAGE_USD_PER_GB=0.10`, `STRIPE_*` config + `STRIPE_CONFIGURED` flag, `CANVA_*` config; new methods `_normalize_tier()` (legacy plan→tier mapping), `account_tier()` (GET handler), `account_tier_upgrade()` (POST handler — 503 `stripe_not_configured` when env vars unset, real `stripe.checkout.Session.create` when configured), `billing_webhook_stripe()` (POST handler — dual-mode signature verification via `stripe.Webhook.construct_event` OR HMAC-SHA256 fallback for CI); CSRF exemption list extended with `/api/billing/webhook/stripe` (Stripe cannot carry the host's session cookie). (9) **End-to-end smoke test** (run during development, not committed as a test file): registered a user → GET `/api/account/tier` returned `tier='free', storageLimitBytes=1073741824, watermarkExports=True, stripeConfigured=False, prices={...}}`; POST `/api/account/tier/upgrade` returned `503 stripe_not_configured` (no Stripe env); POST `/api/billing/webhook/stripe` with valid HMAC signature promoted the user `free→pro` and set `stripe_customer_id`, `stripe_subscription_id`, `tier_expires_at`; duplicate webhook returned `200 {duplicate:true}` (idempotent); cancellation webhook demoted `pro→free`. All assertions passed. (10) **Design principle**: "Storage-tier pricing with no per-guest fees" per ROADMAP §8 — this is the single clearest wedge against competitors. Self-hosting remains the ultimate tier-cap escape hatch (Free-tier limits apply automatically to self-hosted users, aligning with the community-friendly positioning). | `docs/hosted/STORAGE-TIERS.md` (3 tiers + pricing + nonprofit discount + upgrade/downgrade flow + reconciliation policy with legacy `plan` column), `docs/hosted/CANVA-BRIDGE.md` (import + export pipelines + OAuth2 + asset mapping + limitations table + tier gating), `docs/hosted/ONBOARDING-FLOW.md` (6-step flow with bilingual EN+KH labels + 6-stage conversion funnel + skip-and-return), `docs/hosted/BILLING-INTEGRATION.md` (Stripe Checkout + Subscriptions + metered Usage Records + 30-day data grace + nonprofit application route sketch + 5 new users columns), `src/python/server.py` (L127-164 new constants + L1209 CREATE TABLE users with 5 new columns + L1307-1315 ALTER TABLE migration + L3198 new GET route + L3377 CSRF exemption for stripe webhook + L3456-3458 new POST routes + L3922-4152 new methods `_normalize_tier` + `account_tier` + `account_tier_upgrade` + `billing_webhook_stripe`), `docs/postgres_schema.sql` (L4-22 CREATE TABLE users with 5 new columns + L740-749 ALTER TABLE IF NOT EXISTS migration), `deploy/.env.example` (L142-160 new Phase 5 env vars), `deploy/.env.production.example` (L144-163 new Phase 5 env vars with REPLACE_ placeholders) |
| V54.6 | Phase 4a — Plugin marketplace governance. Spec + SDK + example plugin + stdlib-only validator. (1) **Manifest spec** (`docs/plugins/PLUGIN-SPEC.md`): declarative JSON manifest (schema_version=1) with required fields (`name`, `name_kh`, `version` semver 2.0.0, `author` w/ `vendor_id`, `author_key_id` = SHA-256 of Ed25519 pubkey, `permissions[]` resource-scoped, `extension_points[]`, `entrypoint`, `min_platform_version` / `max_platform_version` semver range, `content_security_policy` per-plugin, `signature` + `marketplace_signature` Ed25519 blocks); recursive forbidden-field scan extends the existing V48 `forbidden` set (`src/js/plugin-runtime-v48.js`) with DOM-API tokens (`innerHTML`, `outerHTML`, `document_cookie`, `window_location`); resource-scoped permissions reuse the Phase 1a grammar from `docs/ai/RESOURCE-SCOPED-PERMISSIONS.md` §2.1 (`{resource_type}:{resource_id}:{action}[:{sub_resource}[:{sub_id}]]`); wildcard resource grants (`*`) are rejected; legacy V48 13 coarse tokens (`project.read`, `assets.insert`, etc.) mapped to the new grammar for a V54.6→V55 transition window. (2) **13 extension points** enumerated (11 inherited from V48 `allowedPoints` in `src/js/plugin-runtime-v48.js` — `editor.panel`, `content.block`, `asset.provider`, `export.provider`, `communication.provider`, `payment.provider`, `analytics.provider`, `storage.provider`, `ai.provider`, `calendar.provider`, `map.provider` — plus 2 new in Phase 4a: `template.provider` and `automation.provider`, both gated to Verified Vendor status per `MODERATION-PIPELINE.md` §5), each with input/output contract. (3) **JSON Schema** at `plugins/sdk/manifest.schema.json` (draft 2020-12, `additionalProperties:false` at top level, `$defs` for `permission` / `extension_point` / `csp` / `signature`). (4) **Double-signing spec** (`docs/plugins/PLUGIN-SIGNING.md`): author Ed25519 + marketplace CA Ed25519, both required at install + launch; canonical payload = JCS-canonicalised manifest with `signature`+`marketplace_signature` set to `{}` + bundle_sha256 + bundle_size; signed_payload `manifest+bundle` (author) and `manifest` (marketplace); marketplace `approved_permissions` subset intersection; signed CRL endpoint `/_marketplace/crl.json` checked every 24h; author key rotation via dual-signed rotation record; marketplace CA rotation requires platform release with 6-month overlap window; dev-only unsigned plugins gated on `EINVITE_ALLOW_UNSIGNED_PLUGINS=1` (rejected by `production_preflight`, same pattern as `EINVITE_ALLOW_NO_SCANNER=1`). (5) **Sandbox spec** (`docs/plugins/PLUGIN-SANDBOX.md`): UI plugins = cross-origin iframe at `plugins.einvite.local` with `sandbox="allow-scripts"` (NO `allow-same-origin` — opaque origin protects host DOM/cookies/localStorage) + `credentialless` + `referrerpolicy="no-referrer"` + per-load nonce + per-plugin CSP (intersected with host base CSP); logic plugins = WASM or JS in a dedicated Web Worker with minimal import object (no `fetch`/`XMLHttpRequest`/`WebSocket`/`crypto.subtle`/`setTimeout`); `MessageChannel`+`postMessage` protocol with 3 message types (request/response/error); 13 host-bridge methods (`invitation.read`, `invitation.update`, `invitation.publish`, `guest.list`, `guest.update_rsvp`, `message.send`, `asset.upload`, `asset.read`, `event.read`, `event.create_automation`, `template.install`, `plugin.bridge`, implicit `locale.get`/`quota.get`/`log.write`); resource limits (50 MB heap, 100 ms CPU/call, 5 MB bundle, 100 req/s, 1000 outstanding) enforced via token-bucket + WASM linear-memory cap + Worker memory polling. (6) **Moderation pipeline** (`docs/plugins/MODERATION-PIPELINE.md`): 4-stage pipeline (automated pre-upload → human review → marketplace CA signing → post-approval monitoring); automated checks cover schema validation, dependency audit (allow-list of CDN domains, `https://` only, no query params), 23 suspicious-pattern detectors (`eval(`, `new Function(`, `document.cookie`, `.innerHTML=`, `setTimeout("string")`, `fetch("https://...")`, `new XMLHttpRequest/WebSocket/Worker/SharedWorker`, `navigator.serviceWorker`, `importScripts(`, `WebAssembly.compileStreaming(`, base64 >4 KB, dynamic `import(`), bundle size (5 MB), entrypoint signature verification, CSP audit, permission audit, bilingual completeness (every user-facing string must have EN+KH variants, Khmer cannot be byte-identical to English); human review = 4-pass audit (source code, permissions, security, UX) with separation-of-duty (takedown maintainer ≠ approval maintainer); post-approval takedown via CRL within 24h; user reports via `POST /api/plugins/{id}/report` with anti-abuse rate limiting; **Verified Vendor badge** (§5) requires 4 artifacts (business license, photo ID, bank statement, DNS `_einvite-verify` TXT record), grants expedited review + badge in marketplace UI + eligibility for `template.provider`/`automation.provider` extension points. (7) **Plugin SDK** (`plugins/sdk/`): `README.md` (bilingual EN+KH getting-started guide per ROADMAP ground rule 5); `manifest.schema.json` (canonical JSON Schema, also reproduced in PLUGIN-SPEC.md §6); `einvite-plugin.js` (framework-free host-bridge shim — lifecycle hooks `onReady`/`onContextChanged`/`onLocaleChanged`/`onSuspend`/`onResume`/`onUninstall`, 13 host-bridge methods, internal bilingual EN+KH error strings, idempotent global, supports both iframe + Worker contexts); `example-plugin/` (minimal confetti-animation plugin with `manifest.json` + `index.html` + `index.js` + tests); `validate_manifest.py` (stdlib-only Python validator with hand-rolled JSON Schema subset + recursive forbidden-field scan + permission grammar check + CSP audit + bilingual check + Ed25519 signature verification via `openssl pkeyutl` subprocess with pure-Python RFC 8032 fallback — single source of truth used by both the marketplace submission pipeline and the host install path). (8) **Tests**: `plugins/sdk/example-plugin/tests/test_example_plugin.py` (16 stdlib-only assertions: manifest loads, validator passes on clean manifest, all required fields present, schema_version=1, permission grammar, extension-point enum, bilingual completeness, CSP no unsafe tokens, forbidden-field scan catches injected `eval` at top level + nested `innerHTML` inside `extension_points[].schema.properties`, wildcard permission rejected, unknown extension point rejected, marketplace signature is the all-zeros placeholder, entrypoint .html file exists, SDK shim is loadable + clean of forbidden patterns, example-plugin index.js is clean of forbidden patterns — `EXAMPLE_PLUGIN_TEST_PASSED`). (9) **Design principle**: "Do it differently from JetBrains" per ROADMAP §7 4a — JetBrains plugins run with full IDE privileges (no sandbox, no fine-grained permissions); eInvite's users are non-technical event hosts, so third-party plugins are sandboxed + scoped from day one with no escape hatch and no "trusted plugin" mode. (10) The V48 runtime (`src/js/plugin-runtime-v48.js`) and platform UI (`src/js/plugin-platform-v48.js`) are NOT modified — Phase 4a layers the sandbox + manifest spec + signing on top; the existing V48 `registerManifest()` UI in `src/js/plugin-platform-v48.js` continues to serve as the dev-only entry point for unsigned development plugins. | `docs/plugins/PLUGIN-SPEC.md` (manifest format + JSON Schema + 13 extension points with I/O contracts + permission grammar + per-plugin CSP + semver + platform-compat range + worked example), `docs/plugins/PLUGIN-SIGNING.md` (Ed25519 double-signing + canonical payload via JCS + marketplace approval subset + signed CRL endpoint + key rotation for both author + CA + pinned CA key in `src/python/plugin_marketplace_ca.py` follow-up), `docs/plugins/PLUGIN-SANDBOX.md` (cross-origin iframe w/ `sandbox="allow-scripts"` + WASM-in-Worker + MessageChannel protocol + 13 host-bridge methods + resource limits + 13 explicitly-refused capabilities), `docs/plugins/MODERATION-PIPELINE.md` (4-stage pipeline + 8 automated checks + 4-pass human review + post-approval takedown + Verified Vendor badge + 5 maintainer roles + SLAs), `plugins/sdk/README.md` (bilingual EN+KH getting-started), `plugins/sdk/manifest.schema.json` (draft 2020-12 JSON Schema), `plugins/sdk/einvite-plugin.js` (host-bridge shim, framework-free, idempotent, supports iframe + Worker contexts, bilingual EN+KH error strings), `plugins/sdk/validate_manifest.py` (stdlib-only Python validator with hand-rolled JSON Schema subset + recursive forbidden scan + permission grammar + Ed25519 verifier via openssl + pure-Python RFC 8032 fallback), `plugins/sdk/example-plugin/manifest.json` (minimal confetti-animation manifest, V54.6-compatible), `plugins/sdk/example-plugin/index.html` (UI entrypoint w/ nonce placeholder for script tags), `plugins/sdk/example-plugin/index.js` (plugin logic, bilingual EN+KH strings), `plugins/sdk/example-plugin/tests/test_example_plugin.py` (16 stdlib-only assertions) |
| V54.7 | Phase 4b — Y.js CRDT upgrade (initial migration; coexists with V31 during Phase A). (1) **Design doc** (`docs/collab/CRDT-DESIGN.md`, 13 sections): why Y.js (90 % of new 2026 products per ROADMAP §10 step 6); 3-phase migration plan (Phase A dual-write — V31 authoritative, Phase B dual-read — Y.js authoritative for new sessions, Phase C V31 removed); Y.js document model mapping (Y.Map for registers, Y.Array for sequences, Y.Text for character-level concurrent edits in rich-text runs); snapshot publishing (Y.js state vector → V32 immutable publications table); backwards compat for reading old `collaboration_updates` V31 rows (`migratedFrom:'v31'` flag in V52 snapshot response); Mermaid sequence diagram of the new V52 collaboration flow (live editing + offline merge); risks table. (2) **y-indexeddb integration** (`src/js/crdt-yjs-indexeddb.js`): wraps `y-indexeddb` for per-invitation IndexedDB persistence; database `einvite-yjs-collab` with 3 object stores (`documents`, `updates`, `meta`); 30 s / 100-update compaction cycle rewrites snapshot + trims update log; localStorage fallback (256 KB queue) when IndexedDB unavailable; FNV-1a fingerprint matches V31 algorithm for cross-version debug parity. (3) **Per-user UndoManager** (`src/js/crdt-yjs-undo.js`): `Y.UndoManager` with `trackedOrigins = new Set([localClientId])` — one user's `Cmd+Z` does NOT revert another user's work (the core V31 limitation called out in ROADMAP §7.4b); 500 ms capture window coalesces rapid ops; bilingual EN+KH announcement labels (EN: "Undo"/"Redo"/"Nothing to undo", KH: "មិនធ្វើវិញ"/"ធ្វើវិញ"/"មិនមានអ្វីដែលត្រូវមិនធ្វើវិញ"). (4) **Dedicated presence channel** (`src/js/collaboration-presence-v52.js`): separate ephemeral channel from content sync — heartbeat 3 s (was 5 s in V31), grace period 7 s (within ROADMAP's 5–10 s window); WebSocket transport with short-poll fallback; broadcasts `{actor, name, color, avatarUrl, cursor, selection, mode}`; idle detection at 30 s (`mode:'idle'`); 30 s reaping of stale remote entries; bilingual EN+KH labels. (5) **Rich media sync** (`src/js/crdt-yjs-rich-media.js`): custom Y.js types for image + embed elements — stores the asset REFERENCE (V32 object_key string) in the CRDT, NEVER the bytes (bytes flow through V32 `ObjectStorage`); resize handles as nested Y.Map of `{x,y,width,height,rotation}` — drag throttled to 60 Hz local render + 10 Hz CRDT replication; concurrent-drag resolution via Y.Map register last-write-wins; `assertNoAssetBytes(ydoc)` CI guard throws if a `data:` URL is found in the doc. (6) **Backend endpoints** (`GET/POST /api/invitations/{id}/collaboration/v52/{snapshot,updates,presence}` + `POST .../snapshot` publish hook): Y.js binary `Uint8Array` update stored as opaque BLOB in new `collaboration_updates_v52` table (id, workspace_id, invitation_id, document_epoch, client_id, logical_clock, "update" BLOB, update_bytes, created_at, revision; unique on invitation_id+document_epoch+client_id+logical_clock for idempotent retry); V31 endpoints unchanged; Phase A dual-write opt-in via `EINVITE_COLLAB_V52_DUALWRITE=1` env var (off by default — V52 endpoint stores binary only and does NOT mutate `invitations.draft_json` during Phase A). (7) **Schema migration** (`platform_v32/schema.py` SCHEMA_VERSION 18→19, `docs/postgres_schema.sql` additive tables — no DROP/ALTER to existing tables). (8) **Offline-merge test suite** (`tests/v52_crdt_offline_merge_test.py`, 6 scenarios via `v14_test_utils.app_server` real-HTTP server + dependency-free `YjsShim` binary encoder): (1) two users each produce 5 offline updates → both reconnect → 10 updates merged with monotonic revisions; (2) concurrent edit + delete of same element → both ops stored server-side, delete-wins enforced client-side per Y.js CRDT semantics; (3) three users × 3 concurrent inserts → 9 inserts merged, 3 clients agree, revisions unique + sorted; (4) simulated 1-hour offline reconnect → 50 remote updates + 3 local updates merged cleanly, no `EPOCH_MISMATCH`; (5) idempotent submission of same `(clientId, clock)` returns existing revision with `duplicate:true`; (6) Y.js snapshot publish round-trip — save state vector + snapshot, re-join returns it, decodes to identical payload. (9) **Vendor Y.js self-host** (`vendor/yjs/README.md` + `vendor/yjs/download.sh`): since the project has no npm/bundler, documents how to download pinned `y.js@13.6.27` + `y-indexeddb@9.0.12` + `lib0@0.2.99` from unpkg CDN with SRI `sha384` hashes; script auto-computes integrity hashes for `<script integrity="...">` tags in editor.html + dashboard.html; CSP `script-src 'self'` already permits vendored scripts. All four new V52 modules gracefully detect missing `window.Y` and emit a clear error pointing at the vendor README (rather than silently throwing). Bilingual EN+KH strings in all four modules per ROADMAP ground rule 5. | `docs/collab/CRDT-DESIGN.md` (13 sections + Mermaid sequence diagram), `src/js/crdt-yjs-indexeddb.js` (y-indexeddb wrapper + IndexedDB schema doc), `src/js/crdt-yjs-undo.js` (per-user UndoManager with trackedOrigins), `src/js/collaboration-presence-v52.js` (dedicated ephemeral presence channel, 3 s heartbeat + 7 s grace), `src/js/crdt-yjs-rich-media.js` (custom Y.js type for images/embeds/resize handles + assertNoAssetBytes CI guard), `src/python/server.py` (5 new V52 routes at L3032-3096 + L3315 path-guard update), `platform_v32/service.py` (8 new V52 methods: `collaboration_snapshot_v52`, `collaboration_updates_v52`, `append_collaboration_update_v52`, `presence_update_v52`, `presence_list_v52`, `save_collaboration_snapshot_v52`, `_v52_update_row`, `_fingerprint_bytes`), `platform_v32/schema.py` (SCHEMA_VERSION 18→19, 2 new tables `collaboration_updates_v52` + `collaboration_snapshots_v52` with 4 indexes), `platform_v32/config.py` (new `collab_v52_dualwrite: bool` field + `EINVITE_COLLAB_V52_DUALWRITE` env var), `docs/postgres_schema.sql` (additive V52 tables at L559-576), `tests/v52_crdt_offline_merge_test.py` (6-scenario real-HTTP integration test, dependency-free `YjsShim` binary encoder), `vendor/yjs/README.md` + `vendor/yjs/download.sh` (CDN self-host instructions + SRI hash computation) |
| V54.5 | Phase 3 — Production Certification framework. (1) **Native platform matrix** (`docs/certification/NATIVE-PLATFORM-MATRIX.md`): clean install + V53.1→V54 upgrade + V54→V53.1 rollback on Windows 11 + Windows Server 2022 + Windows Server 2025 + Ubuntu 22.04 LTS + Ubuntu 24.04 LTS + Debian 12. Acceptance gates verified by the existing `tests/v14_live_server_acceptance_test.py` (Linux runtime, 210-line real-HTTP walkthrough) and `tests/v16_windows_ui_hardening_test.py` (Windows portability, CRLF + SIGBREAK + UTF-8 env assertions). (2) **Browser matrix** (`docs/certification/BROWSER-MATRIX.md`): Chrome + Firefox (ESR + stable) + Safari + Edge desktop, Safari iOS + Chrome Android mobile. Cross-browser feature matrix (`:has()`, `aspect-ratio`, WebAuthn ES256, WebCrypto subtle ECDSA, WebGL 2.0). Existing automated coverage via `tests/browser_runtime.py` (Chromium launcher) + `tests/inline_editor_runtime_test.py` (editor smoke). (3) **Penetration test scope** (`docs/certification/PEN-TEST-SCOPE.md`): OWASP ASVS 5.0.0 Level 2 (not L1 — handles guest PII); black-box + grey-box + white-box methodology; targets the ~150 routes in `server.py`, the 80 AI agent tools (AISVS C9/C10 cross-reference), and the V32/V52 platform endpoints. Links the Phase 1b gap analysis (`docs/security/ASVS-L2-GAP-ANALYSIS.md`) as the input. (4) **Load test plan** (`docs/certification/LOAD-TEST-PLAN.md` + `tests/load_test_k6.js`): k6 (preferred over Locust — single binary, JS-based, deterministic VU model). Four scenarios — RSVP burst (1000 VUs / 5 min, wedding-season spike), AI agent tool calls (50 VUs / 2 min), concurrent collaboration edits (20 VUs / 3 min, worst-case contention with 409 retry loop), public page views (200 VUs / 1 hr). Acceptance: p95 < 500ms RSVP, < 2s AI agent, zero 5xx. Reference deployment: 2 vCPU / 4 GB RAM / PG 15 / MinIO / Caddy. (5) **Second quarterly DR drill** (extended `docs/ops/BACKUP-DR.md` §9 — the first was Phase 1c §5): scenario = simulated primary DB corruption (`rsvps.name=NULL`) at 02:00 UTC; canaries extended with `audit_events` hash-chain integrity + `rsvps.name IS NOT NULL` checks; dedicated drill-report template with separation-of-duties witness sign-off. (6) **Sign-off** (`docs/certification/CERTIFICATION.md` §6): maintainer + external auditor signature blocks; only after all 5 sections pass. Bilingual EN+KH load-test report template fields. | `docs/certification/CERTIFICATION.md` (§1-§6, sign-off + change history), `docs/certification/NATIVE-PLATFORM-MATRIX.md`, `docs/certification/BROWSER-MATRIX.md`, `docs/certification/PEN-TEST-SCOPE.md`, `docs/certification/LOAD-TEST-PLAN.md`, `tests/load_test_k6.js` (4-scenario k6 script: `rsvp_burst`, `ai_tool_calls`, `collab_edits`, `public_views` with thresholds + setup/teardown + retry loop for 409), `docs/ops/BACKUP-DR.md` (extended §8 change history + new §9 second quarterly DR drill) |
| V54.4 | Phase 2a (part 2) — Shared photo album + post-send editing. (1) **Shared photo album**: `GET/POST /api/invitations/{id}/album` (cursor-paginated list + multipart upload), `DELETE /api/invitations/{id}/album/{photo_id}` (uploader or host), `POST /api/invitations/{id}/album/{photo_id}/moderate` (host sets `pending|approved|hidden`). Bytes flow through `security_scanner_v54.scan_bytes` BEFORE being stored in `ObjectStorage`; `MalwareDetected` returns HTTP 422. New `album_photos` table. (2) **Post-send editing** (wedge vs Paperless Post + Evite, both of which lock after send): `PUT /api/invitations/{id}` no longer blocks after `sent_at`; instead bumps `document_version`, writes a row to new `invitation_edit_history` table (id, invitation_id, edited_by, edited_at, diff_json, reason, document_version) with a small structured diff (added/changed/removed field paths, capped at 50 KB), and sets `invitations.edited_after_send_at` on the FIRST post-send edit (COALESCE — never overwritten). New `GET /api/invitations/{id}/edit-history` (host-only, paginated) and `POST /api/invitations/{id}/resend-notification` (host triggers an "invitation updated" email to already-viewed guests via existing SMTP pipeline). Frontend: `src/js/album.js` (upload button + progress, lazy-loaded IntersectionObserver grid, delete-own + host-moderate, bilingual EN+KH) and `src/js/invitation-edit-history.js` (public "Edited after sending" badge + host history view with diff rendering + resend-notification button, bilingual EN+KH). Tests: `tests/p2a_album_test.py` (upload + list + delete + moderate + **EICAR test signature rejection at HTTP 422** via dev-only `EINVITE_TEST_SCANNER_EICAR=1` env var gated on `EINVITE_ALLOW_NO_SCANNER=1`) and `tests/p2a_post_send_edit_test.py` (edit-after-send bumps version + writes edit-history row + `edited_after_send_at` set on first edit only and pinned across subsequent saves). | `src/python/server.py` (schema block at L1090/L1296/L1302, `save_draft` at L4549, `upload_album_photo` at L7309 with EICAR test-mode hook at L7332, `list_edit_history` at L7452, `resend_notification` at L7478, `get_public` at L4748 surfaces `editedAfterSendAt`), `docs/postgres_schema.sql` (V54.4 ALTER TABLE invitations ADD COLUMN sent_at + edited_after_send_at), `src/js/album.js`, `src/js/invitation-edit-history.js`, `src/js/public-page.js` (mounts EInviteEditHistory badge), `docs/route-bundle-sources-v15.json` (added `invitation-edit-history.js` to `public.html` and `dashboard.html` bundles), `tests/p2a_album_test.py`, `tests/p2a_post_send_edit_test.py`, `src/python/bundle-public-v15.js` + `bundle-dashboard-v15.js` (rebuilt) |
| V54.3 | Phase 2a (part 1) — Multi-channel delivery abstraction + sign-up sheets + polls. Pluggable `delivery_channels/` package (stdlib-only `urllib.request` adapters for email/SMS/WhatsApp/Telegram — no third-party SDKs) wraps the existing SMTP pipeline as the default email channel and gracefully skips unconfigured adapters; `POST /api/invitations/{id}/deliver` + `GET /api/invitations/{id}/deliveries` + `GET /api/invitations/{id}/delivery-channels` endpoints persist attempts in the new `delivery_attempts` table. Sign-up sheets (`signup_sheets`/`signup_claims` tables, host CRUD + guest claim/cancel endpoints, capacity enforcement, deadline-aware). Polls (`polls`/`poll_votes` tables, host CRUD + guest vote + aggregated results with `hidden_until_close`/`live` visibility, single-select vote replacement, multi-select). Frontend: `src/js/signup-sheets.js` + `src/js/polls.js` + `src/js/delivery-dialog.js` (bilingual EN+KH strings per ROADMAP ground rule 5). Tests: `tests/p2a_delivery_channels_test.py` + `tests/p2a_signup_sheets_test.py` + `tests/p2a_polls_test.py` (real-HTTP integration via `v14_test_utils.app_server`). | `src/python/delivery_channels/` (7 modules: `base.py`, `email_channel.py`, `sms_channel.py`, `whatsapp_channel.py`, `telegram_channel.py`, `registry.py`, `__init__.py`), `src/python/server.py` (schema block at L1090, routes at L3184-3431, handlers at L6863-7622), `src/js/signup-sheets.js`, `src/js/polls.js`, `src/js/delivery-dialog.js`, `tests/p2a_delivery_channels_test.py`, `tests/p2a_signup_sheets_test.py`, `tests/p2a_polls_test.py`, `docs/route-bundle-sources-v15.json`, `src/python/bundle-public-v15.js` (rebuilt) |
| V54 | On-startup fail-closed malware scanner (ClamAV INSTREAM on Linux, MpCmdRun.exe on Windows), auto-secret generation into repo-root `.env` (mode 0600, never overwrites non-placeholder values), production preflight enforcement, editor chrome refactor, UX bug fixes. | `src/python/security_scanner_v54.py`, `src/python/secrets_v54.py`, `src/python/production_preflight.py`, `src/python/dependency_preflight.py`, `deploy/linux/install-einvite-laptop.sh`, `deploy/.env.example`, `deploy/.env.production.example`, `docs/SECURITY_HARDENING_REPORT_2026-08-10.md`, `docs/PYTHON_9009_FIX_README.txt` |
| V54.2 | Phase 2b — Khmer typography + WCAG 2.1 AA audit: W3C Khmer Script Resources implementation guide (`docs/i18n/KHMER-TYPOGRAPHY.md`), font registry manifest (`assets/fonts/registry.json` — 7 vetted Khmer-safe fonts, OFL-1.1, SHA-256, OpenType features), WCAG 2.1 AA audit of all 16 HTML pages (`docs/a11y/WCAG-AA-AUDIT.md` — 60.5% pass, 7 systemic fails remediated). Remediation applied: light-mode `--text-3`/`--app-faint` contrast bumped from 3.15:1/2.6:1 → 4.96:1 (AA-normal pass), skip-to-content link added to all 16 pages, `<main id="main" tabindex="-1">` landmark added to all 16 pages (dashboard.html wrapped in `<main>`). Variable-font migration plan documented (waiting on maintainer download per `docs/i18n/KHMER-TYPOGRAPHY.md §3.3`). | `docs/i18n/KHMER-TYPOGRAPHY.md`, `docs/a11y/WCAG-AA-AUDIT.md`, `assets/fonts/registry.json`, `src/css/tokens.css`, `src/css/modern-ui.css`, `src/css/accessibility-v12.css`, `src/css/organized/styles.css`, `src/js/font-browser.js`, `src/html/*.html` (16 pages) |

---

## Current State (V53.1 + V54 hardening)

The platform is production-ready with:

- ✅ Visual invitation design with bilingual support (English + Khmer)
- ✅ Comprehensive guest management and RSVP system
- ✅ Team collaboration with review/approval workflows (CRDT since V31)
- ✅ AI-assisted design and content generation (V28, V35, V53.1)
- ✅ Template marketplace with versioning (V36)
- ✅ Plugin extension system (V48)
- ✅ Event automation and operations management (V52)
- ✅ Self-hosted deployment (Linux/Windows/Docker, SQLite or PostgreSQL)
- ✅ Enterprise-grade security and compliance (Argon2id, MFA, passkeys, CSP, audit events)
- ✅ Malware scanning on every upload (V54, fail-closed)
- ✅ Automated backups and disaster recovery (V32 + V54 preflight)
- ✅ Systemd service integration (Linux)
- ✅ One-command installation scripts

---

## Deprecated scripts & files

The following version-specific scripts have been consolidated into unified deployment tools:

### Removed version-specific runners
- All `RUN_V*_GATE_3X.*` scripts (V20–V28)
- All `RUN_V*_RELEASE_CHECK_*` scripts (V18–V28)
- All `V*_WINDOWS_READINESS.*` scripts (V26–V27)

### Replaced by
- **Linux**: `deploy/linux/install-einvite-laptop.sh` — unified installer
- **Windows**: `scripts/setup-einvite-complete.ps1` — complete setup
- **Windows**: `scripts/host-einvite-laptop.ps1` — laptop hosting
- **Backup**: `deploy/linux/backup-einvite.sh` — automated backups

---

## Migration notes

If you were using version-specific scripts:
1. Delete all old `RUN_V*` and `V*_READINESS` scripts.
2. Use the new unified installation scripts.
3. Refer to `LINUX_LAPTOP_HOSTING.md` or `docs/FIRST_TIME_INSTALL_AND_HOSTING.md` for guidance.

---

## Support

For deployment assistance, refer to:
- `README.md` — quick start guide
- `docs/LINUX_LAPTOP_HOSTING.md` — Linux laptop hosting
- `docs/FIRST_TIME_INSTALL_AND_HOSTING.md` — general setup
- `docs/PRODUCTION_DEPLOYMENT.md` — production deployment guide

---

*Phase 0.1 deliverable of `docs/ROADMAP.md`. Verified against actual code 2026-09-14.*
